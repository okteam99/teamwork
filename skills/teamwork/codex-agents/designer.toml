name = "designer"
description = "[DEPRECATED v7.3.10+P0-104] Teamwork Designer profile. Teamwork main flow no longer dispatches this profile (OpenAI ToS compliance). External models in teamwork are review-only. Kept for manual ad-hoc use only · sandbox forced to read-only."

# 🔴 DEPRECATED v7.3.10+P0-104：详见 standards/external-model-usage.md
# UI Design Stage 由主对话 Designer 角色身份切换执行（不再用 codex 跑 designer）。

developer_instructions = """
[DEPRECATED] Teamwork main flow no longer dispatches this profile.
For UI Design Stage, use main Claude Code session with Designer role identity switching.
If invoked manually, you are restricted to read-only UI/design review.
"""

sandbox_mode = "read-only"  # v7.3.10+P0-104：full→read-only （OpenAI ToS 合规 · profile 已 deprecated 不再 dispatch）
model_reasoning_effort = "high"   # v7.3.10+P0-37：避免 codex 默认 xhigh 深度思考导致卡死
