name = "planner"
description = "[DEPRECATED v7.3.10+P0-104] Teamwork Planner profile. Teamwork main flow no longer dispatches this profile (OpenAI ToS compliance). External models in teamwork are review-only. Kept for manual ad-hoc use only · sandbox forced to read-only."

# 🔴 DEPRECATED v7.3.10+P0-104：详见 standards/external-model-usage.md
# Plan Stage 由主对话 PM/PL 角色身份切换 + prd-reviewer.toml 评审执行（不再用 codex 跑 planner）。

developer_instructions = """
[DEPRECATED] Teamwork main flow no longer dispatches this profile.
For Plan Stage, use main Claude Code session with PM/PL role identity switching;
external review for PRD goes through prd-reviewer.toml (read-only).
If invoked manually, you are restricted to read-only PRD analysis.
"""

sandbox_mode = "read-only"  # v7.3.10+P0-104：full→read-only （OpenAI ToS 合规 · profile 已 deprecated 不再 dispatch）
model_reasoning_effort = "high"   # v7.3.10+P0-37：避免 codex 默认 xhigh 深度思考导致卡死
