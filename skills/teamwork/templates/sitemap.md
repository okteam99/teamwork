# sitemap 模板（IA 地图）

> 位置：`{子项目路径}/docs/design/sitemap.md`（**与全景 panorama 同目录** · `panorama_path = {子项目}/docs/design` · IA 地图与 preview-project 视觉稿内聚）。单项目仓库 = `docs/design/sitemap.md`。
> 🔴 **只写地图（页面层级 / 导航 / 路由），不写视觉** —— 视觉在 `design/preview-project/`（源即权威）· 防「地图 + 视觉」双副本漂移。
> 🔴 首次 seed 在规划层「UI 全景初步规划」（feature-planning Step 5 · 拆 WS 前）· ui_design 阶段增量扩本 Feature 的页节点 · 路由结构须与真实 app 一致（IA 镜像律）。

---

```markdown
# {子项目} · Sitemap（信息架构）

> 页面层级 / 导航 / 路由的**唯一地图**。视觉见 `preview-project/`。

## 路由树

| 路由 path | 页面 | 层级 / 父节点 | 导航入口 | 状态 |
|----------|------|--------------|---------|------|
| `/` | 首页 | 根 | — | 已建 / 设计中 |
| `/{a}` | {页 A} | `/` | 顶栏 / 侧栏 / 页内链接 | 设计中 |
| `/{a}/{b}` | {子页 B} | `/{a}` | {a} 页内入口 | 规划中 |

## 导航结构

- **主导航**：{顶栏 / 侧栏 · 列一级入口}
- **次级导航**：{Tab / 面包屑 / 页内跳转}
- 🔴 页间跳转优先**页内入口**（真实 app 有的导航/链接/按钮）· 全景 dev 顶栏只兜底页内到不了的页。

## 变更记录

| 日期 | 变更（新增/调整页节点） | 关联 WS / Feature |
|------|----------------------|------------------|
```
