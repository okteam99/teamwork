# RETRO-LEDGER(teamwork 框架自省台账)

> **telos**:框架自身的「问题 / 优化点 / 有用的地方 / 反思」**跨项目汇总一处** —— consuming 项目目录只放项目的事(`project-specs/PROCESS-LEDGER.md` 度量流程环节价值);框架自己的演化反思归框架仓。「**有用的地方**」是 CHANGELOG 没有的正反馈维度。
> **三层分工**:`CHANGELOG.md` = 单版变更细节(keep-5 · 归档定期清空 · **易逝**)· `PROCESS-LEDGER.md` = 项目侧流程环节价值(一行一 feature)· 本表 = 框架侧自省蒸馏(一行一版 · **永久**)。
> **写入时机**:每次流程完善发版(minor bump)与 CHANGELOG entry **同 commit** —— 发版三件套:CHANGELOG entry + 本表 1 行 + 版本 bump。🔴 单元格 ≤1 行 · 不复述 CHANGELOG(细节在那边 / git)。
> **消费方**:① 年检 /「teamwork 是否该存在」类审视(直接读表算账)② 任何框架改进 session 动手前读最近 10 行(防重复踩坑 / 重复讨论)。
> **膨胀控制**:超 ~500 行 → 年检时把老行主题化压缩(原文 git 可溯)。

---

| 版本 | 来源 case | 问题 | 优化点 | 有用的地方 | 反思 |
|---|---|---|---|---|---|
| v8.120 | PTR Assets prepare 总览(aon) | 流程概览只有「怎么流转」· 缺「要达成什么」 | 总览加「流程目标」首行(写要什么不写怎么做) | 暂停点的固定结构让缺口能被用户一眼指出 | review 第一校准点应是目标理解 · 不是流程形态 |
| v8.121 | 用户:全量 review 所有 md | 数字宣称/断链/旧名/脏标题 16 实锤(版本滞后 33 版 · stage 数三口径) | P0 批次 9 项口径对齐 + 断链清零 | 6 路并行 review + 主对话逐条复核挡掉 3 条误报 | 数字写死多处又无校验 → 演进必漂 |
| v8.122 | review P1 | frontend 1684 行教程混规范 + TDD 未注册 fork + 历史格式事故腐坏 | 瘦身 166 行 + spec 宣称 pytest 物化(4 测) | 负验证(regex 实测命中再信)防校验空匹配白过 | 文档体量 ≈ 物化欠账的度量 |
| v8.123 | 用户:guide 是否有必要(委托裁定) | 1554 行通用教程无归属层 · 维护负债 | 删除 + 「实施教程不入库」哲学行防回潮 | 三层律给了现成裁决依据(通用知识归模型) | 保守外迁回避不了「该不该存在」· 删除可逆(git) |
| v8.124 | 用户:后端规范也看下 | backend TDD fork + Date.now 腐坏 ×3 | 收敛 tdd 单源 + 全仓腐坏归零 | 同病例复扫暴露上次扫描正则的盲区 | 体检结论因文件而异:backend 主体是真规范 · 不照搬 frontend 砍法 |
| v8.125 | 用户:API 规范是否 DEV-RULES 优先 | §三写成绝对条款违用户主权 · 覆盖注册处两套口径(KNOWLEDGE 错档) | DEV-RULES 优先级链 + STANDARDS 全局优先级成文 | dev-rules 模板自带的边界表直接提供裁决依据 | standards 是「未规定时的缺省」· 不是凌驾项目的法律 |
| v8.126 | 用户:去 KNOWLEDGE 兼容层 | 兼容层让注册处仍是两个 | 收口单源两级链(有则为准 · 无则默认) | 用户对单源的坚持比兼容方案更简单正确 | 兼容层是迁移成本不是终态 · 能一步到位就别留尾巴 |
| v8.127 | 用户:体量分析后拍板删历史 | 冻结历史热存 16.3k 行占仓库 61% · 违自家三层律 | 删 docs/archive + 归档定期清空制(−55%) | git 历史天然就是冷库 · 删除完全可逆 | 框架要先对自己执行自己的律法 |
| v8.128 | aon「ci 编译失败」case | 现象类输入被措辞路由直接定 Bug · 未验证猜测进 prepare 总览押上命名/路由 | 排查先行律(按事实已知度定路由 · 不按用户用词) | 用户中途质疑「你排查到原因了么」= 暂停点人工监督在工作 | 判别题是「定流程所需事实是否已知」 |
| v8.129 | 用户:是否加流程价值反思 | 框架只有负反馈(出事加规则)· 仪式价值靠信念 | PROCESS-LEDGER + digest(零暂停 · 机器字段 · 指名消费方) | 「是否该存在」的存在性拷问直接催生了度量机制 | 反思流程要先防自己变成新仪式 |
| v8.130 | 用户:归档压缩后还能取数么 | 台账采集在归档删目录之后 = 取数断粮 + 写入无 MR 载体 | 两段式:planning-backref 采写(文件尚在)· finalize 后 emit | 收尾 MR 是现成合入载体 · 读码核证 --planning-artifacts 兼容 | 机制设计要沿数据生命周期走一遍 |
| v8.131 | 用户:框架反思归框架仓 | 框架自省散落 chat/CHANGELOG(易逝)· 正反馈无归档处 · 项目目录不宜放框架事 | 本表(发版三件套 · 一行一版 · 永久)+ 回填 11 行种子 | 项目侧/框架侧台账成对 · 反馈闭环补全 | 「有用的地方」也需要账本 · 否则年检只剩坏消息 |
| v8.132 | 用户三连问:goal 合理性 → 上抛风险 → 对抗性 | goal 五层皆有缺口:输入薄/无人杀前提/全员通过纸面/模板三方矛盾 | 调研前置+PL 质疑五问(subagent 隔离)+三闸早问门+verdicts·CHALLENGE 物化(+9 测) | 用户连环追问本身就是对抗评审 · 三问各堵一个我初版设计的洞 | 防上抛的不是禁令是入场券(调研穷尽)· 对抗的关键是不喂心路 |
| v8.133 | jdp console case:设计页顶在 / · 栈版本漂移 | spec 亲手教给根 URL ·「同栈」未定义镜像范围 · AI「有意省略 router」照章办事 | IA 镜像律 + 数据层唯一差异律 + route_path 直达预览(五文件联动) | 用户两轮反馈精确指出 spec 的两个缺口(镜像范围 · IA 结构) | 「全景」的定义要同时覆盖空间结构(路由)和物质构成(栈)· 缺一半就只是页面集 |
| v8.134 | 用户四轮推演:镜像最合理么→mock 态全景→规划层需终稿→分层同构+编译契约 | 全量镜像=双源死结 · mock 态撞规划期/全貌需求 · dev-stage 还残留已废 in-app 路由模型 | 分层同构律(L1 基建共享包完全一致 · L2 页面意图四要素 · 权威时效)+ 共享基建改动须验全景编译 | 用户逐轮否证把方案从两个极端逼到正确中间形态 · 共享包让「一致」从纪律变结构 | 同构承诺要按「能否由结构保证」分层 · 全镜像与废镜像都是错的 |
| v8.135 | codex-cli case:spawn_agent 契约要 「explicitly asks」 卡 teamwork 流 | 宿主工具契约与流程预设的 subagent 用法有缝隙 · 授权无长期载体 | 注入块加「Subagent 默认授权」段(三宿主通用 · 约束仅指针防双源)+ R4 挂授权指针 | 注入块正是宿主契约的读取处 · 一处声明三宿主生效 | 授权管「能不能」· 判据管「该不该」—— 两者分开才不会把授权读成滥用许可 |
| v8.136 | PTR case:claude -p 三轮两坑(stale review · 双嵌 PRD) | 审计副本固定名被下轮当输入(缓存无失效)· 模板整文件替换把 PRD 灌进占位符示例表 | 每轮唯一 doc 名(审计=输入)+ Prompt 主体提取 + 显式 doc staleness 门禁(改 2 增 3 测) | 用户方案(唯一命名保 doc 优先)优于我的废 doc 方案 —— 病根是隐式复用不是 doc 本身 | 审计产物与缓存必须分离 · 或者像这样合一但身份唯一 |
| v8.137 | 用户:确认前输出值得关注的点 | 出口无导读 · AI 替用户做的判断(REJECT/DEFER)不可见 | substep 9 六节照实抄导读 ·「替你做的判断」第一节 · 空节写无 | 入口流程目标(v8.120)与出口导读成对 · 暂停点两端都有校准面 | 导读的天敌是营销式总结 · 防法 = 每节必有产物出处 |
| v8.138 | PTR case:sitemap 仅描述行更新仍停 reviewer 暂停点 | 暂停粒度(语义级)与触发粒度(文件级)错配 · 推荐理由自证无风险还停 = 纯过场 | 变更判级:L1 三判据全过不暂停(WARN 留痕 + digest)· L2 结构变更才停 · change_level 物化 | LEDGER「暂停点 改:默」kill criteria 第一个判例先到 · 事实已知度判别三连复用(v8.128/132/138) | 暂停点价值在「用户真要决策」· 事实已查明的确认 = 把审计成本转嫁给用户 |
| v8.139 | 用户:外部评审发起后完全黑盒 · 提议同名 log + 模型自报首行 | v8.55 日志跑完才写/藏 ~/.teamwork 不配对 · 运行窗口零痕迹 · stderr 死因等验尸 | 流式执行器:START 行 harness 写 · 实时落 prompt-doc 同名 .log · 超时保留部分输出 · codex 补审计 doc | 用户方案对一半(配对对 · 自报错)—— print 模式输出整体到达 · 模型首行物理不可能先到 | 时间戳/状态行可枚举归脚本 · 评审内容不可枚举归模型 —— 可观测性是 harness 的职责不是被观测者的 |
| v8.140 | 用户(承 v8.139):prompt 注入让模型开机写 .log 声明开始+时间戳 | 模型零工具写不了文件 · 「开始时」恰是模型没起来的窗口 · 直注入必落空 | 拆两层:RUNNING 心跳(harness · 活性)+ 首行 REVIEW-ACK 回显(模型 · 对应性 · WARN 不 BLOCK) | 用户的执念点出真盲区:pid 到 END 之间心跳缺失;ACK 把防 stale 补到输出侧 | 用户提案不可行时别只说不行 —— 拆出背后两个诉求各给可行载体 |
| v8.141 | 用户:claude -p 支持 --allowedTools 么 · 重点解决 MCP 卡死 | v8.106 归因错了:裸 -p 也每轮 spawn 项目 MCP(marker 实测)· 现行一直在拉火药库只是新 CLI 不阻塞 | --strict-mcp-config 固定进 argv(零 spawn · auth 完好)· 测试 pin 缺一不可 | 四组对照 + marker 文件法把「相关」和「因果」拆开了 · C4 还白捡一个解锁选项 | 修历史结论先复现历史实验 —— 环境(CLI 版本)变了 · 结论的边界条件要重标 |
| v8.142 | 用户:更新提示要带更新描述 | 升级暂停点只有版本号 · 判断「升不升」要出门翻 GitHub | outdated 拉线上 CHANGELOG 标题行进 prompt(cap 8 · 断档加 git 历史注 · 失败降级指针) | 发版纪律的标题行=现成蒸馏摘要 · 零新增维护成本就喂给了暂停点 | 暂停点的决策参考应随暂停点到场 · 不应让用户出门取 |
| v8.143 | 用户:rsync 去掉 · 本地项目走 dev 升级 · 你只管提交到 dev | 发版 rsync 是无成文的 session 习惯 · 本机项目被静默推到未发布版本(无提示/无确认/无 backup) | 交付止于 push dev 成文进 CHANGELOG 头部 · 本机项目走 localconfig update_channel=dev 标准链路 | 用户从「静默最新」一个观察反推出整条交付边界问题 | 例程里没成文的步骤 = 下个 session 的隐性行为 —— 砍掉它也要成文 · 否则惯性会把它带回来 |
| v8.144 | aon 收尾 transcript:终态对但 AI 做了 20 条 git 手术 | pop 结果无视(改动埋 stash 被手工重写成双份)· pull 失败一律喊分叉(实际仅落后一条 pull 即愈)· stash 跨 feature 堆积 | pop 必查 + rev-list 对症判别 + 残留清除即补 pull + stash 盘点透出 | 沙箱实测推翻两个想当然:staged 删除不阻塞 ff-pull ·「下次自愈」的下次不该留给人 | 错误文案是更深的债 —— 工具喊错病名 · AI 就开错刀;终态残留 = 下一轮的事故输入 |
| v8.145 | 用户:重新思考 ship · 终点=提 MR · 收尾不属于本次 ship | 12 个版本修同一条尾巴 = 架构信号:在不可控主工作区做内容性工作 | ship1 全交付(worktree 内 · 单 MR 原子)/ ship2 零内容清场 · 砍双 MR 链路 −979 行 | 用户一句原则(内容/清理分离)解开十二版补丁没解开的结 · diff 还更干净了 | 反复修补不收敛 = 该换位置不是该再修 —— case 火山的根因常是职责放错了环境 |
| v8.146 | 用户(承 v8.145):MR 创建后大概率冲突 · 要不要自动检测处理 | v8.145 把共享追加文件移进 feature 分支 · 冲突从「可能」升「并行必然」(设计时低估) | archive 内置 sync:behind 自动 merge · INDEX 机械自动解(origin 为基+重放己行)· 代码冲突 PENDING 留 AI | 重跑 archive 顺势成为冲突修复入口 —— 幂等命令天然是修复入口 | 大重构的 trade-off 标注要按「并行场景」估频率 · 用户对实操频率的直觉比设计推演准 |
| v8.147 | aon 实战截图:v8.146 防线首战 · LEDGER 冲突 AI 手工 union | v8.146 保守把 LEDGER 留给 AI · 实战证明其处置零判断纯机械 | 三方对比纯增行判定 + union 进脚本(名单制 · 非纯增拒动留 AI) | 防线首战全对还暴露了下一刀 —— 实战是最好的边界测试 | 「可枚举」的边界由实战画:AI 重复做同一个零判断动作 = 该物化的信号 |
| v8.148 | 用户连查三轮:ship2→框架反馈通道/年检/项目级总结全是空 | 反馈环只建「写」侧 · 「读/rollup」整个没规范 · 真判例(jdp commit bug)漏掉 | ship2 后落审计到安装目录 docs/audit/(机器数据工具抽 + 三段判断 AI 静默)· 框架跨项目 harvest | 写侧勤勉到贴「供年检」标签 · 收件人(年检)却从没建 —— 寄信给不存在的地址 | 建消费方前先建产物的回收落点 · 否则数据写得越勤漏得越多 |
| v8.149 | 用户:goal 外部评审去掉 · 总挑细节和过度设计 | external 是 MVP(82% 采纳)但 goal=业务对齐阶段 · 技术挑刺错位 · 框架早知道只贴创可贴(让 AI 自筛) | DEFAULT_REVIEW_ROLES goal 去 external(1 处接线)· 防线交 Architect 内审 · 能力 opt-in 保留 | 高采纳率掩盖了阶段错配 —— 「该不该用」要拆到「哪个阶段该用」· 创可贴(让 AI 筛)是该移病因的信号 | 仪式价值是分阶段的 · 一个环节在 A 阶段值≠在 B 阶段值 |
| v8.150 | 用户:AI 处理 review 应先质疑再确认再采纳 · 不盲目认同 | 举证责任不对称:rejected 必给依据是硬的 · ADOPT 只填「改了什么」· adversarial 示例全是 reject 方向 → 盲采成无摩擦默认 | §12 加固定思考顺序(质疑→确认→裁决)+ 举证对称(confirmed 同责)· adversarial schema 双向 | 框架点名了盲采是默认倾向却只防了反向(盲驳)· 设防方向跟病不匹配 | 防反模式要防在「阻力最小的那条路」上 · 防错方向 = 没防 |
| v8.151 | 用户(承 v8.150):相关 brief 要不要对应提示 | v8.150 强化的是被动 spec(要 AI 主动读)· findings 消费那刻 brief 零质疑姿态 = 防线不在决策点 | finding 质疑姿态做成常量接进 external-review emit + review brief(消费时点主动推) | 改 spec 是必要不充分 —— 主动告知型框架里 · 防线得跟到 state.py 推 brief 的那一刻 | doc 是被动的 · brief 是主动的 · 行为改变要落在主动推送处 |
| v8.152 | 用户:REJECT 也要给理由 | v8.151 推姿态进 brief 时只摆明 ADOPT · REJECT 只剩抽象「对称」· 犯了 v8.150 要治的同款不对称 | hint+brief 两个方向都点名实证(ADOPT 给 X · REJECT 给 Y) | 抽象说「对称」不够 · 不摆明的那侧就是会被忽略的那侧 —— 我自己刚踩了一遍 | 对称要逐侧摆明 · 一句「对称」=没对称 · 连改 spec 的人都会偏向新强调的那侧 |
| v8.153 | 用户:disable_heterogeneous_review 改名 disable_external_review | 旧 jargon 名 + 是用户文件里的 key · 硬改会 silent break 别的机器;且 bootstrap additive 自愈会补默认顶翻旧值 | 全量改名 + 两 reader OR 兼容旧名(新或旧 true 即禁)· 顺带校正 exec→subagent stale 注释 | 改用户文件的 key 不能假设没人用 · OR 兼容比 fallback-if-absent 更稳(防 healer 补默认顶翻) | rename 用户配置必留兼容读 · additive 自愈场景要用 OR 不能用「缺则回退」 |
| v8.154 | 用户(承 v8.153):不用兼容 | v8.153 默认按「rename 必留兼容」加了 OR · 但该 key 无任何项目使用 · 兼容是凭空成本 | 撤 OR · 只读新名 · 兼容测试反转为 hard-break 守护 | 「留兼容」是好默认但不是铁律 —— 确知零使用时 · 兼容反而是债(多读一个 key + 误导后人) | 兼容与否看实际使用面 · 不靠教条;v8.145「不留兼容期」同源 |
| v8.155 | 用户:goal 改成草稿后并行派 3 Agent 评审 | 数据证 in-context 的 arch/qa 锚定鼓掌(info-only)· 漏细微 gap · 框架只隔离了 PL 没推广 | goal §3+§5 → 并行 3 隔离冷审循环 · PM 退整合者 · architect.md 翻默认隔离 | 一条贯穿多轮的诊断线(过场→举证→external→subagent)收口成一次流程重构 · 用户的直觉每步都对 | 防鼓掌的真解是隔离不是劝诫 —— 「怀疑者视角防鼓掌」是乐观假设 · 结构(冷审)才靠谱 |
| v8.156 | 用户:归档 INDEX 描述灌了过程信息(评审/测试/全绿) | --archive-desc 引导只说「极简描述」没说业务非过程 · AI 把手头过程数据塞进业务索引 | 引导改「只业务不过程」+ ✅/❌ 对照例 + 过程信号嗅探 WARN | 「极简」不等于「业务」—— 没说清内容类型 · AI 就拿最顺手的(刚跑完的过程数据)填 | 字段语义要说清「写什么类型」· 只说长度/格式 = 留给 AI 自由发挥的空当 |
| v8.157 | TermPro locate-vs-open case:既有行为变更没进用户拍板 | PRD 把默认行为改动登记「取舍」叙述当既定事实 · PL 五问无一覆盖「改既有行为必 ratify」· 8 轮评审打磨实现不回炉前提 | PL 加第六问「既有行为变更」+ §待决策项 硬规则(改既有行为必入) | 评审越细越巩固错框 —— 防的不是漏想是「该停下问的地方没停」· 前提逃逸比细节漏检更贵 | 「取舍」叙述段是 premise 蒙混的温床 · 改既有行为必须显式 ratify 不能 narrate |
| v8.158 | 用户:PRD 机读内容预览隐藏 + 待决策项上移 | frontmatter AC 的 description 机读不用却两处重复撑爆预览 · 决策项埋文档底用户难快读 | frontmatter 瘦留机读字段(description 归 body 人读单源)· 指引转 HTML 注释 · 待决策项移到交付预期下 | 「隐藏机读」的真解常是「去冗余」不是「加注释」—— 先查谁真机读 description(答案:没人) | 重复的人读/机读双份是预览噪音源 · 字段归位(机读进 frontmatter · 人读进 body)比同步两份更省 |
| v8.159 | 用户:PRD 模板瘦身 · 过时/历史背景 | 模板攒了多版迁移考古(重构合并/删字段/历史兼容/方案 A-B)+ 版本演进注 + label 清理留的悬空「：」 | 删死历史(迁移考古/版本注/artifact)· 留有用 why(防漂移/省评审轮等判断依据)· 384→367 | 「历史背景」分两种:死迁移考古该删 · 活的 why(为什么这么定)该留 —— 砍的是「曾经是什么」不是「为什么是这样」 | 文档考古会沉积 · 定期砍迁移注 · 但别误伤非可枚举判断的 why |
| v8.160 | 用户:查其他模板是否有 prd.md 同类问题 | 10 模板攒了贴在活规则上的版本号(v7.3/v8.x)+ 决策考古(撤销 off/已移除字段/上线 Week 计划) | 剥版本号留规则 · 删考古块留 why(治本双副本/残留 WARN)· 假阳性不碰(功能 marker/状态枚举/活约束)· −18 行 | 死历史是横切问题:一个模板有、多半全有 —— 单点修完该同律扫一遍同类 · 但「废弃」分两种(状态枚举 live vs 迁移考古 dead)别一刀切 | 文档考古跨文件沉积 · 修一处后同律 grep 全库 · 核验功能 marker/数据模型再删 |
| v8.161 | harvest docs/audit/(aifriend 20 条 ship2 审计)最高频信号 | external review 评 merge_target...HEAD · 长 WS/stacked 分支累积 → 跨 feature 串味(finding 大半非本 feature)+ 600s 超时 | 进 dev 冻结 pre-dev HEAD 作 review_base_commit · review 外审评 base...HEAD = 本 feature 增量 · 祖先校验+merge_target 兜底 · +11 测试 | 一个症状(串味)+一个症状(超时)常同根:都因 diff base 选了不前进的累积点 · 治 base 一处灭两症 · 比分别加「scope 分流工具」+「分块评审」都简单 | 反馈消费侧(v8.148 audit)第一次产出框架级改进 · 数据驱动定位根因比拍脑袋准 |
| v8.162 | 设计讨论:意图理解偏差谁来兜 | 冷审/PL 只查前提 soundness(理解对的前提下稳不稳)· 抓不了 fidelity(有没有理解对)—— 共享派生产物 = 全锚同一误读 · garbage in 严格验证过的 garbage out | 意图确认提到 prepare 前门(规划→执行膜)· 暴露 AI 补的假设让用户校(唯一 oracle)· setup 塌一行 · 459→417 | fidelity ≠ soundness:前者只能用户校(accountability 不可转移)· 给没确认的前提加更多 AI 严谨反而制造虚假信心 · 把对的门挪到最早位置 = 砍下游 churn 而非加仪式 | 暴露假设(非干净 restatement)是抓误读零件 · 原料早采(user-intent/ai_rationale)只是没 surface · 反转信噪比净更短 |
| v8.163 | 用户追问 prepare 前读代码么 | v8.162 意图门 🧩 没区分「意图假设」(用户可拍)vs「代码假设」(AI 在猜)· 而 prepare 在强制读代码前 → 可能让用户确认 AI 本该去查的代码事实 | 🧩 只列意图解读假设 · 禁抛未验证代码猜测(留 goal 深门)· 一句硬约束 | 浅意图门(读代码前)只能拍「想要什么」· 不能拍「系统怎样」—— 后者 AI 没查就是猜 · 把它当假设抛给用户 = 误导 review · 浅/深分工的边界就在「有没有读过代码」 | 加暂停点/反射时必问「这层有没有 ground truth」· 没有就别让用户为 AI 的猜测背书 |
| v8.164 | 设计讨论:模板该不该留 AI 发散区 | 统一模板平铺必填/按需 · 没给「结构没预见到的」留口 · 既有行为侦测埋在 HTML 注释(被动·模型可能不读)| 三层显式(必填核/按需/开放区)· 开放区=可见尖问题+「实质 or 无+理由」· 既有行为侦测提成主动挑衅 · +14 行 | 开放区不是空白「自由发挥」(得通用废话)· 是逼判断的尖问题 + 必答装置 · 且只放人读(机读禁入保确定性地板)· 侦测=开放/后果=刚性 的 factoring | 模板=活契约:开放区反复出现的 → 该毕业进必填核 · 人人 N-A 的 → 降级 · 边界随数据移 |
| v8.165 | 用户实测 Zed/TermPro 双截图 | v8.158 赌「frontmatter 预览隐藏」· 但 Zed/GitHub 都不隐藏 → 机读 AC 裸露顶部 · 修 viewer 改不了 Zed | 机读契约搬进 <!-- TEAMWORK-MACHINE --> HTML 注释块(所有渲染器隐藏)· 2 解析器优先读块兜底 frontmatter · +6 测试 | 「隐藏机读内容」HTML 注释才到处生效 · frontmatter 只有部分渲染器隐藏 —— 别赌渲染器行为 · 选确定的载体 · 带兜底防 in-flight 破 | 实测>假设:截图 2 个编辑器才暴露「frontmatter 不隐藏」是普遍而非个例 · 跑跑看真能逼出盲区 |
| v8.166 | 用户看 audit 截图要加耗时/模型 | audit 实际数据只有总时长 · 没各阶段耗时(看不出瓶颈)· 没记主对话模型(harvest 没法按模型分析质量)| _stage_durations 抽 duration_minutes 渲染各阶段 + 最耗时% · host 确定性 + model 由 PMO --main-model 声明 · +4 测试 | 确定性的进工具(各阶段耗时/分析 = stage_contracts 现成数据)· 非确定性的留声明(精确 model state 不记 · PMO 知道 → 显式传)· 别把 AI 才知道的硬塞进「工具自动抽」 | 加 arg 先确认绑哪个 subparser(ship-phase vs ship-finalize 两个 · 差点加错)· --help 实测验证 |
| v8.167 | 实战:ui_design 涉交互「降智」(Deli Yang) | designer 角色全是全景机制 · 零交互质量 rubric · 模型对交互没判断力 → 要人逐条喂细节 | 吸收 design-review+frontend-design 的可枚举设计判断成 rubric(交互状态/视觉地板/copy)· host-agnostic · 按扩展既有 app 裁剪 | 把「设计 taste」拆成可枚举 rubric 模型就不需要天生品味 —— 这是 R0(可枚举进脚本)用在设计质量层(之前只用在机制层)· 但选择性吸收:质量地板要、brand-distinctiveness ethos 不要(语境是扩展既有 app 非 greenfield)| 吸收外部 skill = 吸收知识不吸收 skill 本体(host-locked + 重耦合不可移植)· 蒸馏 + 按本框架语境裁剪 |
| v8.168 | 实战:重点 review 指引太粗(ADMIN)| 第一节把 QA/PL 的 NEEDS_REVISION collapse 成「全部 ADOPT」一句 · 用户看不到冷审实际抓了什么 | 改冷审逐条:每条 finding 一句话通俗 + 结论(ADOPT/REJECT/DEFER)· REJECT/DEFER 标重点抽查 · 禁 collapse | 导读的价值是让用户看到「评审实际发现+怎么处理」· 把多条 collapse 成一句省了字但杀了信息 —— 摘要要保留可抽查的颗粒度 · 不是越短越好 | 「照实抄不美化」要延伸到「不 collapse」· summary 藏掉逐条 = 另一种美化 |
| v8.169 | 用户:UI 规范没家 + 设计稿要=代码 + 顶栏放测试入口 | 设计规范散在 KNOWLEDGE · 分层同构允许「像素自由」· 状态切换器内嵌页面 = 设计≠代码(实证 AON)| UI-RULES 两层(策略不装视觉值)· 同构按介质拆 same-stack 完全一致 · 预览工具外置 dev 顶栏(页面零预览痕迹)· +骨架/登记/cite | 「设计=代码」的机制不是人肉对齐 · 是把预览工具全外置让页面内容就是真组件(Storybook 模型)· 规范分两类:策略进 UI-RULES(markdown)· 视觉值留代码(防 drift)| 改 bootstrap 骨架清单必同步 test fixture mock 模板 + 断言(setUp 硬编码列表)|
| v8.170 | 用户 QA:stage 和 brief 匹配么 | v8.167/169 加 UI-RULES/rubric/dev顶栏到 spec · 但 stage-start 主动推的 _ui_design_brief 没跟 = spec 被动躺没人主动看 | brief 加两行主动推(读 UI-RULES + 对照 rubric + same-stack 设计=代码走 dev 顶栏)| 加新机制到 spec(被动)必须同步 brief(主动推)—— v8.151 原则:消费时点主动推 · 否则 spec 躺 doc 里被跳过 · 尤其治「模型不主动应用判断」类机制 brief 推才生效 | 改 stage spec 的 checklist:动 spec 后查 brief_template_fn 是否同步 |
| v8.171 | 实测:PRD 机读块预览又裸露(TermPro)| v8.165 marker 行描述「保持 <!-- --> 包裹」· 字面 --> 提前闭合 HTML 注释 · 后面 YAML 全裸露 = 自身 marker 破坏了隐藏目标 | marker 去掉字面 <!-- --> · 加防回归测试(marker 到第一个 --> 须含完整 YAML)| 🔴 永远别把注释分隔符(-->)当描述文字写进 HTML 注释内容 —— 注释不嵌套 · 第一个 --> 即闭合 · 描述注释语法要用 backtick/转义不用字面 | parser 用 [^\n]* 不受影响 · 只有渲染器在意 —— 「能解析」≠「能正确隐藏」· 隐藏类改动要在真实渲染器验 |
| v8.172 | harvest 31 done 审计记录的高频反馈 | ① v8.166 耗时把等用户当工作(pm_acceptance 84%)② add-concern 文档引用命令不存在 ③ review-fix 后 external 锚 pre-fix 报 stale ⑥ playwright 写不了约定截图目录 | ① 区分工作/等用户阶段 ② 重加命令 ③ review-fix 推进 auto_commit ⑥ .playwright-mcp 兼容 · +4 测试 | 反馈环第二轮产出:我刚加的 v8.166 耗时功能数据立刻打脸(等用户≠工作)· 自己的功能也要进 harvest 受检 · doc 引用的命令必须真存在(漂移=AI 撞墙)| 加 stage 字段/命令后查全文档引用是否对得上 · 删命令前查文档是否还引用 |
| v8.173 | 实战:重点 review 指引读起来难(aon)| v8.168 冷审逐条全列 → 决策与 ADOPT 平铺等权(决策被淹)+ 代码 id 当正文(逼回翻 PRD)| 拆两层(你要拍板/已处理)+ 决策写选择题 + 代码挪括号 + ADOPT 压主题 | 导读的标准 = 没读过 PRD 的人也看得懂(代码是 ref 不是正文)· 按可操作性分层(决策 prominent · ADOPT compact)· compact 保 substance ≠ collapse 藏 substance | 防 v8.167↔168 钟摆:不是「全收/全列」二选一 · 是按可操作性分层 |
| v8.174 | 用户看 TermPro WS 文档:有模板么/乱/无进度标识 | WS frontmatter 被渲染器当正文成 YAML 墙+与 body 重复(乱)· `features[].status` 只规划态、执行态从不上卷、翻 5 个 ROADMAP 才知进度(无进度)| ws-progress 命令(glob ROADMAP 按「关联WS」派生 rollup 写回标记区)+ frontmatter 藏进注释去墙(字段全留)+ 三消费点(planning/ship/模板)接钩子 | 「想一处看全」与职责单一(执行态单源在 ROADMAP)不冲突——靠**派生只读视图**调和、不靠手抄双源 | 「无进度」类诉求先找单一源在哪、再决定**派生而非新建**(派生才不 stale)· 隐藏 frontmatter 直接复用 PRD 已验的 TEAMWORK-MACHINE 注释方案、不重造 |
| v8.175 | 实战:AON Offer-Analysis ui_design 出概念页(扩已有页)| feature 给真实页 `/analytics/offers` 加 tab · ui_design 只画孤立新 tab、没按真实代码复现整页(筛选区/KPI/Top card)→ 设计稿与实际不一致被打回 | §3 加「扩已有页→复现整页」复现门(读真实代码定形态 → preview-project 复现整页 → 集成新部分 · 禁概念页)+ §1 判定 + brief 同步推 + brief 断言测试 | 「在已有全景增量补」默认页已 seed —— brownfield/扩已有页时该假设破,得显式要求**按真实代码复现整页** | same-stack「设计=代码」不只「从真实组件渲染」· 还要「复现新 feature 落地的整张真实页上下文」——局部概念页绕过了它;改 spec 必同步 brief 才生效(v8.170 再验)· brief-content 该有测试锁 |
| v8.176 | 用户:最大限度保障设计稿和实际效果一致 | 同构「设计=代码」是口号不是闭环:构造侧「复用真实组件」只偏好不强制;验证侧「和实际一致」只能 dev 后验,但那道闸(dev §3 并排对照)optional+含糊 | 闭环两端收口:构造(扩已有页导入真实页源/镜像退路·禁凭印象重画)+ 验证(dev §3 并排对照升必做四要素核对+留证+背离 reconcile)+ `_dev_brief` 推 + 测试 | 设计稿在 dev 前出 → 一致性只能两段保:设计时构造保真 + 实现后落地验证 · 缺一端就有 drift 窗口 | 「保障一致」别只盯设计时——真正的闸在真实代码出来后的核对;把已有软建议(可选并排对照)升成硬闸,比新造机制省 |
| v8.177 | 实战:supersimples WS-03 总览漏跨子项目 K0 + 无依赖图 | ws-progress 纯按「关联 WS」列扫 ROADMAP,漏掉登记在 SDK legacy 表(无该列)的跨子项目前置 SDK-F040(用户手写 📌 补);无 feature 依赖图 | 改**名册驱动**(读 WS `features[]` 当权威·声明的全列·状态自 ROADMAP 按 BL 匹配·放宽解析吃 legacy 表)+ 自 `dependencies` 派生 Mermaid DAG | 「某 WS 拥有哪些 feature」是 WS 自己的声明、不在 ROADMAP 那头——派生进度该以 **WS 名册为锚**、ROADMAP 只供状态;按「关联列」反扫会漏没那列的 | 派生工具的"锚"选在**声明源**(名册)不在被汇总源(ROADMAP);放宽解析器吃 legacy 比要求迁移所有旧表省 |
| v8.178 | harvest 89 条:基线失败集无门禁(欠最久 · 跨 3 次 harvest 8×) | brownfield 共享套件 base 即红 · 每 feature 重复人肉 stash-baseline 甄别正交 vs 回归(跨 3+ feature 反复确认同批失败) | `project-specs/test-baseline.md` 登记预存在失败成项目级单源 + `test-baseline` 命令(add/list/diff)+ dev/test gate 差分(0 新增 → 红 base 放行)+ brief 推 | 重复成本的根治 = 把「每次重 derive 的判断」固化成**可复用单源**;差分(当前−基线)比绝对(全绿)更适配 brownfield;AI 提 id 集(不可枚举)+ 工具算差分(可枚举) | harvest 数据驱动:欠最久项(频次×时长)优先 · 差分 gate 让 base 即红从「每 feature 论证」降为「一次登记」 |
| v8.179 | 用户调 yolo 策略:非异质提示 + 冷审 + 跑前确认 | yolo 无人值守:① 非异质降级缺醒目提示 ② 1644 实跑日志闸误 BLOCK 单模型 yolo(异质日志拿不到)③ 无「正式跑前深入调研 + 核心确认」环节(意图错了没机会纠) | 预研门(`YOLO-PREFLIGHT.md` 物化硬门 · 跑前调研 + 核心决策用户确认)+ 非异质降级认 subagent 冷审证据(`review_via:subagent`)+ init kickoff 醒目警告 + localconfig 单源 | yolo 零暂停点 → 意图保真膜必 **front-load 到跑前**、不能边跑边发现;无人值守下安全网降级必须「**明示 + 仍冷审**」不能静默削弱(非异质 ≠ 不独立) | yolo「加重审核」的延伸:跑前确认(front-load 意图)+ 降级也要冷审;物化卡住比文档纪律牢 |
| v8.180 | 用户看 yolo ship2:感觉没更新 WS 文档 | WS 进度块更新是软指令(ship §3.5「翻完跑 ws-progress」· v8.174 只接 emit 没同步 spec)· planning-backref 门不校验刷没刷 → yolo 自主无人接住 → routinely stale | ws-progress 加 `--feature` 自解析 WS(F-id→对应F编号→关联WS·名册反查退路)+ ship archive 翻牌后**确定性自刷** + 纳进 commit + §3.5 spec 同步 | 软指令在「无人值守(yolo)」下必漏 —— 关键回写要做成**工具确定性自动**、不靠 AI 记得跑;派生的锚(feature→WS)靠 ROADMAP 已有的「对应F编号」链路即可解析、不必新存字段 | 凡「AI 该记得做的回写」在 yolo 下都要审视有没有确定性兜底;v8.174 接了 emit 没接 spec/不自动 = 半拉子 |
| v8.181 | 用户 review TECH 模板(进 dev 前最后 spec)找遗漏 | blueprint 规范 §3 要求 5 段(模块/数据/接口/**依赖与影响/风险**)· 模板只有前 3 段 —— §依赖与影响、§风险 规范要求但模板**没槽位**(AI 必漏)· 另 4 真盲区(错误处理/现状基线/简洁性/测试策略) | 补 §依赖与影响(消费方清单)+§风险 + 现状基线 + 错误处理 + 简洁性自查 + 测试策略层次 · 同步 blueprint §3/§88 | 「最后一道 spec」缺的不是细节是**横切维度**:失败语义/影响面/风险/现状基线这些 dev 必需的维度,happy-path 模板天然漏;规范列了段、模板没给槽 = AI 没处填 | review 模板先拿「规范自己要求的段」对模板槽位 —— **spec/artifact mismatch 是最硬的缺口证据** |
| v8.182 | 用户:需要 RD 开发完文档内逐项打钩的自查清单么 | dev-stage §4 有自查清单但 ① 在 spec 不在 per-feature 文档(打钩只在 AI 脑子)② 通用门不覆盖 TECH 设计承诺(错误处理/依赖与影响/测试策略/parity/基线) | TECH.md 加 §完工自查物化清单(**对着设计逐条** + 通用门 · 每项指向证据 · RD 实现完打 ✅)+ dev §4/cite/brief 改「文档内打钩」 | 自查清单的价值在「**对着设计逐条**」(防设计了没实现)+「**物化在文档**可被 review 核」· 通用清单 + 脑内打钩 = 抓不到设计落地缺口 | 横切维度(v8.181 刚加进 TECH)要配「完工核对」闭环否则设计了不一定实现;清单定位 soft(完整性)· 强门禁仍是硬墙 |
| v8.183 | 用户:错误异常加 WARN/ERROR 日志必查 + SQL 查询性能给理由必查 | TECH §错误处理无「日志级别」(可能静默吞异常 → 线上盲区)· 无「查询性能」段(慢 SQL 进 dev 无人拦) | §错误处理加日志列+不静默吞 · 新 §查询性能(涉 SQL 给理由 · justify 两方向)· 完工自查 +2 必查项 · 规范同步 | 「可观测(日志)」「查询性能」是 dev 必需但 happy-path 模板天然漏的横切维度;性能/日志类检查关键在「**给理由**」(justify 而非默认优化/默认吞)· 同 §简洁性/§FK | TECH 横切维度按用户实战反馈持续补;**justify 模式**(够快也说为什么)已统一简洁性/FK/查询性能/日志多处 = 防「默认做/默认不做」两头 |
| v8.184 | 用户:feature-planning 动文档+全景 · 进流程应建临时 worktree | feature-planning 明确「不需要 worktree · 主工作区写文档」· 但产出已含全景 preview-project 代码(v8.169 后真 code)→ 落主工作区污染主分支/撞并行 feature 基线 | 反转:planning-check emit `worktree_setup`(进流程先建 `planning/<名>` worktree)+ feature-planning §1/§2 Step0/§9 改 worktree+MR + bootstrap forewarn | worktree 隔离与「进不进状态机」**正交** —— planning 不进执行层状态机,但产物是 committed 文档+代码,同样需 worktree 隔离;旧「planning 不需 worktree」是产物**还纯文档时代**的结论 | 框架结论随**产物演化**要复审:v8.169 把全景做成真 preview-project 代码后,「planning 不出代码/不需 worktree」两条旧前提都该跟着更新 |
| v8.185 | 用户:feature-planning 涉 UI 要全景确认暂停点+预览 URL · 未确认不算规划完成 | Step 5 出全景无用户确认暂停点;WS `ui_panorama:✅` 只表「页在 preview-project」(AI 自标)≠ 用户确认;规划完成门只卡 ✅ → 用户没看过全景也能转规划完成 | Step 5 加 R5 全景确认暂停点(`preview.sh` 抓 PREVIEW_URL 给用户)+ WS 加 `ui_panorama_confirmed` 字段 + 规划完成门卡它 + 全门禁/checklist/SKILL 同步 | 「产出存在」≠「用户认可」:`ui_panorama:✅`(AI 可自标的覆盖度)和 `ui_panorama_confirmed`(用户拍板)是两个语义,门禁该卡后者;高风险/难回退的产物(全景定 UI 走向)必须**用户看真东西(可访问 URL)确认** | 凡 AI 可自标的「完成」标识,若该产物需用户认可,必加独立的「**用户确认**」标识 + 门禁卡它(同 ui_design/blueprint 的 R5 预览确认) |
| v8.186 | 实证 AON WS-012:feature-planning 写的 WS 不符最新模板 | AI 写 WS 抄项目里旧/混合格式(裸 `---`·无 TEAMWORK-MACHINE/标记区·缺 `ui_panorama_confirmed`)·无符合性检查·只有用户问才发现 | 加 `ws-lint` 工具(对照 `templates/workstream.md` 校验硬性形态)+ feature-planning/planning-check 写完必 lint + 别抄旧 WS 纪律 | AI 写产物天然倾向「抄项目里现有的」而非 canonical 模板——模板演进快时(WS 从 v8.174→185 改了 6 版)旧格式会被复制扩散;治本 = 给可自动校验的产物加 **lint**;「抄历史」反模式各 stage Output Contract 都警告、但 WS 漏了 | 凡 AI 易「抄历史」且模板会演进的产物,都该有 lint 校验最新形态;templates 是 canonical 源·项目里的实例可能是旧版快照 |
| v8.187 | 用户(AON admin 预览):全景尽量页面内跳转·不要都依赖 dev 顶栏·顶栏只放页面到不了的 | v8.169 矫枉过正:「页面+状态切换都走顶栏·页面禁内嵌任何预览控件」把真实可达交互(按钮→Drawer)也赶顶栏·反不如真实 app 保真 | 区分**交互可达性**(真实 app 能点到的→页面内真实交互·默认)vs **状态注入**(Loading/Error/Empty→顶栏)·「禁内嵌 switcher」澄清=禁预览专属控件、非禁真实按钮 | v8.169 治「内嵌 state-switcher=假控件」治对了·但「全外置到顶栏」过头——真实交互按钮不是假控件·赶顶栏反降交互保真;一刀切(全走顶栏)和不管(随便内嵌)之间要按「真实 app 有没有它」分 | 「设计=代码」对交互的含义 = 真实交互**页面内真实可点**·不只组件渲染保真;治过度内嵌别走到过度外置的反面(钟摆) |
| v8.188 | 实证 AON KA-PAGES:规划完成后 AI 自建 MR 就跳下一 feature | v8.184 规划 worktree+MR 收尾没框清:AI commit→push→建 draft MR→**立刻**起下一 feature 的 prepare(新 feature target **未合并的** planning 分支)·无「是否合入」暂停 + 无「提示合并→停」 | Step 9 框成 3 步(暂停问合入 merge_target→建 MR→提示合并+停)+ 不自动起下一 feature + 别叠 feature 在 planning 分支 · planning-check 同步 | 规划的「**收尾**」= 交付给用户合并的 MR、不是接着干活的起点;跨阶段自动接续(规划→实施)在无人 gate 时会**叠在未定基线**上 · 收尾就该像 ship1 停在「MR 待合并」 | 凡产出走 MR 合入的流程,收尾统一 = 暂停确认→建 MR→提示合并→**停**;下一步是用户合并后的独立决策、不自动接续 |
| v8.189 | 用户续 v8.188:规划 MR 合并后要有收尾(切回主分支/清 worktree/净化) | v8.188 只到「建 MR+提示合并+停」(=ship1)· 缺**第二段 finalize**(用户合并后的收尾) | 加收尾-2 finalize(=ship2):cd 回主工作区→`git worktree remove`→`state.py main-sync` 净化主分支 · **复用** feature 的 main-sync(v8.145 起不依赖 feature) | 规划的 worktree 生命周期该完整镜像 feature:ship1(建 MR 停)+ ship2(合并后 finalize 清场净化);main-sync 本就 feature-independent · planning 直接复用不必新造 | 给某流程套 worktree,**收尾也要套齐**(建→合→清→净四步);能复用的 feature-independent 工具(main-sync)优先复用不重造 |
| v8.190 | 第二轮 harvest(163 条):stash 累积无回收 26×(跨两次 harvest 最高频) | main-sync stash-pull 每次备份 stash 不 pop → 跨 feature/session 累积 11+ · human 难判哪些可 drop | main-sync 加 `_reclaim_stashes`:只认 teamwork 自建 · drop **可证冗余的**(空/内容已在分支 · reverse-apply-check)· 剩 surface + `--drop-stashes` 全清 · 不碰用户 stash | 防御性备份(stash 不 pop)**不配回收机制 = 债务累积**;回收要「**可证安全才自动 drop**」(reverse-apply-check 证内容已在树)· 存疑 surface 不猜;识别自建产物靠**消息标识**、绝不误伤用户的 | 凡框架自动创建的「防御性副产物」(stash/lock/注入块)都要配回收机制,否则跨 session 累积;自动清理的边界 = **可证安全** |
| v8.191 | 耗时归因:external 机械成本 20×(超时/重试/未登录/全量重跑) | 超时 600s 固定无自动重试 · 空跑手动重跑 · CLI 未登录到 review 才发现 · fix 后全量重评(微改动 80% 墙钟是重跑) | --preflight 微 probe(干活前验登录/网络)+ 超时/空跑自动重试一次(1.5x · localconfig 可调)+ --verify-fixes 增量重验(锚上轮已评 commit · 逐 finding verdict) | 「评审慢」要拆**价值 vs 机械成本**:拦真问题 92× 是价值 · 超时/重跑/未登录是纯 overhead —— 修机械不动原则;增量重验的锚 = 上一轮结果文件的 target_commit(已有 · 不新存) | 优化流程耗时先归因(数据)再动刀:本轮三修全是工程活 · 零流程削弱 |
| v8.192 | 待优化 #5:计时数据失真(goal 均 157m vs 中位 22m) | stage 内 R5 暂停的等用户墙钟混入工作时长 · v8.172 只拆了 pm_acceptance · 每次归因人肉排毒 | pause-mark 打点 + 引擎 4 choke 点自动闭合(resume 零纪律)+ durations 扣 await | 打点只能落在「AI 必经的工具调用」上:emit 侧一次轻 call · resume 侧借既有命令自动闭合 —— 单侧纪律比双侧纪律可靠一倍 | 度量基础设施先于优化:#1 风险档的效果要靠这份干净数据验证 |
| v8.193 | 用户:整体 review skills 哪些可删 | 29.6k 行里攒着退役物:v7 迁移器(190 版前)· v7.3 收尾脚本(v8.145 重构漏删)· 给「禁手写」产物留的手写模板 · ARCHIVE 60 版没照章清空 | 删 3 文件+测试+引用清理+ARCHIVE 清空(−2.2k 行) | 删除的判据全是框架自己的律法(三层律/定期清空/工具自动落)—— review 只是照章执行欠账 | 大重构(v8.145)后要扫一遍旧机制的配套物(post-feature 漏删 190 版);「模板」目录里躺着被工具取代的手写模板 = 自相矛盾信号 |
| v8.194 | 承 v8.193 下批:agents/README 瘦身 | 683 行里躺着自标「已废止」的 77 行 · v7.3 产物协议残留 · 模型逐 stage 枚举(代际漂移拐杖)· 同一 Progress Log 规则三处重述 | 重写 64 行紧凑协议(协议留 · 教程/废止/重复删)−619 行 | 「自标历史记录仍全文保留」= 最明确的删除信号 —— 指针一行即可 · git 是冷库 | 协议文件的膨胀模式:每次改进往里加段 · 旧段标废不删 → 读者要自己判断哪段作数;瘦身标准 = 只留「Subagent 真要照做的」 |
| v8.195 | 🟡 待确认项裁决(4 件) | diff-html 工具服务的 static-html 介质已退役(audit 使用率 1/163)· 另 3 件初看引用少 | 逐件消费点确认:删 1(diff-html+测试+3 引用改写)· 留 3(e2e-registry/config/architecture 各有活消费) | 「引用少」≠「可删」—— e2e-registry 被 ship 采写字段消费 · architecture 含 TECH 上游的 database-schema 模板 · 间接消费要顺链查 | 删工具先查它服务的**介质/机制**是否退役(数据:使用率)· 比查引用数更本质 |
| v8.196 | 规划链路审视 #1+#2 | F↔BL 绑定只在 ROADMAP 手填单元格(ship 自刷/翻牌全押它)· 「下一个做什么」要人肉对照 DAG | init-feature --bl 写 state(解析优先读 · 单元格降兜底)+ ws-progress ready_to_start(依赖全✅的待开始) | 链路机读化的顺序:先把**绑定**落进 state(执行层单源)· 派生(进度/可启动)自然就通 | 手填单元格承载链路关键绑定 = 脆;凡「用户高频人肉推导」(下一个做什么)都该是派生字段 |
| v8.197 | 规划链路审视 #3+#4 | 承接执行线是纯 doc 约定(幽灵 Line 不报)· 规划完成后变更无成文路径(WS-03 追加 BL-006 灰区) | ws-lint 加执行线存在性(对照业务架构 · 无则 skip)+ feature-planning Step 10(追加=轻量 · 砍/改向=回流程 · 已启动 F 除外) | taxonomy 引用(WS→Line)和结构一样可 lint —— 语义引用校验只要两头都机读;变更路径按「影响拆解结构与否」分轻重 | 「规划完成 ≠ 冻结」要成文 · 否则每次追加都是一次无据操作;lint 的 skip 边界(无上游文档不误报)与硬校验同样重要 |
| v8.198 | 用户:loops 文章对照 review · 拍板 1/3 | teamwork 全是用户触发的 turn-based:等合并窗无人看(132h 长尾/CI 红无人接)· yolo fix-retry 无轮次上限(runaway) | await-merge 30s 轮询(MERGED 自动下一步 · WAITING 重跑续等)+ yolo 同 stage ≥10 轮硬停止损 | 结构性等待窗 = time-based loop 的天然挂点(把「停等人」变「轮询+自动接续」· 人仍可随时打断);goal-based 的 max-attempts 对无人值守是止损不是限制 | 对照外部最佳实践框架 review 自己 · 缺口按「已有结构里哪最像该模式」找挂点(等待窗→loop · fix-retry→max-attempts) |
| v8.199 | 精简讨论首刀:cite 纪律该不该删 | 每 substep 引原文自证的仪式:163 audit 零实证 · cited_specs 零消费 · 病已被 brief 主动推+gate 接管 | A 全删(定义+11 表+死字段)+ brief 全面性核查(13 个全指回 stage 文件 · 补 blueprint TECH 新结构推送) | 删 crutch 的三问:实证过价值吗(audit)· 产物有人消费吗(grep)· 病有没有更好的接管者(brief/gate);「cite」误删教训:同词多义 · 按行删词要人工核 diff | 年检判例范式成立:零实证+零消费+已被接管 = 可删;删除后检查替代通道(brief)完整性是必做的配套 |
| v8.200 | 用户:模板是否限制模型能力 · 要不要自由补充区 | 槽位不限能力但「填完表=完成」心智会 —— 模板外发现没处落就不写(PRD v8.164 已治 · 其余模板没有) | tc/tech/ui/bug-report 统一加 🧩 补充洞察末段(可留空/删 · 不为凑内容而写) | 模板 = 地板(必须想的)+ 自由区(想到的)· 两者合起来才不封顶;自由区的防腐剂是「可留空」—— 允许空才不变成凑字仪式 | 与精简同向:砍的是过程仪式 · 加的是发挥空间 —— 都是把模型当聪明人用 |
| v8.201 | 扩展区实证:设计有效(2 例真货)分发无效(采用 2/11) | post-v8.164 十份 PRD 仅一份 canonical · 抄旧/自由结构 → 新机制到达不了(同 WS-012) | goal brief 推「照模板别抄旧」+ goal-complete 校验三命门段(机读块/AC/扩展区 · 可写无但段要在) | 「设计有效性」与「分发有效性」要分开度量 —— 模板演进快时到达率是第一瓶颈;lint 只查机读命门段不管风格 = 约束最小化 | 每类核心产物(WS✅ PRD✅)都该有 canonical 符合性轻校验;TC/TECH 待同判例(年检批) |
| v8.202 | 用户:所有 start brief 都给模板地址? | scaffold_hints 早已给绝对路径但被忽略(PRD 2/11)· diagnose 漏映射 · hints 无「别抄旧」警示 · brief 与 hints 割裂 | diagnose 补映射 + hints 加 usage 警示(单点全 stage)+ 4 brief 指针(指向不重复 · 防双源) | 「已有机制被忽略」≠「再造一个」—— 先补它的可见性(warning/指针)与覆盖(diagnose);路径给一处 · 其余指过去 | 机制失效先查到达/可见性再谈重造;brief 指向 hints 而非复制 = 单源纪律 |
| v8.203 | AON WS-14 规划 case:收尾多段手动接力 | 收尾 = 建 MR→等告知已合并→再收尾 · 用户被迫『你直接合并然后收尾』手动短路 | Step 9 暂停点重构:头两项一步到位(自动合并+收尾 / +启动首个 BL)· 自动合并硬门=非主分支 · 选2守 v8.188 护栏 | 常用路径要做成一等选项 —— 用户手动短路某段流程 = 那段该被默认选项吸收;自动合并的安全边界 = 非主分支(同 yolo) | 收尾类流程按『用户实际怎么用』设默认项 · 而非按『机制怎么分段』;v8.196 ready_to_start 在此被选 2 消费(链路复利) |
| v8.204 | 用户:external 异质评审太耗时 · 默认关(yolo 也跟随) | 异质外部 CLI 冷启动是耗时大头 · 但它只是第三视角(架构师+QA 多角色不受影响) | 默认翻转三处(读取器+bootstrap+template)· 缺省→关 · 显式 false→opt-in · 告警软化 · 物化门禁不变(第三视角仍必真跑冷审) | 翻核心默认 = 代码+文档双改:不改文档则 README 四支柱『异质 cross-review』变假话;区分『降级第三视角』vs『去掉第三视角』(前者可默认 · 后者仍 BLOCK) | 默认值翻转的完整清单:读取器语义 + 自愈默认 + 模板 seed + 测试 setUp opt-in + 每次响的告警软化 + 会变假话的文档;缺一处就是半吊子 |
| v8.205 | TermPro case:AI 把 ROADMAP 放项目根来回挪 | 位置权威分裂:模板说 docs/ · SKILL 裸文件名读起来像项目根(矛盾第二源)· sitemap 连模板都没有(3 落点) | SKILL 三裸名加路径 + 表头指「templates = 位置权威」· 补 templates/sitemap.md · conventions/Step7/README 拉齐 | 「同一事实两处描述」= 漂移源:清单类文档只要列了名就隐含位置 · 要么带准路径要么显式指单源;缺模板的产物 = 无位置锚 → 必漂 | 每个产物都该有唯一「位置：」声明(模板头部)· 派生列表指向它不复制;发现某产物无模板 = 优先补(位置/格式双漂风险) |
| v8.206 | 用户看预览页:实际右下角 Prototype Nav 比顶栏合理 | v8.187 定「工具面板放什么」对 · 但位置写死顶栏 —— 顶栏 offset 真实布局 · 违 same-stack 零预览痕迹 | 位置改右下角悬浮 overlay(不占布局流 · dev 工具惯例)· 内容保 v8.187(页面导航+状态注入·页内优先)· 同步 5 处引用 | 「放什么」与「放哪」是两个决策 —— v8.187 只解了前者;工具外壳的第一约束 = **不 shift 真实布局**(overlay 非 bar)· 否则再怎么『页面=真代码』都被顶栏破功 | 预览/调试类 UI 一律 overlay 悬浮(不进布局流)· 别用会 offset 内容的 bar;位置决策也要问『真实 app 有没有它』(顶栏像产品 chrome · 角落像工具) |
| v8.207 | 用户看 Codex ship2:反过来 unzip 解压归档 | ship-finalize 先删 worktree 后要 AI 补三段判断 · 源材料(REVIEW/TEST)只剩 zip 内 → 逼 AI unzip 反读 | worktree-remove 前 _capture_audit_sources 抓 REVIEW*/TEST-REPORT 摘录嵌 audit 草稿 §源材料摘录 · 三段照草稿填不 unzip | 「后置消费的数据必在其源被销毁前抓存」—— 删 worktree 是不可逆销毁点 · 任何依赖 worktree 内容的后续步骤都要在删除前取数(同 PROCESS-LEDGER 在 archive 时采写) | 每个「销毁/清理」步骤前列清单:哪些后续步骤还要读被销毁物?全部前移取数;区分内存态(state 安全)vs 磁盘态(worktree 文档=易逝) |
| v8.208 | 用户:台账时长要细化 + 加用户邮箱 | 台账只有单个『时长』· 混着人工等待 · 无法答『AI 到底跑多久』· 无按人维度 | _timing_split(AI 自主=扣等待 · 等待=stage内暂停+纯等待stage)+ _git_user_email · 落 archive emit ledger_timing / audit / 台账列 | 时长归因要分「AI 在跑」vs「等人」—— 混在一起的墙钟无法评估 AI 效率;基础设施(v8.192 await_minutes)已在 · 只差把数暴露到消费面 | 度量类需求先查底层数据够不够(await 打点已有)· 够则只做聚合+落表;数据源统一 archive emit(确定性)让 AI 照抄不肉眼算 |
| v8.209 | 用户:台账要记 AI 宿主(codex/claude) | state.host 已有但没进台账/audit frontmatter → 无法按宿主对比流程质量 | archive emit ledger_timing 加 host · audit frontmatter 加 host: · 台账加宿主列 · 口径说明按宿主对比 | 已有的 state 字段要暴露到消费面(台账列/frontmatter)才可分析;宿主维度 = 跨宿主流程质量对比的分组键(同 user_email 按人) | 每加一个『按 X 分析』维度 = 台账列 + audit frontmatter 双落(列给人看 · frontmatter 给 harvest 机筛) |
| v8.210 | 用户:模板升级旧项目台账没升级 · 要迁移么 | 台账无列位解析(不 crash)· 但 v8.208/209 中间插列 → 新行 vs 旧表头错位、年检读错列 | 改 schema 纪律=只在末尾加列(旧行=有效前缀)+ 幂等 ledger-migrate(只换表头 · 旧行不动 · canonical 单源自模板)· §16 append 前跑 | schema 演进的黄金律 = **只 append 列不 insert** → 迁移永远退化成表头替换、旧数据永远有效前缀;先查有没有 parser 再定迁移力度(无 parser → 轻迁移) | 加列一律加末尾(即使前置更好看)· 换来零重映射迁移;canonical schema 单源(模板)· 迁移器读它不硬编码 |
| v8.211 | 用户看共享仓库:AGENTS.md 被注入 · 波及不用 teamwork 的同事 | 注入宿主指令文件的作用域是「整个仓库的所有人」· 而 teamwork 的正确边界是「用 teamwork 的 session」 | 注入退役:bootstrap 反转为清理历史块(用户内容不动·幂等)· Subagent 授权等关键信息迁 SKILL.md(加载即生效)· sync-drift/注入模板删 | 配置的**作用域要匹配受众**:skill 内信息只影响加载者 · 仓库文件影响所有 committer —— 后者只该放用户自己写的东西;退役注入机制要配清理逻辑(治已污染存量) | 任何「往用户仓库写共享文件」的机制都要过共享仓库多人测试;退役 = 停写 + 清存量 + 信息找新家 三件套 |
| v8.212 | 用户:skill 有目录索引么 | 有(两类三层)但 § 文档导航缺口:docs/ 高频规范(prepare/feature-planning)、STAGES 编排单源、agents 协议全不在 —— 注入退役后 SKILL 是唯一入口 | 导航 15→24 行补全 · agents profile 目录核实活资产后收录 · TEMPLATES 指向全清单 | 入口唯一化(v8.211)后导航完整性从 nice-to-have 变必需 —— 单一入口的死角 = 全局死角 | 砍掉冗余入口(注入)时 · 幸存入口(SKILL)的索引要同步补强;收录目录前先核实活性(claude-agents 差点被当死资产) |
| v8.213 | 用户:Claude hooks 逻辑去掉 · teamwork 不需要 | hooks=宿主独有自动触发层 · 违跨宿主原则 · post-compact 恢复已被 state.json 断点续跑覆盖 · codex hooks.json 是 cyber-abuse 警告诱因且 spec 已令删但代码还拷(spec-代码矛盾) | 退役三件套:删 hooks/ · maintain 反转清理(签名守卫 4 标记 · 外来 hook 保留)· codex toml 部署保留 · git-hooks 不动 | 退役边界要切三刀:同名不同类(git-hooks≠Claude hooks)· 同函数混装的活功能(codex toml)· 签名守卫防误删用户资产;spec 说删而代码还在做 = 最高优先清理信号 | 多段脚本编辑先算好 assert 顺序(前段成功后段 assert 失败 = 全部丢失);机制退役后查它的『恢复职责』由谁接(state.json 续跑) |
| v8.214 | 用户:升级后会清注入段么 | 会(升级必触发)· 但清理在 skip_maintain 门内 → 同版本 merge 回流旧块永不清 | 清理挪出版本门(每次 bootstrap 跑 · 幂等轻操作 · 同 localconfig backfill 先例)· chmod/gitignore 留门内 | skip_maintain 版本门的分界 = 「一次性维护」vs「持续卫生」:后者(清理/自愈)必须门外 —— 污染源(merge 回流)不随版本节奏出现 | 回答「X 会发生么」类问题时顺手验边缘(同版本/二跑/回流);清理类逻辑默认放版本门外 |
| v8.215 | i18n case:大而明确的需求走全重流程 · 用户:分诊要更智能 | 车道绑死「大」和「不确定」两维度 · 分诊听完就判(无侦察证据)· 判据无学习回路 | v1:triage_evidence 证据槽(看过再判·空着不给判)+ init --clarity + explicit 消费(goal PL 跳/冷审 3→1 · blueprint external 跳 · review 不动) | 智能分诊 = 维度向量(车道退化为预设)+ 证据先行 + 持续分诊(升降级)+ 学习回路(预测 vs 实际);v1 只落第一二层 · 评审重量跟不确定性走·骨架跟半径走 | v2 待做:降级触发 + 台账记分诊预测 vs 实际(末尾加列);v3 年检算分诊准确率校准判据 |
| v8.216 | 用户修正 v8.215:clarity 固定消费仍太规则化 · 该动态决策(可去 QA/ARCH 不只 PL) | 维度→固定动作 = 换了个更细的车道 · 仍是规则;动态机制(stage_review_roles+审计)早已在 · v8.215 绕过它另立硬 gate | 拆硬编码 · gate 回归 roster 路由 · emit role_value_criteria(逐角色「能拦住什么」判断框架)· clarity 降为纯记录信号 | 「可枚举进脚本 · 不可枚举留 AI」的分界再校准:哪些角色有值=不可枚举(留 AI+判据框架+审计)· 框架只供判据/留痕/下限;新机制先查旧机制是否已覆盖(roster 早就能表达一切) | 加规则前先问:这是证据(记录)还是决策(该留给 AI)?决策类只给判断框架不给映射表 |
| v8.217 | 智能分诊 v2(承 v8.216) | 分诊判定无事后打分 → 判据只能拍脑袋;只有升级触发无降级 | triage_calibration 束(预测 clarity/roster vs 实际 diff/轮次)+ 台账末尾加列 + blueprint 降级提议 | 学习回路三件:判定落 state(v1)· 预测vs实际落台账(v2)· 年检算准确率(v3);末尾加列纪律首次实战 —— migrate 单源自模板零改动自动升 14 列 | 度量先行于校准:没有预测vs实际的对照 · 任何「判据太松/太紧」的争论都无据;v3 = 年检分诊准确率报表 |
| v8.218 | 用户第一性重审:保 stage+必须规则 · 评审方式变建议 · 降强制比例 | 1666 行 🔴×139=全是红线等于没有红线;「怎么做」教程把强模型的地板变天花板 | 四段结构试点 review(235→77)+dev(149→63):目标/硬规则白名单(每条 why)/手段菜单(何时值得)/契约;TDD 节奏降为建议(测试证据仍硬门) | 硬规则保留判据=治结构风险(独立性/证据/主权)非教干活;强制的正确归宿:行为指令→产物契约(异常有log=TECH槽)或门禁(test evidence);819 全绿证 stage 散文与机器层零耦合=可安全重写 | 余 10 stage 待试点数据(校准回路)后推开;四段结构进 STAGES.md 定为标准 |
| v8.219 | 用户:goal 要调整么 → 试点 3/12 | goal 除四段欠账外有 v8.216 活冲突(spec 写死派 3 个/2 个 · Output Contract 又说按 roster · 同文两套口径) | 四段重构 153→85 + 组合全交 roster;质疑六问/三闸/导读分层等判断框架归位(roles 单源/硬规则/契约段) | 机制改了 spec 文字没跟 = 活矛盾比缺文档更毒(照章办事把旧规则执行得一丝不苟);判断框架三归宿:角色 telos/硬规则(带 why)/产物契约 | 动态化机制落地后要 grep 全仓旧口径(数量词「3 个/2 个」是好线索);余 9 stage 待校准数据 |
| v8.220 | 用户:6 种类型还有必要吗 → 直接到位 | 敏捷+Micro 仅 11% · 是重量档不是工作形态 · 与维度化冗余;Bug/Planning 是真形态 | 6→3:flow_type∈{Feature,Bug}+preset(full/lite/micro)· 内部图保留键收编 · legacy 别名自动归一 · M 退役 · 存量零迁移 | 合并类型的判据 =「结构不同」vs「重量不同」:前者留(diagnose 先行/不出码)· 后者并入维度;真合并的省力式 = 对外收缩+内部映射(图原样 · 行为等价 · 测试只改 3 处断言) | README/SKILL 6 类型表全文重写 + FLOWS.md 待下版;prepare 关键词表细化 preset 推荐同批 |
| v8.221 | 用户看 prepare 面板:flow=Micro/M-id/micro 前缀旧口径 | v8.220 只合了机器层 · prepare 的 emit 词汇/链预览/分支前缀/关键词表没跟 | emit 加 flow_type_public+preset+config_line_hint · 链键归一 · 前缀统一 feature/ · 关键词表 preset 化 | 大合并的完整清单要含「面向用户的每一处词汇面」:emit 字段/面板模板/命名前缀/关键词表 —— 机器对了面板还说旧话 = 用户困惑源 | README/SKILL 6 类型表全文重写仍欠(下版);branch 旧前缀存量不迁(历史分支不动) |
| v8.222 | 用户:物化校验匹配合并么 → 审计 | 10 处 legacy 比较失配:Micro initial=goal 真 bug + 9 死门(needs-ui/转移/skip/distill) | internal_flow_key/_flow_key 统一归一入口 · init 归一提前到首个消费点前 · +7 死门复活断言 | 枚举收缩类重构的必做审计 = grep 全部「旧值比较位点」:值不再出现 → 比较静默失配(不是报错是**沉默地错**);归一要放在**第一个**消费点之前(50 failed 抓出顺序 bug) | 「对外收缩+内部映射」模式的代价就是归一入口纪律:所有按 flow 分叉必走 _flow_key · 新分叉 review 时查 |
| v8.223 | 用户连问:blueprint_lite/lite 链还需要吗 | lite 三件套逐层坍缩:stage(与 blueprint 同目标只差重量)→ 链(= Feature 的 needs-ui=false 剖面)→ preset(残余职责全被 roster/clarity 接管) | preset 收 full/micro · 敏捷需求→full · 存量三保留(图/内部键/spec 注册标 deprecated) | 合并判据的传递性:同目标只差重量的结构逐层追问会连锁坍缩(flow→stage→chain→preset)· 每层用同一判据「结构 vs 重量」;退役的安全式 = 新路收口 + 旧路保留走完 | 存量 in-flight 走完后删 BLUEPRINT_LITE_SPEC + stage 文件 + AGILE_FLOW(年检查存量清零) |
| v8.224 | 用户:skill 全文件描述审计 | 合并三连后 7 项旧描述在教旧规则 · 流程类型三处平行(FLOWS/SKILL/prepare)· 历次退役各漏扫一个目录(cite→templates·顶栏→standards·hooks→docs) | FLOWS 重写薄壳(102→40 · prepare=判定权威)· SKILL 类型段吃掉过渡注记真重写 · 7 项残留清零 | 大重构后要做一次「全文件描述审计」收尾(注记过渡→真重写);退役清扫 grep 必须覆盖全部内容目录(stages/roles/standards/docs/templates/agents/顶层)不能只扫主场 | 观察项:PRODUCT-OVERVIEW-INTEGRATION(305 行)· 余 9 stage 四段化(等校准数据) |
| v8.225 | 用户:skill 层加模型建议 + 鼓励 subagent 并行 | v8.194 删过 stage×型号枚举(代际漂移拐杖)· 但档位判断框架一直缺;subagent 姿态偏抑制与并行红利实证(i18n case)不符 | 档位表(任务性质→深度/执行/验证档 · 型号仅示例列)+ 三硬边界(关键裁决不降档/独立性>档位/主对话=用户主权)+ 并行姿态翻转(默认先问哪些可并行 · 护栏原样) | 「建议类知识」的正确形态 = 判断框架 + 有界示例 + 硬边界白名单 + 数据回路(台账 host/model 可验);姿态词(鼓励 vs 警告)本身就是规范的一部分 · 要随实证调 | 年检:按台账验「验证档跑校验类」finding 率不掉 · 并行度是否升(subagent 使用数可从 audit 挖) |
| v8.226 | 让渡战略首刀:ultra review 接入 + 用户修正时序(评审时无 MR) | 原方案主路径押 PR comments · 实际评审窗口 MR 未建 —— 时序假设错了 | external-ingest 三模式(session 主/paste 兜底标降级/pr-comments 窗口增强)· 转录归一与裁决分层 · 门禁认 ultra-ingest+异质名豁免 | 接外部能力的通用式 = 摄入器(转录层机器 · 裁决层 AI)+ 产物契约不变 + 证据分级(机器拉取>会话内>手贴);方案要先对时序(产物何时存在)再定信源 | workflow 改投年检工具化(harvest/spec 审计)· review_engine 适配层确认不建(PENDING 留档) |
| v8.227 | backlog:README-EN 类型表残留 | v8.224 全文件描述审计只扫了中文面 —— 双语仓库的「全文件」必须含两种语言面 | EN 表/R2 行/启动例重写为 preset 口径 | 描述审计的目录清单要加「语言维度」(zh+en 各一遍);双语文档最好同 commit 同改(异步必漂) | 年检可加 en-zh 口径 diff 抽查 |
| v8.228 | 用户:管辖外输入(尤其问现状)该直答不走流程 | 5-mode 是全覆盖分类器 —— 缺「不归我管」出口 · 闲聊/现状问答被套 audit_line/状态行 = 仪式噪音 | 管辖判据前置(四类工作才进分诊)+ 直答通道零仪式 + audit_line/状态行作用域收窄 + 禁答后推销流程 | 分类器要有 reject 类:框架的第一判断是「归不归我管」而非「归哪类」;仪式的作用域 = 流程内 · 不是 session 内 | 观察:直答通道被滥用(该走流程的被直答)时 · 用「答完追问升级」的边界兜 |
| v8.229 | 用户:goal 冷审都是主对话模型 | 档位框架在 agents/README 被动躺着 · brief 没推 · Agent 默认继承会话模型(v8.170 判例复发) | goal/review brief 各推一行(QA 验证档 · Architect/PL 不降档 · 按角色性质非一刀切) | 「spec 有但行为没变」先查 brief 推没推(v8.151/170 判例是框架最常复发的病);角色档位要按 mandate 性质分 · 同 stage 冷审不同档 | 台账 model 列可验采纳率(dispatch 是否真开始分档) |
| v8.230 | 用户:档位逻辑该放 SKILL 全局非 goal brief | v8.229 把横切规则塞进两个 stage brief = 三处重复必漂(v8.170 的解药用过头成新病) | SKILL subagent 条目加全局档位规则(表单源仍 agents/README)· 撤两处 brief 行 | 「brief 主动推」适用于 stage 专属机制;**横切规则的消费点是 SKILL 全局条目**(PMO 任何 dispatch 都在其语境下)—— 推送位置按作用域选 · 不是一律塞 brief | 同类横切候选(worktree 纪律/降级 WARN)已在 SKILL 全局 · 姿态一致 |
| v8.231 | 用户:subagent 还在主模型跑 | 档位规则新(到达率)+软约束惯性+**观测盲区**(per-dispatch model 零记录 · 采纳率无数据) | _dispatch_model_distribution(dispatch_log 宽松解析 · unspecified=未分档信号)→ triage_calibration/audit/台账口径 | 软约束的生命线 = 观测数据(无观测的建议永远无法校准);「unspecified」这类缺省值要显式命名成信号而非静默丢弃 | 覆盖面=文件化 dispatch;Agent 工具直派未记 model 的仍不可见(接受 · 可审计路径优先) |
| v8.232 | 用户 case:MR 地址埋段落 · 被迫追问 | ship1 收尾输出靠 AI 自由发挥 · spec 模板 URL 也在最底部 —— 暂停点第一信息该是用户要点的链接 | push emit 生成 user_card(URL 置顶独立行 · AI 原样贴禁总结段)+ §5 模板重写(合并即确认 · 仅冲突/撤回需回话) | 面向用户的关键输出要**工具生成 AI 照贴**(自由发挥必埋关键信息);暂停点排版铁律 = 第一屏第一信息 = 用户的下一步动作对象 | 同类候选:规划收尾 MR 提示 · pm_acceptance 三选项(已卡片化)· 年检抽查暂停点排版 |
| v8.233 | 用户修正 v8.232:不是禁总结 · 是规范结构(卡片+总结都要) | v8.232 把「URL 埋段落」误治成「禁总结」—— 治过头:总结内容(链路/决策/解锁)本身有价值 | 两段定序都必含:卡片(URL 置顶 · 工具给)+ 交付总结(AI 写 · 三槽:链路/决策遗留/解锁)· 次序不可倒 | 治「关键信息被埋」的解是**定序与结构**不是删内容;工具生成与 AI 叙事可以共存 —— 各管各的段 | 钟摆教训再现(v8.169/187 同型):禁令式修复常过头 · 先问「用户不满的到底是什么」(是位置不是存在) |
| v8.234 | 用户 case:auto 停在 pushed · 合了没人收尾 | await-merge 指令只在 stage 文档(emit 没提 = AI 不跑)· 「必停暂停点」被读成 session 终结 · 监控被误解为 yolo 专属 | push emit 补必跑步(全模式)· SKILL 表 stop→stop+监控 · stage 措辞同步 | 「停」的语义要拆:停『替用户做决定』≠ 停『替用户盯状态』—— 暂停点三件套 = 停动作 + 保监控 + 给口令;指令必须在 emit(消费时点)而非仅 stage 文档 | 同型排查:其他暂停点(规划收尾 MR)是否也缺 emit 级监控指令 —— 规划收尾提示已含 await-merge · OK |
| v8.235 | 用户:派发要给匹配模型+为什么 · 鼓励多用并行 | 档位规则(v8.230)+观测(v8.231)都在 · 缺「声明动作」—— 无声明的选择永远是惯性继承 | 声明制:每次派发必给 model+一句 why(派发语句/dispatch Meta/workflow 注释)· Meta 加 model_reason 字段 · 并行鼓励语气再进(能并行的不串行) | 软规则的执行三件套:规则(怎么选)+ 声明(逼出思考 · 留痕)+ 观测(unspecified 计数)—— 三者闭环才会真改变行为 | 台账观察声明制生效后 unspecified 占比走势 |
| v8.236 | 用户:dev brief 有提醒并行么 | 没有 —— 并行规则全在全局(被动)· dev 恰是并行红利最大 stage;brief 还留着四段重构前的「6 步」stale 话 | dev brief +1 行(先问哪些模块可并行 · workflow 优先 · 声明 model+why · 指向全局不复制)+ stale 修正 | 横切规则放全局(v8.230)与 stage 专属提示进 brief(v8.151/170)不矛盾 —— 判据:该 stage 是否是该行为的**高价值主场**(dev×并行=是);brief 里的结构描述要随 spec 重构同步(6 步→四段) | 同型自查:其余 brief 的「必读」行是否还有四段重构前的旧结构描述(review/goal 已新写 · OK) |
| v8.237 | 用户 case:缓存报 up_to_date 实际落后 8 minor | 24h TTL 是按低频发版设的 · 发版节奏已 12h/8 minor —— 缓存参数没跟上使用节奏 | TTL 24→8(失效五条件不变 · FORCE 变量留强制实查口)· 测试用常量零断言改 | 缓存 TTL 要对齐**数据变化节奏**而非拍脑袋;emit 里 from_cache 标记救了诊断(可观测性红利) | 发版节奏若再变 · TTL 可进 localconfig(暂不 · 单常量够) |
| v8.238 | 用户 case:冷审没用其他模型 · 全 Fable 5 | v8.230「SKILL 全局=消费点」的假设被实证打破:全局规则 session 早期读一次 · 派发时刻消费的是 stage-start emit · 最高频派发点(goal 冷审)无提醒 | DISPATCH_TIER_REMINDER 单源常量接进每个 stage-start emit(工具生成 · 全 stage 覆盖 · 不复制回 brief) | 横切规则的消费点之争的终解:**文本单源一处 + 工具在每个消费时刻机械附带** —— 既非散 brief(漂)也非仅全局(读不到);emit 字段是比 brief 文本更强的消费点载体 | 观察台账 dispatch_models:声明制+emit 提醒生效后 unspecified 走势;直派 teammate(无 dispatch 文件)仍不可见 · 接受 |
| v8.239 | 用户:WS 调研浅 · 拆得散 · 该是代码现状×深度讨论的产物 | Step 1 调研软指令无证据契约;拆解无用户讨论暂停点(成品才见人 · 方向无法纠偏);粒度无反压 | 调研深度契约(current_state 附来源 · ws-lint 抓占位/缺失)+ Step 5.7 拆解讨论暂停点(草案先讨论后落盘)+ 粒度反压(BL>8 给不合并理由) | 「产物 = X × 用户讨论」类预期的落法:X 侧上证据契约+机器抓浅信号 · 讨论侧上 R5 暂停点(草案阶段 · 不是成品阶段);占位符是最廉价的机器可抓「敷衍信号」 | 观察:讨论暂停点的「改:默」进台账(既有暂停点观测列)· 若长期全默认 → 讨论稿质量或时机再调 |
| v8.240 | KA-PAGES-F260714041628(aon-main)用户「没看到链接」 | AI key-filter 读 push emit 丢 user_card · 手写卡片 URL 被 markdown 包裹 | user_card 三重物化:pmo_must_read 首位 + 落盘 SHIP-USER-CARD.md + hint 冗余 | v8.233 工具生成卡片方向对 · 缺的只是防截断兜底 | prose 防线对「AI 省上下文的截断/过滤习惯」不设防 · 用户可见载荷一律「位置+落盘+冗余」三保险 |
| v8.240 | JCB WS 7 件审计:4 件薄 · 用户问「是否可以合并 · feature 也可以跨子项目」· 合并后第三次重排编号整卷重写 | 拆分判据缺「交付内聚>子项目边界」(target 单选注释是结构偏置根源);含金量悬殊无信号(反压 BL>8 零触发);落盘后重排 id=整卷重写漏引用风险 | Step 5.7 边界判据(可跨子项目 · 薄承接默认并宿主 · 独立四硬理由:外部gate/节奏/blast radius/管辖)+ 含金量对照标注 + id 不重排纪律(幸存者保留 · 缺号留遗迹行) | 该 case 同时验证 v8.239(file:line 审计=调研契约在跑 · 合并讨论=Step 5.7 该前置的事)——本版只补判据不加新暂停点;判据菜单从真实裁决萃取(S1+S2并/S3半径独/S6gate独/S7节奏独)比抽象原则可靠 | 计数阈值(BL>8)抓不住结构问题(7 件 4 薄)· 比例/关系信号(含金量悬殊 · 承接关系)才抓得住;模板字段注释(target 单选)也会当拆分规则被执行——注释即规则 |
| v8.241 | 用户令全库文档 review(不合理/冲突/冗余/待优化) | 五次大改版各留扫尾债 · 一次审计清出 83 处(照抄即错命令 2 条 · 外审默认反转簇 7 · 流程收缩词表 ~20 · README 双语 8 · 断链 ~12);v8.239 我自己刚写的 Step 5.7 留痕也踩了「add-concern 在规划层不可用」 | 5 路评审 subagent 按簇并行 + 3 路修复 worker 分治(文件集互斥防冲突)· 工具侧 2 治本(REVIEW roster 化 · close-unmerged 放宽)· agents/README 评估后保留但 §一 去重 · 退役词表回归网 test 化 | 病灶不是随机腐烂而是版本事件的尾巴——按「哪次改版留的」聚簇修比逐文件修高效;审计发现「模板字段注释被当规则执行」(target 单选)与「机器 hint 自身残留」(config_line_hint lite)两类非文档源头 | 制度化 > 一次清扫:回归网只覆盖词表类 · 默认值反转/断链类仍靠下次审计——年检把「全库文档审计」定为常规项;发版时改了闭集/默认值 → 当版顺手全库 grep |
| v8.242 | case:DB schema 确认点只给分类概括+TECH 指针 · 用户被迫追问「DB 变更方案是什么」 | §7.5 模板本身无变更点槽位(请确认→直跳选项 · 决策参考=指针)——AI 忠实执行模板仍失败 = 模板 bug;R5(b) 只管选项结构不管决策材料深度 | §7.5 模板补变更点明细槽(对象|变更|用途 + 迁移策略 · 指针降深读补充)+ R5(b) 全局红线(方案确认类必自带明细)+ blueprint brief 机械提醒 | 追问后的第二回就是该有的范式——用户实际消费行为是最好的格式 spec(同 v8.233 ship1 两段定序);「AI 执行了模板仍失败」→ 先怀疑模板再怪执行 | 同类确认点(全景确认/拆解讨论稿/ship1)已各有内容契约 · 但『暂停点自带决策材料』作为通例这次才进 R5(b)——新暂停点设计时默认带明细槽 |
| v8.243 | 用户:PRD 评审 3 个改两路并行 · 保留 PL+外审 · 外审至少覆盖可实现/可验证(并入 QA/ARCH 的点)+ AI 自己的评审角度 | 三路冷审的编排/整合开销 > 收益(QA/ARCH 视角高度可枚举 · 适合方向制);此前 coverage 化讨论一直悬而未落 | 默认 roster [pl, external] + 外审覆盖方向制(必覆盖可实现/可验证 + AI 自主方向 ≥1 · coverage 申报物化门对称 PL-CHALLENGE)+ 六消费点同步 · roster 机制不动可加回 | 「角色」与「覆盖方向」解耦的第一刀落在 goal:枚举型视角(可测/可行)方向化 · 判断型视角(PL 质疑)保持独立角色——这个分界线可复用到 blueprint/review;顺带抓到 v8.241 漏网(architect.md blueprint 主对话残留) | 观察:外审 coverage 申报质量(是否每方向真有 finding/查过无发现)与 PL-CHALLENGE 采纳率同进台账过场观测;若外审泛谈率高 → 覆盖方向拆回独立角色 |
| v8.244 | 用户:tech review/代码 review 同改两路并行 · 内容不变提效率 · 问合理否/更好建议 | 三路冷审 dispatch+整合开销 ×2 stage × 验证轮复利;v8.243 分界线(判断型独立/枚举型方向化)在 blueprint/review 的判断型主线是 Architect 非 PL | 默认 roster ×3 行 → [architect, external] + 分 stage 从严覆盖清单(review 重一档)+ AI 自主方向候选菜单 + cross_review_coverage 物化门(三 stage 统一闭环) | 先评估后动手(用户问『合理么』→ 给判断+三增强+一注意 · ok 后落):增强点都被采纳——评估轮是免费的方案精化;v8.241 roster-aware 铺路使本刀机器侧近零改 | 三 stage 外审 coverage 申报质量统一进台账过场观测 · 泛谈率高拆回独立角色;下一候选:pm_acceptance/ui_design 席位是否同刀(等两路制数据) |
| v8.245 | case:排查收尾 emit「Bugfix / 修配置 / Feature」斜杠清单 · 用户回 ok 无从解析 · AI 再问一轮 | 模板早存在但只有「进X/不升级」单流程形态 · 三候选塞不进就退化斜杠清单;排查不进状态机无机器提醒 · 长会话凭记忆 emit 格式丢 | 模板升级多候选形态(逐一编号 · 具体化自结论 · 💡 必给)+ R5(b) 全局补「斜杠并列=自由文本」+ prepare 排查先行律点名反例 | 「规则存在但模板形态不覆盖实际场景 → AI 退化到自由文本」与 v8.242(模板缺槽位)同构:先怀疑模板形态再怪执行;ok 快捷词的失灵是编号+推荐缺失的连锁症状(好的诊断信号) | 排查无状态机 = 无机器消费点 · 文本载体是唯一防线——若同类格式丢失再犯 · 考虑给排查收尾造一个轻量工具命令(如 state.py probe-close)专为挂 emit 模板 |
| v8.246 | case:test→browser_e2e 自动流转后汇报完即歇脚 · 用户问「为什么暂停了」· AI 复盘自认把回合边界当暂停点 | R4「回合边界不构成暂停理由」规则早在 SKILL · 但流转时刻的 emit 无提醒——长会话磨完 dev/review/test 后『汇报即收工』引力最大 | AUTO_TRANSITION_CONTINUE_REMINDER:每次 auto-transition 的 complete emit 机械附 continue_reminder(立即继续 <next> · 合法停点仅清单)+ R4 补实证 | 消费时点原则第三次验证(v8.238 档位 · v8.242 变更明细 · 本次流转)——「规则在文档里」对长会话中段的行为几乎无约束力 · 工具在动作时刻塞一行才有 | 观察:台账 per_stage 耗时里 browser_e2e/review 的 await_user_min 若仍频现非暂停点等待 → 说明歇脚还在发生 · 再考虑 stage-start 侧也附 |
| v8.247 | 用户递交跨 session 提案:CI 机磁盘 100% 打满 · /tmp/teamwork 48GB cargo target(单 feature 26GB 躺数月) | /tmp/teamwork 是事实约定但框架从未定义(无主命名空间「有人写没人收」)· ship2 只清 worktree · 容器 /tmp 非 tmpfs 无兜底;review-logs 同坑已踩过(300MB)但模式没推广到新载体 | 约定固化(scratch 根+完整 ID+禁根外)+ ship2 tmp-cleanup(verify 后整树删)+ bootstrap TTL 7 天(深度 2 mtime · 按目录整体删防 cargo fingerprint 打碎) | 提案质量标杆:现象下钻表+根因三分+「为什么直觉方案不够」双证据+先例对照+关键设计差异显式偏离+局限明示——可作为跨 session 提案模板;「同坑先例已治但载体换了就复发」= 治理要按模式不按实例 | 观察:下个 feature ship2 看 tmp_cleanup.pruned_bytes>0;bootstrap audit 的 teamwork_tmp_prune.freed_bytes 首月数据 · 若 /tmp 根外仍现泄漏 → 评估 CACHEDIR.TAG 扫描增强(慎:可能误伤非 teamwork 产物) |
| v8.248 | 用户规划 session 实测报 2 bug:ws-lint 把 risks 的 - id: R1 数成 feature(6/10 误报);ws-progress by_bl 全局首见即胜 · 三子项目同号 BL 张冠李戴(勿手改块每刷必错) | ①v8.239 计数正则没圈定 features 段 · 模板自带同写法的 risks[]——**新校验没拿模板全量自测**;②join 键设计违反自家 conventions §4(BL 项目独立)· 且下游 ready_to_start 同键复用 | features 段切片计数 + _pick_bl_row 三级归属判别(registry docs_root/f_id 前缀/目录名 · 兜底保旧)· 两消费点全改 · 测试 +6 | 用户报 bug 姿势好(没擅自改 skill · 绕开+回报+行号+根因)——skill 是共享件 · 项目侧绕开框架侧治本的分工成立;改容器结构(tuple→list)必 grep 全部消费点(:976 漏网当场 IndexError) | 校验类新功能上线前用**自家模板实例**跑一遍(模板即最低测试夹具);观察:BL 撞号判别在多子项目项目的首月命中率(registry 缺失时 f_id 回退够不够) |
| v8.249 | 台账年检(AI 自主时间太久)→ 追 test 23% 根因 → 撞见 v8.247 按 stage 切 target 打掉增量编译 | v8.247 提案「按 stage 隔离防锁争抢」推理错:混淆 feature 级隔离(防并行 worktree)与 stage 级切分(无并发·纯害);我 ship 时没质疑就照收 | target 改按 feature 共享(<feature_id>/target · 串行 stage 复用)· 显式标注错误推理 + 例外 · 两消费点同步 · GC 不受影响 | 年检的价值实证:一个「优化点在哪」的追问顺出两轮前自己 ship 的性能自伤——数据驱动比拍脑袋准;「X 是正确设计」这种自证式断言最危险(v8.242 同类:先怀疑再照收);提案再漂亮也要过第一性(串行 stage 为何需要并行隔离?) | 观察:下批 feature 的 test 阶段 AI 自主耗时中位是否明显下降(现基线 test 6m 中位 / 极值 1256m · 若极值段收敛=命中) |
| v8.250 | 用户:micro 零逻辑改动却背全套 stage 门禁 · 去 dev+pm_acceptance · prepare 后自由执行然后 ship | micro 的门禁是历史包袱(dev evidence/pm_acceptance)· 安全其实早已前置到准入白名单(零逻辑)· 门禁纯冗余 | 新 execute stage(零 prerequisites/artifacts/evidence · 唯一硬边界=worktree 路径+准入白名单)→ ship · 用户验收挪 MR diff · 两套流程图+初始 stage+6 消费点同步 | 「安全前置到入口(准入)→ 执行阶段就能无门禁」是干净的设计:门禁该在最省的地方设一次,不是每 stage 叠;引入新 stage 要同步两套图(engine FLOW_STAGE_CHAIN + state.py MICRO_FLOW)+ ship 前置豁免,漏一处就断链 | 观察:micro 的 AI 自主耗时是否明显低于 full(现在应接近纯改动时间 · 无冷审/评审开销);execute 的准入白名单硬边界是否够——若 AI 常在 execute 里越界改逻辑 → 准入判据要收紧 |
| v8.251 | case:review 卡不可本地闭合的 BLOCKER(F-002 真实 rollout/7d soak)反复磨轮 · 用户手动介入 4 次拆 release-gated vs 本地可 mock | review 收敛协议把「代码缺陷」和「发版证据」混为一谈 —— 都是 open BLOCKER 就都卡 APPROVE;AI 既不会自主 defer release-gated 又会过度 defer 本地可做的(F-004) | 拆两道门:release-gated 裁决(deferred+reason)+ 双向护栏(WireMock 反例进判据 · 空 defer 物化拦)+ carry-forward(pm brief + ship card 列待补) | 「拆开两道门」比「放宽一道门」对:代码门守本地可修,发版门追踪不消失 —— 护栏必须双向(既防死磨又防滥用 defer),单向会从一个极端到另一个;WireMock 这个用户划的具体分界线直接进判据+hint 比抽象原则可教 | 观察:release-gated defer 的采纳率与真实性(发版后是否真补了证据)进台账;若 AI 借 release-gated 逃本地可做的 → 护栏判据再收紧(要求 owed 证据具体到不可 mock 的理由) |
| v8.252 | 并行 session 修的两 ws-progress bug:状态别名(已交付/已上线)判待开始 · .worktree 旧副本被扫到并写入 | 状态匹配只认「已完成」不认交付态同义词 + 子串匹配(基本已完成误判);全仓 rglob 排除集漏 .worktree(并行 worktree 内是旧基线) | 状态桶归一(别名+起始词+剥 emoji)+ unrecognized surface + _ws_scan_ok 排除隐藏段/.worktree + find_ws_file 正本判定多候选 surface | 「工具写进别人 worktree」是并行开发的隐蔽污染 —— 全仓 rglob 类工具必须排除 .worktree(和 v8.247 的隔离同理);状态词表这种自然语言字段要么收同义词要么 surface 不认,静默吞是漂移温床 | 观察:unrecognized_status 警告的命中频次(高=项目状态词写法乱 · 该在模板更醒目约束);多候选 surface 是否真出现过(=历史有 worktree 副本被误扫) |
| v8.253 | case(WS-19):S1 ship 漏翻 ROADMAP 状态格 → 进度误报 0/4 · ready_to_start 失灵 · 人工查账才发现 | --planning-artifacts 是自由声明 · 机器只查「传没传文件」不验「翻没翻状态」—— 声明与落地之间无验收 | archive 新门 _check_bl_flipped:state.bl 行必翻完成态(复用 v8.252 状态桶+扫描排除)· no-planning-changes 不豁免 · 例外走 --bl-flip-exception 审计 | 「自由声明类参数都该有验收门」是通用模式(同 v8.243/244 coverage 申报):声明字段上线时就该问「机器怎么验它真做了」;v8.252 的词表/扫描件是本门的现成零件——基建先行让新门三十行搞定 | 观察:bl_flip_exception 使用频次(高=完成态语义有歧义 · 部分交付场景要正式建模);PENDING bl-status-flip 命中率(高=AI 惯性漏翻 · hint 是否足教) |
| v8.254 | case:dev 集成测试阶段整包塞单 agent · 主对话裸等 · 用户问「为什么只有一个 Agent」· AI 点破后当场拆三线 | 「开工先问哪些可并行」只问一次 —— 耦合度随子阶段变 · 开工最优拆分会过期;「等待 agent」被当合法闲置(主对话可干的自查证据行就摆在那) | 两问补丁进三处(SKILL 姿态单源 + dev brief 消费点 + dev-stage ③ 含独立判据:零文件重叠+可独立隔离) | AI 被点破后的自查质量极高(拆法/判据/主对话活全对)= 能力在 · 缺时刻触发 —— 和 v8.246 歇脚同构:一次性提醒对『随时间演进的决策』无效 · 要么改成『每状态变化重问』的规则形态 · 要么找机器时刻;本次 dev 内无机器 emit 可挂 · 先落文本 | 观察:后续 dev 案例里「等待 agent 期间主对话干了什么」;若文本两问仍频繁失效 → 考虑给 dispatch 文件写入时刻(PMO 建 dispatch_log 时)机械附带两问 |
| v8.255 | 用户看 DB 确认截图:变更只有内容没有目的 · 三张新表无人问「有没有更简方案」 | tech.md 变更表清单天生无「为什么」列(项目照模板填必缺)· 最小化质询无处安放(Architect 简洁性 lens 太泛 · 没点名 DB);v8.242 暂停点「用途」列被项目抄 TECH 表时丢失——两表不同构就会漂 | 模板列升级(+解决什么问题+为何非更简方案不可)+ 四问前置门(全否才入表)+ 暂停点表与 TECH 表同构 + brief 消费点同步 | 「模板有什么列 · 产物就有什么内容」第三次验证(v8.242 明细槽/v8.245 编号模板/本次为什么列)——要什么信息先给模板开列;两处同类表必须同构否则抄写时丢列 | 观察:后续 DB 确认暂停点「为何非更简」列的质量(敷衍一句 vs 真否过方案);若新表数在四问后仍偏高 → Architect 冷审加 DB 变更数硬审查项 |
| v8.256 | 台账年检二批:提效五刀 · 用户批 ①④+投机 · 否 ②③⑤(启动下一 BL=用户主权;不启 auto 本就是要人工确认 PRD/DB;等待是有意设计) | 验证轮从未点名降档(AI 继承重档跑校验活);TC/TECH 顺序陈述被串行做;投机点若放 PRD v1 会撞 80-90% finding 采纳率 | 验证轮降档进 Round 2+ 消费时刻 emit + TC∥TECH 并行同发 + 投机窗(终确认后 · 改:默≈全默 · 等待 26m≈blueprint 27m 恰好藏下)· 时点纪律写死 | 【关键】「等待≠浪费」——有些等待是用户有意设计的确认闸(PRD/DB)· 效率优化的边界=不吃掉决策权;投机执行的安全时点靠数据选(变动率≈0 的窗口)不靠直觉;否决理由与采纳同等入档 · 防同一提案换个包装再来 | 观察:验证轮 dispatch_models 里 sonnet 占比(降档采纳率);投机 TECH 草稿的返工率(用户 ok 后草稿可用率);blueprint 中位是否近半 |
| v8.257 | 用户:DEV-RULES 简化 · 架构有 ARCHITECTURE 了 · 只留 API 契约/错误处理/其他约定 | 六段骨架与 ARCHITECTURE.md/standards 职责重叠 · 空段提示反而诱导填通用最佳实践(违自家「不抄通用」原则) | 骨架 6→3(新增 API 契约段 · 含 standards 覆盖唯一注册处语义)+ 去向声明(架构→ARCHITECTURE+ADR · 命名风格测试→standards 缺省)+ 五消费点词表同步 | 模板段落本身就是行为暗示——给了「命名」段就会被填命名规则(哪怕 standards 已有缺省);砍段落=砍重复注册面;「其他约定」兜底槽保住真特例的去处 | 观察:存量项目 DEV-RULES 是否自然向三项收敛(人维护 · 不强迁);若 API 契约段普遍空置 → 说明 backend.md 存量沿用规则已够 · 段可再评估 |
| v8.258 | 用户点单:增 RELEASE-GUIDE.md 版本发布规范 · 默认=staging→main MR + URL + 提醒合入 | 「发布/上线」是流程空档:ship 止于 feature→集成 · 集成→生产无规范 · AI 遇发布请求只能即兴 | 新模板(默认五步可用 · 含 release-gated 待补证据的发布后义务闭环)+ bootstrap 七件骨架 + SKILL 路由/信息架构双表 + conventions §13 | 与 v8.251 天然扣环:release-gated 证据「追踪到发版」终于有了发版侧的承接点(发布流程第 ⑤ 步);人维护模板可以自带可用默认(不必都是空壳)——空壳适合规矩类(DEV-RULES)· 默认流程适合操作类(RELEASE-GUIDE) | 观察:发布请求命中率(用户说发布时 AI 是否真读 RELEASE-GUIDE)· 第⑤步 release-gated 回填率(v8.251 的观察项在此闭环) |
| v8.259 | 用户:dev-rules 关联发版规范 · teamwork-space 目录登记 RELEASE-GUIDE | v8.258 建文件时漏了知识图谱登记(零死角律)与姊妹文档互链;space 模板 project-specs 内含列早已漂移(RESOURCES 已废未删 · UI-RULES/PROCESS-LEDGER 缺) | 双互链 + 知识入口登记 + 顺带纠内含列(标注清单单源 §13) | 新增项目级文档的 checklist 应含:模板+bootstrap+SKILL 双表+conventions §13+templates/README+**space 知识入口+姊妹互链**——后两个这次是用户补的 · 说明我的「新文档接线清单」缺这两项 | 下次新增项目级文档按七点接线清单走(本行即清单) |
| v8.260 | 用户点单:fast mode 去掉所有评审环节 · 默认关 · localconfig 开 | 评审是主要 AI 耗时(goal 44%+review 7%)· 原型/个人场景用户愿以质量换速度 · 但框架无此档 | localconfig fast_mode → state 快照 + roster 清空(复用 roster-aware 门自动放行 · v8.216/241/244 基建)+ dev 跳 review + review_artifact 标记 + yolo 硬互斥 | roster-aware 基建的复利:fast mode 大半靠「roster 清空」白拿(门自己放行)· 只补了静态产物/转移/前置三处;「模式互斥要写成硬拦不是文档警告」(yolo×fast 语义冲突在 init 即拦) | 观察:fast 项目的缺陷逃逸(pm_acceptance 拒收率/线上问题)vs 全速项目 —— 年检验证「以质换速」的真实汇率;若 fast 被用在正式项目 → 考虑 kickoff 加显眼警示 |
| v8.261 | 用户修 fast 语义:留 PRD 评审(PL+外审合并)+ 代码 review(Arch+QA 合并)· 不再零评审 | v8.260 的「全拆」把两个最值钱的质量关口(需求侧/代码侧)也拆了 · 用户要的是砍多路独立性与中段评审 · 不是砍评审本身 | 合并伪角色 fast 进 roster(单 agent 兼多帽 · 收敛协议全保留)· PRD-REVIEW/dev→review 撤销 skip · 三 brief 写死合并关注点清单 | 「fast」的正解=降**路数**不降**关口数**:关注点可合并 · 关口不可少;伪角色进 roster 让全部既有物化门(verdicts/findings/severity/验证轮)零改动照跑——roster 机制第三次白拿(v8.244/260/261) | 观察:fast 项目单路合并评审的 finding 产出质量 vs 两路制(合并会不会两头都浅)· 进台账对比 |
| v8.262 | 用户:yolo 忽略 fast;PM 写 PRD 把评审点前置考虑 · 别等评审出问题再改 | 互斥报错是硬摩擦(用户要的是优先级不是禁止);冷审 mandate 从没镜像到起草侧 —— 起草按 A 写 · 评审按 B 查 · 差集全变 Round 2+ | yolo 静默覆盖 fast(INFO 留痕 · 顺带修 roster 缩进 bug)+ prd.md §送审前自检(PL六问/可实现/可验证/高频finding 镜像成起草清单)+ 两消费点 | 「评审关注点应同时是起草约束」——mandate 只给 reviewer 不给 author = 制度性返工;模式冲突的正解常是优先级(A 覆盖 B)而非互斥报错;条件分支改动必查同块内其余语句归属(缩进 bug 差点让 yolo 掉进 fast roster) | 观察:自检落地后 goal Round 2+ 率与冷审 finding 数是否双降(降=前置生效;finding 不降=自检走过场 → 考虑物化打钩校验) |
| v8.263 | 用户修正 v8.262:不是加自检环节 · 是写 PRD 时按规范思考 | 我把「前置评审关注点」默认翻译成了 checklist 环节 —— 环节化是框架的惯性肌肉(什么都想做成 gate/清单);用户要的是改**写的动作本身** | 删自检段 · 思考规范织进模板头(落笔即换/写不顺=冷审会打处/先读代码再写)· goal ③/brief 从「步骤」改「写法注」 | 「规范化」有两种形态:环节(写完→检查)vs 写法(写时→思考)—— 后者零仪式成本且更早;框架默认长出环节 · 要自觉抵抗;同类既有物(开工前必须想清的)是开放问题 · 新增的应并入写法而非再造段 | 观察同 v8.262(Round 2+ 率/finding 数双降);若写法引导不奏效再谈环节化 · 顺序不能反 |
| v8.264 | 用户问 localconfig 创建时机 · 答题撞出:fast_mode 漏自愈表(存量项目看不到新选项)+ conventions「可提交」与 bootstrap gitignore 相反 | 新增 localconfig 字段的接线点有三(模板 json + config.md + **自愈默认表**)· v8.260 只做了前两个;「可提交」断言从没对照过 bootstrap 实际行为 | fast_mode 入 LOCALCONFIG_CONFIG_DEFAULTS · conventions 纠为本机级+gitignore 事实 | 回答用户问题时顺手核对机器行为 = 免费审计(两个 bug 都是答题时撞出的);新增 localconfig 字段三点接线(json 模板/config.md/自愈表)入清单 | 我在上一轮口头答错过「可提交 · 团队共享」——文档漂移会先污染 AI 的回答再污染用户认知 · 单源必须对齐机器 |
| v8.265 | 用户:方案要考虑复杂度×收益 · 不做没必要的降级/安全兜底 · 确需的在暂停点明确提出 | AI 天然偏加兜底且从不被拍板(藏方案正文)· 简洁性 lens 太泛没点名兜底 · 暂停点无兜底槽位 | 三层落:起草写法(默认不做)+ tech 兜底清单表(单源)+ §7.5 双触发透出(DB/兜底)+ goal 导读兜底行 + Architect lens 点名 | 「不许默默做」类规则的通用落法 = 判据(默认不做)+ 落盘单源(清单表)+ 既有暂停点扩触发(不造新停点 · v8.263 教训);兜底与 DB 变更同构:都是「方案里高影响要素」· §7.5 从 DB 专用泛化为方案要素确认 | 观察:兜底清单的「无兜底」占比(高=判据生效)· §7.5 兜底触发频次与用户砍除率;若兜底仍偷渡(不进清单直接写码)→ review 合并评审加兜底稽查项 |
| v8.266 | 用户修正 v8.265:兜底不是默认不做 · 要考虑 ROI | 我把「不做没必要的兜底」过译成先验偏向(默认不做/重点砍除)—— 用户语义是中性算账 · 偏向词会让 AI 矫枉过正一刀全砍 | 全部措辞改 ROI 中性框架(立得住做/立不住砍 · 两方向都要实证)· 机制(清单/透出/拍板)不动 | 判据措辞的先验方向会被 AI 执行成硬偏向——「默认不做」和「必须做」同样危险 · 中性判据+双向举证才稳(同 v8.251 双向护栏/裁决举证对称的既有原则 · 这次自己又犯);用户说「不要做没必要的 X」重点在「没必要的」不在「不做」 | 观察同 v8.265(无兜底占比/砍除率)· 加看反向:该兜没兜导致的线上问题(一刀全砍信号) |
| v8.267 | 用户指令:fast 评审最多 2 轮 · 未收敛决策点抛用户 | fast 只收了评审宽度(单路合并)没收深度 —— 循环轮数仍与普通模式同预算(3)· 提速模式下多轮修不收敛更该早抛人 | 引擎 review-retry 预算 min(localconfig,2) 硬拦 + 暂停点列 open findings 即决策点;goal 无轮门 → 复用终确认导读「你要拍板的」作抛出通道(不新增暂停点);三处 brief 消费时点提醒 | 收敛上限的逃生通道选既有暂停点复用而非新增:goal 本就终确认必停 · 决策点并入导读 = 零新增打扰;review 本就有超预算 R5 —— 新纪律优先接既有闸门 | 观察:fast 项目 2 轮内收敛率 · 超预算暂停点选项分布(1 收口/2 继续/3 现状)|
| v8.268 | 用户指令:双路评审模型错开(fable5 一路 → opus 一路) | 双路独立性只隔离了上下文没隔离权重 —— 同模型两路盲区相关 · v8.204 降级默认「同模型 subagent」把可白拿的权重错开留在桌上 | 主审路继承会话模型 · 外审路错开一档;规则进 DISPATCH_TIER_REMINDER + 三 brief + external-review 配方(消费时点)· SKILL 🎚️ 单源;异质硬约束与诚实标注不动 | 独立采样有三层:上下文隔离(冷审)< 同厂商权重错开(本版 · 零成本)< 跨厂商异质(opt-in);默认档应取免费拿得到的最高层 | 观察:错开路 finding 与主路重合率(重合高 = 错开增益存疑)· 外审路 opus/sonnet 的 finding 质量差 |
| v8.269 | 用户指令:单路评审也要和主模型分开 | v8.268 我把 fast 单路写成「不适用」—— 恰好反了:路越少每路独立性越金贵 · 单路同模型 = 起草者自审 · 是最该错开的场景 | 不变式补全:任何评审配置至少一路 ≠ 会话主模型(双路 = 外审路 · 单路 = 该路)· 提醒/两 fast brief/SKILL/模板同步 | 规则按配置分支写(双路如何/单路不适用)容易漏 case · 先找不变式再落分支 —— 「至少一路错开」一句话覆盖全部 roster 形态 | 观察:fast 单路错开后 finding 质量 vs 旧同模型单路 · 错开路与起草稿的重合率 |
| v8.270 | 用户指令:bugfix 单路评审只留 external | Bug 流质量重心在 diagnose(方案已经用户确认)· review 双路是 Feature 制式照搬 · 对小修复过重 | roster 默认 [external] 单路 · Architect 一致性视角并入外审覆盖方向 · brief _bug 条件行(fast 优先)· 门禁 roster-aware 零改动自适应 | 评审重量该随「上游已锁定多少」缩放:diagnose 用户拍过板 → review 只验忠实性;roster-aware 门禁的老投资让这类改动只动默认表一行 | 观察:Bug 单路 external 的 finding 质量 · fix 偏离方案被抓率 · 用户 change-review-roles 加回率 |
| v8.271 | 用户指令:每条 AC 配大白话解释 | AC 只有 BDD 一种写法 · 服务了 QA/机器却没服务拍板的用户 —— 终确认导读说人话的哲学没下沉到 AC 粒度 | AC 表加 💬 大白话列 + goal-complete 逐条机器校验(缺列/空/占位 FAIL 列 id)· 思考规范写时即配 | 同一产物的多读者要多写法并置(BDD 给机器 · 大白话给人)· 且新列要进机器门 —— 不校验的列会退化成装饰 | 观察:大白话列的占位/敷衍率 · 终确认轮用户对 AC 的追问是否下降 |
| v8.272 | 用户指令:PRD 确认暂停点回显文件绝对地址 | 导读只给摘要 · 用户核对全文要自己找路径 —— 暂停点缺「原文入口”一跳 | 导读头行回显 PRD 绝对路径(非相对 · worktree 给 worktree 路径)· spec/brief/SKILL 清单三处同步 | 暂停点产物三件套:摘要(导读)+ 决策项(1/2/3)+ **原文入口(绝对路径)** —— 摘要替代不了可点开的原文 | 观察:终确认轮用户「PRD 在哪」类追问归零与否 |
| v8.273 | 用户指令:审核员只审内容 · 不重复跑测试 | 覆盖方向措辞「测试真跑」被读成「自己去跑」· 流水线硬门已产的证据被评审重复生产 | 措辞消歧(真跑 = 读实跑证据)×3 + review-stage 规则 8 + 两 brief 纪律行;external prompt 本已拦 · 补主审/验证轮 | 检查项措辞要写「动作 + 证据来源」不能只写目标态 ——「测试真跑」缺来源指向 · 每个读者会补自己的动作(评审补成重跑) | 观察:review 轮耗时降幅 · 评审重跑测试残留率(dispatch 日志)|
| v8.274 | 用户指令:space 骨架带安装地址(npx + git)| space 是协作者进项目的第一入口 · 却假设读者已装 teamwork —— 新协作者卡在「这是什么框架/怎么装」 | 两处骨架源(bootstrap 精简 + 模板完整)头部加安装行 · 功能测试锁行 | 面向协作的生成物要自带「上手入口」—— 知识地图根不仅路由知识 · 也路由工具链本身 | 观察:新协作者安装摩擦反馈 |
| v8.275 | case:ship1 卡片被吞(回合中段)+ migration 子串误伤 + 配方缺 target_commit | 模板只规定了内容与段序 · 没规定**投递位置** —— spec 写「贴完 → 跑 await-merge」在回合制宿主必然把卡片挤成中段文本;子串匹配没写路径语义;emit 必含清单与消费方(verify-fixes)字段没对表 | R5(b) 投递位置红线(回合终文 · 后零工具调用 · 监控先行)+ ship 三处次序翻转 + 目录级正则 + 配方补字段 | 暂停点三要素:内容 · 段序 · **投递位置** —— 回合制宿主只保证终文可见 · spec 里「贴完再跑 X」= 必吞卡;emit 的必含清单要跟它的消费方字段对表(写方漏一个 · 读方全 FAIL) | 观察:卡片被吞类追问归零与否 · migration 门误伤率 · verify-fixes 锚定成功率 |
| v8.276 | 用户令:审计计时逻辑 · 没别的问题再落空闲扣除 | 主问题(跨session空闲算AI工作)无法直接扣——AI干活期间 state.py 零打点 · 没心跳;审计还撞出 ②restart 跨废弃尝试 ③严格解析静默丢 duration 两个真 bug | 活动时间戳挖掘(git commit+产物 mtime 作心跳替身 · 间隔>阈值判空闲)· 无信号回退墙钟 · 修 ②③ | 「没有心跳就无法测活动」的破法 = 找工作留下的副产品时间戳(commit/mtime)当心跳替身;先审出根本约束再决定扣除方式(用户条件式授权『没问题才扣』正是逼这步)· 不硬扣而是活动信号 sound 才动 | 观察:active_minutes vs duration 的差值分布(挂机占比)· 无活动信号回退率 · 30m 阈值是否需分 stage 调 |
| v8.277 | 用户指令:兜底清单加大白话列 | 兜底清单和 AC 同病(v8.271)—— 技术名+ROI 服务了 Architect 复审却没服务暂停点拍板的用户 | 兜底名后插 💬 大白话列 · 两表同构 · 结构测试锁列集一致 | 「暂停点上给用户拍板的表都该有大白话列」= 一条可推广的规律(AC v8.271 · 兜底 v8.277 · 下个决策表同理);同类表加列必同步改同构对(v8.255 第 N 次) | 观察:大白话列敷衍/占位率 · 兜底暂停点用户追问是否下降 |
| v8.278 | 用户课题:评审问题多·多轮收敛怎么优化 | 数据锤:findings 82% 真实(砍不得)· 反复撞同类(stale×7/timeout×6)· 沉淀回路断 —— goal 有 shift-left 1 轮收敛·dev 没有 · 收敛成本大块是重新发现可预防复发类 | KNOWLEDGE §复发防御清单(review 沉淀→dev 起草必读照写)· 镜像 PRD 起草思考规范 · 判断型非门 | 「多轮收敛」的正解不是砍轮(findings 真·砍=漏 bug)而是 shift-left 掉可预防的复发子集;诊断先分四根因(真涌现/可预防/晚发现/挑刺)· 数据指到「可预防复发」才动手 · 别一刀切砍评审 | 观察:复发清单条数增长 · dev 起草是否真读(review finding 复发类是否下降)· 该 shift 的类占收敛轮比例 |
| v8.279 | 用户点破:安全/兜底降级也要防过度设计 | 简洁性 counter-lens 泛说「过度设计」· 但 external 最爱加的安全/兜底两类没被点名 —— 它们听着最负责任 · 是盲采重灾区却漏在 ROI 门外(v8.265/266 只挂 blueprint authoring · 没接 external 裁决) | §12 裁决源 + goal/review/blueprint/architect 五消费点点名「安全加固/兜底降级必过 ROI」 | counter-lens 要点名高发类才有牙——泛说「防过度设计」会漏掉「加起来最像正确」的那几类(安全/兜底);既有纪律(兜底 ROI)要接到所有消费路径(authoring + 裁决)不能只挂一处 | 观察:external 安全/兜底 finding 的 REJECT 率(过 ROI 后驳回是否上升 = 门生效)|
| v8.280 | case:aifriends micro 4 行 bump · execute-start 撞 preset-blind 死门 | engine 通用 gate 用 raw flow_type 比 legacy 键「Micro」+ 拿错图 —— v8.220/223 把 flow 收成 Feature+preset 后 · resolve_flow_graph/internal_flow_key 只补进 state.py · engine gate 漏了;现有 micro 测试只断言 spec 常量从没真跑 gate → 整版漏网 | engine 本地加 _internal_flow_key/_resolve_flow_graph(同口径)· 三处 gate 归一 · 加真跑 execute-start 的 e2e 测试补集成盲区 | 「常量断言」不是「行为测试」——断言 allowed_flow_types==['Micro'] 恰好把 bug 写进测试当正确;跨模块归一(Feature+preset)必须每个消费方都改到 · 一处漏=一条链死;preset-blind 是 v8.220 大重构的长尾余债 | 观察:是否还有别的 gate/消费方用 raw flow_type(全库扫 state.get('flow_type') 直接比对点)|
| v8.281 | 用户:评审后记录起草可预防性 · 同步台账供年检完善 teamwork | v8.278 harvest 只沉淀复发 bug 类到项目清单 · 缺「起草考虑点缺不缺」的诊断层 —— 一直靠猜(讨论时我和用户都在猜 PRD/TECH 考虑点全不全)| review-preventability 命令记可预防率+缺的考虑点 → ship 聚合进台账新列 → 年检消费;非门禁纯采集 · 活体验证=aon-core Postback 会话手动做了同样归因(4 条缺口)| 「该不该改框架」的元问题也该数据驱动:先建测量仪(台账列)· 跨 feature 复发才动考虑点 · 单 case 不改(避 v8.266 一刀切);shift-left 三层=沉淀 bug 类(v8.278 项目)+ 诊断考虑点缺口(v8.281 框架)+ 消费(年检)| 观察:缺的考虑点跨 feature 复发 top-N(→ 补 PRD/TECH 考虑点)· 可预防率趋势(降=shift-left 生效)|
| v8.282 | Postback case 归因 4 条起草缺口 · 按建议补普适的 | PRD 起草思考规范缺两条普适陷阱:①grounding 在旧分支(读了代码但读错分支)④只写 happy path 漏 miss 分支 —— 都是任何项目都犯的类 | 补 ①（当前 worktree/ship 目标分支读）+ ④（miss 分支必落 AC）进 prd.md/goal-stage/brief · ②情境进台账 ③项目进 KNOWLEDGE | v8.281 纪律首次实战:4 条缺口按「普适→补框架 · 情境→台账 · 项目→KNOWLEDGE」分流 · 不照单全收也不单 case 一刀切;「读了真实代码」不够硬 · 要点名「读对分支」——grounding 陷阱是读错源不是没读 | 观察:①④补后 EXT-2 类(旧分支 grounding)/ miss 漏 AC 类 finding 是否下降(台账起草可预防性列看)|
| v8.283 | 用户课题:模型变聪明后规则是否负向 · 先做 prd/tech 减法 | 规则不是一个类别 —— 证据/独立采样/用户主权类**不随模型变强而衰减**(甚至该加强);手段规定/能力上限/教学示例/重复/环节化**会衰减**。prd.md 里「≤5 文件 / 5-10 min」是直接给调研深度设天花板 · 且与 v8.282 grounding 要求自相矛盾 | 按判据分类做减法:prd 393→325 · tech 277→238 · 门禁/暂停点一条未动 · 加 12 条回归测试锁「砍掉的别回来」 | ①「规则是否过时」要按**类型**判不能一刀切:问「它防的失败前提是否随模型能力改变」—— 防自证的(证据类)永不过时,教干活的(HOW)最先过时;②**规则可以自相矛盾而不被发现**(能力封顶 vs grounding 要求隔了 100 行、差了 1 版);③ 数据打脸:🔴 密度不是可砍量的指标(ship 33 个🔴 几乎全是物化门禁,ui-design 44 个里大量是前端常识教学)| 观察:减法后 goal 轮次/finding 数是否变化(减的若是有效约束应该变差 · 没变=砍对了);批次二(ui-design/blueprint/ship)与 STAGES.md §3 口径矛盾待处理 |
| v8.284 | 用户:先修 STAGES.md §3 再做批次二 | 四段结构卡在 3/13 六十余版的根因不是偷懒 —— **治理规范还在要求旧结构**(STAGES.md §3 必含「怎么做+质量基线」)· 已迁移的三个反而违规 · 未迁移的在守规矩;v8.218 写下「进 STAGES.md 定为标准」但没做 | §3 转正四段结构 + 写明硬规则保留判据与「为什么删怎么做/质量基线」· 批次二三文件减法(ui-design -23% / blueprint -18% 含修矛盾 / ship -6% 只砍旁白)| ①**试点没转正 = 试点失败**:改了样板不改标准 · 后续文件会忠实地继续错;推广类改动的最后一步必须是「改治理规范」;②「指针+复制被指向内容」是系统性漂移源 —— 同一文件里 9 段 vs 5 段两份 TECH 清单是实证,连 v8.277 的「两表同构」也该升级成单源+指针;③ 删除留痕行会包含被删的词 → 回归测试要断言**规则行形态**而非词 | 观察:批次三(其余 10 stage 迁四段结构)· standards 1773 行减法待做(RD 自查 216 行无机器消费者)|
| v8.285 | 用户:批次三 + standards 减法都做 | 四段结构 3/13(v8.284 刚解锁)· standards 1773 行里最大一块(RD 自查 216)零机器消费者 | 11/13 迁完(两例外写进标准 + 测试守护「不许沉默例外」)· standards -27% · 断链 6 处修好 | ①**减法要有对照组**:同为「自查规范」· Designer 的有 verify-panorama.py 校验(留)· RD 的零消费者(砍)—— 判据是**有没有消费方**不是「看起来像不像仪式」;②**并行改同一批文件要防中间态**:全量测试撞上 agent 写文件瞬时红一次(单跑绿)· 结论要以稳定态为准;③ 我自己写的断言两次因措辞变化误报 —— **回归测试要锁实质不锁措辞**(v8.284 教训复发) | 观察:ship 保留操作手册形态是否合理(下次改它时复看)· standards 能否进一步做「硬规则白名单」让「必读」可行 |
| v8.286 | 用户:接完剩下的逻辑 + standards 再精简 | 「AI 读框架规范 + 项目 DEV-RULES 并集」的架构**早就存在**(standards/ + DEV-RULES 模板已写明分工)· 但**没生效** —— 项目侧必读、框架侧不必读,规范只能靠复制进模板才到得了模型(同一条日志规则活在三处) | 不新建文件(会成第三个家)· 建 47 行 HARD-RULES 白名单作唯一必读 + 接通 blueprint/dev 读取路径 + 删最后一处副本;分册再砍到 1135(累计 -36%) | ①**架构存在 ≠ 架构生效**:分工写在文档里、优先级也写了,但没有「谁必读」的路径,它就只是一张纸;②**必读的前提是短** —— 1773 行不可能必读,47 行可以,所以「精简」和「接路径」是同一件事的两半;③ 逆默认规则的 **why 要留**(模型会反驳教科书之外的规则)· 默认对齐规则的 why 可省 | 观察:HARD-RULES 是否真被读(dev/blueprint 违反白名单的 finding 是否下降)· 分册是否沦为死文件(长期无人查 → 再砍)|
| v8.287 | 用户:TDD 不用写进规范 · 一句「每个 TC 有对应实现」即可 · 怎么做 AI 自觉 | TDD 是手段(判据⑤衰减类)· 模型早内建;框架该管结果。但用户那句话有个缺口:「TC 有实现 + 测试绿」抓不到**假绿**(恒绿空壳测试) | 撤全部手段规定(Iron Law/红绿/自检/反模式/跳过例外)· 留三条结果规则,并**补上用户没提的第二条**(测试真断言 · 禁 mock 自身)堵假绿 | ①**撤手段前先找结果侧防线**:核实假绿已有三道(不走捷径门 / 外审测试真实性 / 假绿抽查),撤才安全 —— 没核实就撤 = 拿掉手段又没有结果门;②用户给的结果规则可能不完整,**补全比照搬更负责**(但要说明补了什么、为什么);③「跳过 X 需用户同意」的例外机制,在 X 不再强制时应一并删 —— 否则是没有主条款的例外 | 观察:review 的假绿类 finding 是否上升(上升 = 撤手段有代价 · 需回补)· TC↔实现漏配率 |
| v8.288 | 用户:TDD 只剩三行 · 还要单独一个文件吗 | 不但不需要 —— 那三条**两条与 HARD-RULES 逐字重复**,tdd.md 已退化成我们一路在消灭的「指针+复制」第二份副本(v8.287 撤手段时只想着「留结果」· 没回头看结果是否已有家) | 删 tdd.md · 唯一独有内容(结果由谁保证表)压成白名单一行 · 10 处入链改指 · 加通用断链守护 + 「退役前规则须已在白名单」测试 | ①**砍到只剩几行时,先问「这几行有没有家」而不是「怎么保住这个文件」** —— 文件是容器不是目的;②删文件类操作要有机器守护:断链检查(v8.285 靠 agent 报出才发现 6 处失效)+ 内容已迁移检查(防删文件把规则一起删了);③ 撤销一个规范体系要两步——撤手段(v8.287)、再回收容器(v8.288),第二步容易忘 | 观察:standards 5 件是否还有可合并的(common 354 / backend 551 仍是大头)|
| v8.289 | 用户:review 流程 · 哪些过程文档没必要写 | 全部产物过一遍「有没有真读者」—— 只有 REVIEW-<role>.md 是纯仪式:门禁只查存在不查内容 · 角色归属早在 findings[].source · 实测与 REVIEW.md 体量几乎相同(37/38·55/63)= 同一批判断写两遍 | 退役独立文件 · 改 REVIEW.md 内每角色一行 coverage 申报(沿用 external 已有形式)· roster-aware 语义原样保留 · 全链 12 处引用清理 | ①**砍产物前先问「它的真价值是什么」**:这里是「防橡皮图章」(光秃 APPROVE 与没评审无法区分)—— 找到更便宜的载体(申报一行)才砍,否则是砍掉性质;②**同一性质在框架内往往已有更省的实现**(external 的 coverage 申报),推广它比发明新机制稳;③ 文件体量对比是「写两遍」最直接的证据 —— 比读内容快、比数引用准 | 观察:coverage 申报会不会退化成套话(「查过无发现」占比)· 若退化则考虑要求点名查过的具体面 |
| v8.290 | 用户:整体 review 流程文档 · 保底线不限发挥 · 砍没必要的 HOW + PRD/TECH 必须高档模型 | SKILL 754 行/74🔴(含单行 3173 字符塞 8 条规则)· prepare/feature-planning 的 HOW-to 教程 · 而设计文档的模型档位一直没规定(只有泛的「判断型不降档」)| 三文档精简(-28%/-11%/-2%)· 行 205 拆 9 条 · 新档位规则落 5 处消费时点 · architect/rd telos 显式「AI 自决」· conventions 如实不砍 | ①**「声称完整的清单」必漂** —— 命令清单自称 55 实漏 11(整个 micro 流程),第三次撞同一模式(blueprint 段落表 / tdd.md / 命令清单)· 凡是「指针 + 复制被指向内容」都该改成纯指针 + 机器守护;②**精简会暴露既有缺口**:UI-RULES 从来没进过路由表,是加同步守护时才发现的 —— 砍的过程本身是审计;③ 我的回归断言**本轮第三次**因锁死措辞误报(「模型错开」vs「模型必错开」)· 已全改锁实质 | 观察:🔴 43 个是否仍偏多 · 分类概览式命令清单会不会让人找不到命令(找不到=补 routing 级条目而非恢复枚举)|
| v8.291 | 用户:跨厂商异质评审太耗时 · 彻底去掉 · 改 subagent 不同模型冷审 | 跨厂商 CLI 的成本不在「调用」而在**故障面**(冷启动/安全审查慢路径/登录/MCP/ARG_MAX)· 为兜住它长出了 ~900 行机械(preflight/超时/降级/质量检查/异质性硬约束)· 而收益只是权重错开之上的边际增量 | 整条路径连机械删除(命令 770→85 · 门禁 136→30 · 规范 286→63 · 配置项/profile 目录/94 条测试)· 契约收敛为 subagent + 申报模型 + yolo 实跑证据 | ①**退役一条路径时要连它的「兜底机械」一起退**:preflight/超时/降级/伪装校验全是为跨厂商的脆弱性而存在 · 留着就是无主的复杂度;②**拆除会暴露不该有的豁免**:「fix 后必验」曾因 CLI 太贵而给 opt-out —— 成本降下来后豁免就该取消(拆除反而让门更严);③ 我两次用正则找 `^def ` 定界替换函数,两次吞掉后面的 SPEC 常量 —— **改代码块一律用行号定界**,正则只用于定位 | 观察:错开 subagent 的 finding 质量 vs 历史跨厂商(台账 external 采纳率)· review 轮耗时降幅 · 假绿/盲区是否上升 |
| v8.292 | 用户:WS 按交付内聚拆 · 不按评审面 · 别拆太多 | 判据表面已写「主判据=交付内聚」· 但同一行把「评审 blast radius」当合法拆分理由、还进了硬理由清单 —— **主判据被自己的例外条款架空** | 交付内聚定为唯一主判据 · 默认合并拆分是例外 · 硬理由 4→3 类(删 blast radius)· 反压 BL>8→>6 且加「按评审面横切」· ws-lint 物化 WARN | ①**主判据会被例外条款架空** —— 写了「主判据 X」还给一堆「但也可以因 Y 拆」,实际执行就是按 Y;例外清单要短且互斥;②「为了好评审而拆」是**用局部优化伤全局**:横切件不能独立上线 · 评审总量不变 · 协调成本上升;③ 判断题的机器守护应是 **WARN 不是 FAIL**(摆问题不代拍板)—— 与 v8.281「非门禁纯采集」同一姿态 | 观察:台账里 WS 的 BL 数分布(是否真下来了)· ws-lint WARN 触发后用户的合并率 · 合并后 review 轮次是否上升(上升说明合过头)|
| v8.293 | 用户:逐个文件整体 review 找冗余 | v8.291 退役只砍入口没砍被调链(死岛 680 行)· 3 处「退役声明贴头上、正文一字未改」(最重一条让 review 硬规则 1 与规则 8 打架)· 三份 flow-key 实现对同一 legacy 输入解析出**不同的转移图**,而其中一份注释声称「严格同口径」 | 净删 1600 行 · 敏捷需求/lite 整条退役(不选边)· 孤儿模板拆解或退役 · 8 类新门禁(死物不复活/声明与正文一致/三实现一致/围栏平衡) | 「零消费者」扫得出死文件,却扫不出**死调用链**与**头部声明与正文的错位** —— 这两类要按「谁在消费它」和「它自己承诺了什么」分别追 | 退役要顺着调用图删到底、并回头读被声明覆盖的正文;**声称同口径的注释是最危险的一种注释**(它让人不再去比对) |
| v8.294 | matrixpower SVC-PLATFORM-F260726 评审耗时复盘 | localconfig 五个读取者「遇 .git 停」· 而配置不入 git 只在主工作树 → 默认 worktree 模式下五项配置**从未生效**(fast_mode 自 v8.260 起一直是死的)· 简洁性四问跑了但**参照物由作者叙事给定**,盲区没破 | 抽唯一 localconfig 解析器(跨 worktree 边界)+ 生效可见 · rival 设计强制(评审新增结构必须自生成替代形态)· TC 职责边界(判据「换实现就要改的不属于 TC」)· 起草期角色合一 / 评审期不合 · 投机窗准入 ≤1 开放结构项 | 复盘的量化(35% 轮次)让人想合并两文档;拆开看**一半是 TC 越界**——**账要拆到能行动的粒度才不会修错东西** | ① 配置读取要按「配置实际住在哪」设计边界,不能按「仓库边界」想当然 ② checklist 是验证式的,**要它破盲区就必须要求生成对抗方案** ③ 复盘给的修法可能治标,先核根因再决定动哪 |
| v8.295 | 用户:要不要加每阶段耗时复盘 · 记到固定文件夹 | 机器采到的耗时只有**数字没有归因**(「318m 花在哪」)· 而归因只有 stage 结束时记得住 —— ship 时回填要靠产物 mtime 反推 | `stage-cost` 采协调开销轮次 + 类型 + 一句归因 → ship 聚合进台账末列 · 提示放 complete emit 只提 7 个有往返成本的 stage | 先盘存量再决定加不加:已有 PROCESS-LEDGER(在项目里、进 git)缺的只是**一列**,不是一套新机制;`docs/audit/` 22 个文件「只写不读」是新建文件夹的现成反证 | **用户提「加个机制」时,先问缺的是载体还是字段** —— 这次缺字段,新建载体只会多一处没人读的产物;另:声称提效的改动必须同时留下能证伪它的采集,否则下一轮复盘还是靠人肉考古 |
| v8.296 | 用户:docs/audit 可以去掉了 · 数据没办法追踪 · 以 retro 为准 | audit 的 telos 是「跨项目搜集流程质量」,产物却落 `~/.teamwork/`(机器本地 · git 不跟踪)—— **载体与 telos 从一开始就矛盾**;代码自陈「只写不读」已挂了很多版 | 整条退役(写入器 + 目录 + 死参数 --main-model + 豁免规则 + 文档段)· 反馈改走 PROCESS-LEDGER 反思摘要 / RETRO 行 | 删之前查了一下:那两个 audit 测试其实是 `ledger_timing`(台账四列的数据源)**唯一的端到端覆盖** —— 改成断言活路径而非删掉,顺带补了这个缺口 | **退役一条链路前,先看它是不是别人唯一的测试宿主**;另:产物落在哪比写不写更决定它有没有用 —— 机器本地 = 注定聚不起来 |
| v8.297 | 用户:耗时归因/流程反思写不进台账 · 该单独成文放复盘目录 | 台账单元格 ≤1 行,v8.295 把归因塞进去 = **压缩掉的正是它的全部价值**;另:digest 四问一直「只 emit 不落盘」—— 说完即蒸发,年检读不到 | 新 `templates/process-retro.md` → `docs/retros/<id>-process.md`(四段 + 开销可判定判据)· 台账列收窄为「比值 + 指针」(查表得趋势 · 顺指针得细节)· digest 四问接进复盘 §三 | 上一版刚立的反向门禁(新增 ledger_* 必须同步进台账指令)**本版当场生效** —— 拦下了 `ledger_process_retro_path` | **给字段选档位**:可算的留台账、叙述性的给文档 —— 塞错档位比不记还糟(看着有数据,其实价值已被压没);另:「只 emit 不落盘」的产物等于不存在 |
| v8.298 | 用户:整体 review 流程文档找同类档位错配 | 两条判据**声明了却算不出来**:PL-CHALLENGE 采纳率进台账(实际无落点 · 列示例只有 review 侧角色)· kill criteria 要数「连续数月无新判例」(判例是叙述 · 无可数字段) | 不加列,用约定修:角色列按 goal/review 两段写全且零也要写 · 有判例时反思摘要加 `判例:` 前缀 · 立类级门禁(率/频次判据必须映射到实有列 + 锁列数 16 防「靠加列糊弄」) | 把上一版的个案抽象成**四类可扫形态**再扫,才找得到同类;只凭印象 review 会漏 | **判据与数据源要一起设计** —— 先写「长期零采纳则收紧」再想怎么取数,多半取不出来;另:约定式修法(前缀/写全)比加字段便宜,但必须**三处同步**(定义+消费方数法+产出方回填),缺一处前缀就永不出现 |
| v8.299 | matrixpower BL-038/039 规范提案 + 用户「测试太多影响效率」 | 退役类改造把两个**互斥 population** 的总数拿来互相印证并自称「吻合」· 体积门(tech.md <250 行)拦下三条逆默认规则 —— **门度量错了东西** · 测试「太多」的前提被数据推翻(61% 用例 <5ms 合计 0.00s) | 收下七条条文(全落逆默认/不可知格)· 体积门 → 价值门(硬规则必带 why)· 立测试效率判据(只优化 >50ms · 共享 setup 不合并断言)· run_tests.py 自学装箱 58s→16.9s | 提案已是条文形态(落点/条文/why 三段)——**这种形态可直接裁决,比复盘叙述省一轮翻译** | **数字碰巧接近比对不上更危险**(它让人以为核过了)· **门要度量价值不是体积** · **优化前先测**:用户说「太多」时,先看是数量问题还是单价问题 · **派发声明漏了不是措辞不够狠是位置错了**:提醒在 stage-start,派发在 15 个工具调用之后 —— 义务要寄生到必写物(prompt 首行),不能另起一句;「这次比较难不该降」是最易自我合理化的一步,故例外收归用户授权 |
| v8.300 | 用户:档位映射写清晰点 + 发版 | v8.299 引入新规则留下**三处自伤**:单源侧仍写被取代的旧口径 · 「三条硬边界」实际已四条 · 两个断言锁死旧措辞 —— **三处都是本 session 反复抓的形态,这次是自己种的** | 档位映射按厂商分列(Claude / GPT·Codex 各一列)· 三处自伤全清 · 立门:宣称条数 == 实际圈号数 且跨文件引用一致 | 「加新规则时只写新的、没回头改被它取代的旧的」**有稳定复发率** —— 值得当一类防,不是每次事后修 | 改规则是**两步**:写新的 + 撤旧的。只做第一步时文档看起来是对的(新规则在那儿),但读者会撞见两套并存的口径,而**旧的那份往往在更权威的位置** · 🔴 **第四处自伤:本版三个新门落在 `__main__` guard 内,pytest 根本没收集** —— 不报错不红就是不跑,我还报了没验证过的测试数;立 AST 门并**注入自验**(否则「立了个不会响的门」是同一个坑套娃) |
| v8.301 | 用户:① 为什么 goal 阶段调 verify-ac ② 要不要减脚本数量 | 在门禁适用阶段之前跑它 → **注定 FAIL → 被自辩成「预期的」** · 而「预期的 FAIL」会把 FAIL 训练成噪声;诱导源是 PRD 模板一句**属实但摆错位置**的话;实测 26 辅助命令只有 6 个进 brief | 工具侧裸失败 → 路由信息(保 exit=1 不拆下游门)· 立 R-SP-1c · 命令按**记忆义务**重分 A必记/B时点推/C逃生口 · 补 review-preventability 与 ledger-migrate 的动作点推送 | 先测再答:用户问「要不要减脚本」,数据显示**该减的是记忆负担不是命令数** —— 删命令丢功能,推到时点则两全 | **属实的话摆错位置也会误导**;**写进 stage 文档 ≠ 到达**(文档在 start 读、动作点在几十个工具调用之后);**没有机器可观测动作点的命令,不硬塞假时点** |
| v8.302 | 用户:抛给用户的问题都该有建议和理由 | 四条待决策项裸列 · 用户被迫逐条追问 —— **而 R5 红线早就强制「推荐+理由」**;真正的洞是 PRD §待决策项**表里没有承载它的列**(注释提过一句,但那是散文不是结构) | 表加「💡 建议 + 理由」两列 + 示例行 · R5 红线从「三选项格式」扩到**任何决策项** · goal 终确认导读两档口径统一 · 给「推荐不了」定义三种合法出口(缺信息/纯偏好/等上游 · 留空不算) | 规则在、却没落地 —— 因为**载体的形状决定内容会不会出现**(同 v8.297/298) | **查「规则有没有」之前先查「结构承不承载」** —— 一条写在红线里的要求,若填写载体没有对应字段,它就等于不存在;加规则不如加列 |
| v8.303 | 用户问「你为什么总出错」· AI 自省 9 个错归纳共同点 | **「读了旁边的代码,然后把结论说成读过那一行」** —— 证据真、推论自加、两者同一语气给出;框架 7+ 条相关规则**全在输入端**(读没读),**没有一条管输出端怎么标** | 新增 R3-E:要么读过那一行要么写「推断」· 不许中间态 · 给分层写法 · 声明抗衰减属性;HARD-RULES 7 补「测试输入必须来自真实链路」(恒绿假绿的第二条路) | AI 的自我归因比笼统的「验证不够」准得多 —— **它自己指出验证量不小,缺的是标注边界**;这种自省值得直接采信并条文化 | **不是所有「体积门」都该让路**:v8.299 的行数门度量错了东西(换掉),本版的 🔴 密度门度量的是注意力经济(服从它,裁自己的解释性 🔴)—— **该服从哪个取决于它度量的是什么** |
| v8.304 | 用户提案:隔离 Reviewer 无文件读取能力 · goal 冷审被阻断(宿主 codex) | 🔴 **v8.293 删了 codex-agents 源却没写回收** —— 零工具 profile 留在用户项目里继续被选中;而它零工具是**旧架构的设计**(当年文件 inline 进 prompt),与 v8.291 subagent 冷审 + v8.303「读真实代码」**架构性不兼容**;门禁又把**能力缺失说成评审问题** | bootstrap 部署→回收(签名守卫)· 门禁区分 CAPABILITY_BLOCKED 并给处置 + 明示预算未消耗 · 配方要求 subagent 有读能力 + 产物记 files_read | 提案的 5 条里有 3 条基于「teamwork ship 了那个 agent」的前提 —— **先验前提再动手**,否则会去修一个不存在的源;而它真正指出的问题(执行失败 vs 评审失败)确实是框架的 | **退役源必须同时写回收** —— 本 session 抓过多次「删了源留下副本」,这次后果最重:**副本在用户项目里阻塞流程**;另:行式 frontmatter 解析器让 `[]` 变字符串,**判定要按解析器实际行为写,不按 YAML 语义想当然** |
| v8.305 | 用户:看下 fast 模式是否有 bug | fast 的 blueprint 被门禁强制要 external,而同 stage 的 brief 明写「评审跳过」—— **brief 与门禁对立**;四个面:两个 evidence check 对空 roster 语义相反 · 靠键缺失表达意图 · change-review-roles 拒未配置 stage(**用户无法自救**)· `fast` 伪角色不在枚举(**框架自产自拒**) | 守卫对齐「空 = 不要求评审」· fast 显式写 blueprint: [] · change-review-roles 可对未配置 stage 设置/清空 · fast 收进枚举 | **AI 做对了每一步**(不篡改 state · 不 bypass · 先试官方命令)**却仍白付一轮** —— 框架没给它任何正确出路 | **修守卫时首版过宽,被既有测试拦住**:两种「缺失」含义相反(有意去掉 vs 未初始化);又一次印证 v8.303 —— 我据一个场景推出改法,**没验证没亲眼看过的那个分支**;另:**放宽约束时依赖它的下游全部要复核**(KeyError 当场) |
| v8.306 | aon-core:AI 在主窗口跑了测试(用户问「为什么没切验证档」才发现)+ 它自己提的 verification receipt 提案 | 规则它读过、还引用了版本号,**漏的是时点**(提醒在 stage-start · 动作在几十个工具调用之后 · 同 v8.299/v8.301 机理);另:框架对「**先绿、后改、仍拿旧日志过门**」此前完全没挡 | dev complete 双门:`test_evidence_fresh`(`HEAD tree` + **未提交 diff** 的 sha256 · 🔴 **complete 自己重算不读申报**)+ `test_runner_declared`(main-window 需 `--user-confirmed`)· dev/test start 直接出 `verification_recipe`(派发语句 + 采指纹命令 + complete 三参数) | 提案分三级裁决比整包收下准:`agent_task_id` 在 Codex 宿主取不到,**收了就是注定失败的门**;而 tree-hash 那条恰恰是提案里唯一完全可机验的,优先做 | **「能验证」与「能拦」是两回事** —— tree-hash 可重算 = 零信任;runner/tier/model 是自我申报 = **拦忘不拦骗**。提案宣称「这一个门禁就能直接防住」是过誉,**条文必须自带能力边界**,否则读者会据它放松其他把关 |
| v8.307 | 用户:①不考虑文档审计,框架相对默认工作流优势在哪 ②指导类还有哪些要砍 | v8.285 砍「模型默认就会」那轮**执行不彻底**:frontend.md 头部写着「选型教程不入库(v8.123)」正文却全是教程;ui-design v8.284 删 WCAG 细则的同一判例没执行到 frontend;backend「模块设计判定」引用的单源 **v8.96 就删了**(~200 版无人发现 = 零消费者) | -525 行(frontend 90→18 · backend 563→329 · tc 266→210 · P-O-I 305→206 · scripts-policy 276→212)· 三处断链修复(死单源退役 · post-feature.py 死引用 · v8.304 toml「部署→回收」两处未撤)· 立零信任门(spec cite 的脚本必须真实存在)· backend/frontend 围栏配平移出豁免 | ①先答「优势在哪」再砍,砍处与四个不衰减机制零重叠 —— **剥掉文档审计,剩下的恰好是分类学里不衰减的四格,文档从来不是承重墙**;②「裁定只执行了一半」有稳定复发率:头部声明与正文矛盾的文件,声明所在的那半永远先写完 | **判例要按类执行不按文件执行** —— v8.284 删教学细则时若按「全库同类内容」扫一遍,frontend 这 70 行当时就该走;另:引用死单源 ~200 版无人发现,本身就是该内容零消费者的最硬证据 —— **断链存活时长是消费者数量的代理指标** |
| v8.308 | 承 v8.307 的第二梯队(用户「按建议」) | **双副本漂移的现行犯**:Designer 自查报告模板 common.md 存 5 维、ui.md(宣称单源)存 6 维 —— 第 6 维经四C 注册只加了一边;顺藤摸出三个死锚点(designer.md §6维自查 / ui-design-stage §框架基线唯一性 / pmo.md §格式权威守门 · 全不存在);另 backend WARN 规则同文件写两遍 | -310 行双写合并(backend 日志双段 · prd checklist · feature-planning 坑 1/3/5 · ui-design 硬规则块 · common §三脚本树)· 报告模板收单源 ui.md + 四B 补第 6 维定义 · 三死锚改指实处 · KNOWN_ODD 6→3 | 「合并双写零语义丢失」用测试逐条锁实质条款(15 条日志条款 / 7 脚本名 / 6 契约点)—— **压缩的验收标准是条款清单不是行数**;自己的门当场抓了自己两处悬空引用(坑 4 编号 · dirty state 计数过严) | **「单源 + 副本」结构里,经注册流程的增量只会加到注册表指向的那份** —— 四C 注册把第 6 维加进 ui.md,common 的副本永远等不到同步;防不住就别留副本。另:verify-panorama 只硬校验 1-5 维的缺口**标注不修**(存量 5 维报告 · 硬拦 = 注定失败的门· 观测第 6 维缺失率再收) |
| v8.309 | 用户裁定 v8.307 摆出的口径冲突:以 R-SP-8 为主 · 实证允许 · 版本标清扫 | conventions 与 R-SP-8 正面冲突多版,各被遵守一半;版本标实测 244 处/49 文件,**相当一部分是本 session 自己近几版写的** —— 禁令在、写它的人也读过,照渗 | conventions 重写(实证=合法消费者标注 · 纯考古判据「删掉这句说服力掉不掉」)· 两轮清扫 244→0 · 立机器门(spec 现 v8.N+ 即红 · 豁免仅版本单源与范式标)| 清扫当场撞出三个「测试拿版本标当语义标记」(实证锚 / 豁免标记 ×2)—— 修成按实质,其中一处 spec 侧改写后测试还能删豁免收紧 | **两条规范打架时,真实行为是各遵守一半**(比全不遵守更隐蔽 —— 每条都能找到被遵守的例子);裁定后要**同时**收条文与立机器门,只改条文 = 下一版照渗;另:**版本字面量不许当语义标记用**(豁免/锚点按实质写 · 否则清扫类操作会把标记连根拔掉)|
| v8.310 | 用户:各 md 是否需要合并 · 去掉没必要的注释 | 索引文件的簿记性内容全在漂:STANDARDS 的分册简介还在描述 v8.307 已删的教程 · TEMPLATES 页脚指向三个不存在的「格式权威」条目 · 红线三重复写;另 14 处「原 N 行已删/借鉴 XX」删除记账 | 退役 STANDARDS/TEMPLATES/frontend 三文件(独有内容逐条搬家 · 测试锁)· frontend 并入 common §七 · 考古注释清零 + 机器门(首跑抓到人工漏的 2 处)| 合并判据一句话可判:**要么减重复要么减读取量,两者都不减的不合** —— 据此否掉 roles 9→1(只减文件数)与 P-O-I 合并(tools 咬死路径 + 450 行巨文件更差),判断记进测试 docstring 防下轮重议 | **索引描述是内容的副本 —— 索引也服从「双副本必漂」**,分册简介漂了两版没人发现;索引只该存在于「有人真按它路由」的地方(SKILL 文档导航),二级索引一律退役;另:删除记账(原 N 行)与 why 原则(为什么不收)要分开 —— 前者是考古,后者防回潮 |
| v8.311 | 用户:TROUBLESHOOTING 理论上不该被 AI 更新 · 它是用户主权文件 · AI 可自动改 KNOWLEDGE | 两处条文明确指示 AI「连法缺失 → 补进 TROUBLESHOOTING」;且 conventions 清单里它**没标维护方**(DEV-RULES/UI-RULES 都标了「人维护」· 它裸着)—— **没归类的文件,写入权默认漂向 AI** | 镜像 DEV-RULES 模式五处修:转记流(记 KNOWLEDGE + 提示用户固化 · 不代写)· 三处归类补标 · KNOWLEDGE 边界表补「运维连法」行 · 机器门(TROUBLESHOOTING+写动词 无豁免即红)| 工具层核过是干净的(只读 + bootstrap 骨架)—— 问题全在条文层;新红点撞 🔴 密度门,按判例服从门裁自己的 | **文件的写入权要靠显式归类守住** —— 边界表(什么写哪)漏一行,对应文件就处在「谁都可以写」的默认态;人维护类文件的判据不是「AI 会不会写坏」,是**运维真值只有用户知道对错**(AI 摸出来的连法可能碰巧能用但不是规范做法)|
| v8.312 | 用户:测试有用没用都写 · 卡 CI · AI 处理 case 消耗大 · 提议按生命周期分层(两次拍板:只写规范不配门 + L3 落 scratch;进 L1 必须有充足理由) | 框架此前没给「临时测试」一个合法的家 → 全部入库进 CI 进维护语料;且 v8.299 的「执行便宜」结论容易被误读成「数量无害」—— **执行便宜 ≠ 维护便宜**(维护按语料线性) | 三层生命周期(单源 tc.md · HARD-RULES 17 必读到达 · dev-stage 动作点规则):判据按「失败信号的消费者」不按阶段名;L1 例外化(`ci: true` 必带 `ci_reason`)· L3 落 scratch `scaffold-tests/` 不入仓库 · CI 对接归项目主权;顺手清「~50 行」数字宣称三处 | L3 方案零新机制 —— 与截图同构复用 scratch + ship2 回收;L1 理由用模板字段承载,「不配门」与「必有理由」两个拍板不冲突(结构可见 ≠ 扫描拦截);守卫测试反向锁「没人顺手造门」 | **同一个「太多」要拆成执行成本与维护成本两本账** —— v8.299 测的是前者(结论:别合并断言),这次治的是后者(结论:临时的别入库);另:**预防式治理优于清退式治理** —— 给临时产物一个合法的家,比造清退门便宜且不会漏 |
| v8.313 | SDK-F260809171303:yolo 在 pm_acceptance 停下等 1/2/3 · 用户:yolo 应自动合入 yolo 分支(追加拍板:外部发布合入后单独停) | `_pm_acceptance_brief` 无条件「停等 1/2/3」**没有 yolo 分支** —— SKILL 承诺的「自动 approved_and_ship」从未物化,动作点载体反向覆盖(fast_mode 同族第二例);另:yolo 安全模型是分支门,而 npm 公网发包/建公开仓**不经过分支**,「零 stop」字面执行无门可拦 | brief 按 state.yolo 物化自动路径(AC 对照不跳 · WARN 留痕 · 真问题 rejected 回修不硬过 · release-gated 随行)· 外部世界动作边界三载体同口径(先自动合入清场 · 外部发布单独停 · 不得以此为由停验收) | **AI 的停错挂点里往往埋着一个对的直觉**——它感到公网发布不该自动,但把这个判断挂到了 pm_acceptance 并转述成框架规则;修法是给直觉一个正确的挂点,不是骂它停错 | **模式类行为(fast/auto/yolo)每一条承诺都要问一句:动作点的 brief/门禁物化了吗?** —— spec 表与 brief 对立时,赢的永远是动作点;这已是同族第二例,「模式承诺 × 动作点载体」值得做一次全量对账(观察项) |
| v8.314 | 用户:跨子项目时容易拆多个 feature · 应从业务交付视角拆 | 规则早已在三处(交付内聚 · 可跨子项目 · 用户拍板过)—— 顶着规则的是三个**结构性推力**:prepare 路由单数假设(跨子项目零指引 · 前缀别扭推向拆开)· 判据埋在 500 字巨条目中段 · 讨论稿「边界理由」槽能用技术话术糊 | 路由补「跨子项目=一个 feature · 前缀取业务交付宿主」· 讨论稿/WS 模板加「业务交付物」必答槽(单独上线后谁得到什么 · 写不出=并回)· checklist 判据拆独立条目(170 字 · 测试锁长度) | 否掉 ws-lint 结构启发式:合法跨子项目对与横切件**结构同形**,「交付物写不出来」这个人读信号比启发式准;规则失效时先找**顶着它的推力**而不是加重措辞 | **判据的对抗面是能糊过它的槽位**:「边界理由」问的是「为什么拆」(技术话术答得出),「交付物」问的是「拆完各自是什么」(横切件答不出)—— 同一判据换个提问方向,可糊性完全不同;另:隐性推力(前缀别扭)不点名就永远在暗处起作用 |
| v8.315 | CA-F260810:四条 D 项「建议 A——<术语压缩>」· 用户被迫追问大白话/上下文/场景 | v8.302 修的建议+理由**都在**,仍追问:术语压缩 · 无场景 · 🔴 **B 选项从未出现**(「可回 D1=B」而 B 没定义 = 假选择题);既有 spec「A/B 选择题 + 说人话」是**形容词**串在巨型段落里,没明说「每个选项内容都要写出」 | 拍板项固定四槽(场景/要定什么/每个选项内容与后果/建议理由)· 三载体同口径(goal 导读独立块 + PRD 表注释〔数据源:问题列自含场景 · 选项列禁占位〕+ SKILL R5)| 挑动作点插独立短块而非改巨型段落(到达质量);SKILL 侧用 ❌ 不用 🔴(密度门 54/55 · 预算内) | **形容词式要求(说人话/写成选择题)糊得过,槽位式要求(每个选项内容写出来)糊不过** —— 与 v8.314「边界理由 vs 交付物」同一结构:修要求先改提问方向再谈措辞;另:检查决策项质量的最快测法 = 假装自己没读过产物,只看拍板块能不能选 |
| v8.316 | 用户附截图:WS 总览加大白话目标列 · 子项目改涉及子项目 | 总览「功能」列是技术短语,扫表看不出每条做完谁能干什么;且 v8.314 刚立的「交付物」槽与本次「大白话目标」是**同一概念两个名字**(昨立今撞 · 不合并就是下一个五维/六维) | 概念合一单名「大白话目标」:讨论稿槽→`features[].goal_plain`→总览表列 四处同名 · 旧名清零(测试锁);parser/渲染容缺(存量 WS 显「—」);「涉及子项目」改名对齐可跨子项目语义 | 新概念落地前先扫一遍「已有槽位里有没有同义物」—— 这次撞上的是自己 24 小时前立的槽;合并成本今天最低 | **用户要的「加一列」往往不是加数据,是把已有判断变成可扫视的形态** —— 大白话目标的内容在讨论稿里本来就必答过,缺的只是它流到表里的管道(字段+透传+列);修管道别造第二个源 |
| v8.317 | matrixpower 前后端联动小改触发复杂度门推荐 Planning · 用户问门怎么判的 + 拍板收窄 | **三载体两口径,权威那份过宽**:prepare §2.1(判定权威)写「跨独立部署服务」·另两处写「跨仓库联动」—— AI 跟权威 → monorepo 双部署单元命中;但部署拓扑不影响承载,**真边界是 git 仓库**(worktree/MR 都是仓库级) | 信号 1 收窄为跨独立 git 仓库 · 同 repo 多部署单元显式不计入(接盘:规模→≥2 BL 信号 · 部署协调→WS 串行约束)· 五处统一 · 旧口径清零测试锁;模板写死推荐 Planning 保留(收窄后真命中的都是跨仓库大变更) | 门响得对不对,先查「信号度量的量 与 它想保护的承载机制」是否同轴 —— 部署单元与 worktree 容量根本不同轴;另核实了调研义务链真在工作(triage_evidence 空着不给判 · 本 case AI 调研扎实到发现 AntD 视觉必填问题) | **多载体写同一判据时,口径分叉的那天没人报错 —— 直到有人踩中较宽的那份**;判据类内容要么单源+指针,要么上一致性测试;「强制问但出口留给用户」的门设计本身被 case 验证是对的(用户一个『2』就纠回来了) |
| v8.318 | worknode /tmp/teamwork 141GB · 单 feature 78GB · 用户:feature 结束是否清理 + 拍板 ship1 就该清 | 回收双通道在 worknode 形态下都够不着:ship2 清理**不在积灰的机器上跑**(session 在 ship1 交 MR 后结束/换机)· TTL 是时间判据救不了空间问题(7 天窗 × 78GB/件) | push 成功即清(emit scratch_cleanup 含体量)· abandon 即清 · closed_unmerged 可重开保缓存 · ship2 转幂等兜底 · 口径双通道→三通道 | 终止路径的保留语义要分开:abandon(不回炉)清 · 暂关(可重开)留 —— 一刀切会把可重开的冷编成本白付 | **清理动作要挂在「证据完成上岸」的最早时点,且必须是积灰机器自己会跑到的命令** —— 挂在换机后才跑的阶段 = 设计上存在、物理上永不执行;时间型兜底(TTL)对空间型风险只是缓冲不是防线 |
| v8.319 | 用户:/tmp/teamwork 能放 worktree 下随它一起清么(+ 追问走不走脚本 · 耗不耗时) | scratch 与 worktree 是同生命周期的两个东西却住两处 · 需要三条清理通道去弥合错位;且旧根在容器**可写层**(141GB 元凶)· 大树同步 rmtree+全树统计会拖住 push 分钟级 | 根迁入 `<worktree>/.teamwork-scratch/`(gitignore 通配含 trash 残骸)· off 模式回退旧根 · push 即清改 rename+后台真删(毫秒返回)· TTL 只删 scratch 子目录绝不动 worktree 本体 · spec 九处同口径 | 拍板前先实测命门(worktree remove 不被 ignored 拦)· 指纹机制推演过再动;版本标门当场抓了自己新文的手滑 —— 机器门对写门的人同样有效 | **同生命周期的产物应该住同一个生命周期载体里** —— 弥合错位的通道再多,不如让错位不存在;时间型兜底(TTL)与显式清理通道都是「住错地方」的补偿机制,搬对家后它们全部降级为残骸兜底 |
| v8.320 | 7-29 用户拍板「browser e2e 关键路径必留可重放脚本 · 优先 Playwright」后对话转向未落地;今日用户重提「用什么有规范么」 | **拍板与落地之间没有账本**:拍板只活在聊天记录里,对话一转向就蒸发(规则存在≠执行的前置版:连存在都没存在过);且原产物契约只收截图 —— 截图一次性,工具名约束不了可重放性(手点 MCP 也算「用了 Playwright」) | stage 硬规则 6(判据「交付后还需要重跑」→ 脚本进 repo+TC 注册 · L2 · 进 L1 仍走 ci_reason)· 菜单 Playwright 默认首选+既有基建复用 · 报告 frontmatter `replay_entry` 必填槽(一次性填 n/a)· tc 执行方式二分 · brief 同步 · 不设机器门 | 主动认账(用户没追责)· 判据写「重跑需求」不写「用没用某工具」—— 把工具偏好翻译成产物契约才锁得住行为 | **拍板必须当轮开账 —— 对话转向不是豁免,没有载体接住的拍板等于没拍**;工具类约定的实质是产物的可重放性,点名工具锁不住(反例:用 playwright MCP 手点) |
| v8.321 | 用户问「browser_e2e 是 subagent+验证档执行么」 | 白名单立在**派发方**规则(SKILL/agents README),但该 stage 的**执行入口**(stage 硬规则+brief)零承载 —— 对没带全局 context 的执行路径,规则等于不存在;goal/blueprint/review/test 都有档位行,唯独 browser_e2e 空(「模式承诺 × 动作点载体」第三格) | stage 硬规则 7(🎚️ 默认验证档 subagent · 降档唯一路径=派 subagent 传 model · 例外 R5 · 降级 WARN)+ brief 同步 + 测试含 agents/README 单源锚 | 用户一句问句撞出的格子,只补这格;全量对账(fast/auto/yolo × 各 stage 动作点)仍挂账观察 | **全局白名单对执行方是「别处的规则」—— 白名单成员的每个 stage 执行入口都该有一行档位复述(指针式 · 不另立判据),否则派发方合规、执行方照旧** |
| v8.322 | aon-core/supersdk 深挖:引擎静默升 37 版而项目状态零迁移(台账 50-61% 旧列宽 · gitignore 水位 v8.89 · state 无 schema 版本 · 升级提示被跳过) | **升级只换引擎不迁状态,且迁移动作全挂在「用户会去跑 bootstrap」上** —— bootstrap 在积灰项目上 20 天不跑,提示型机制被实证不发生;「旧行是有效前缀」的台账设计在按列索引解析下静默错位 | state.py 入口 best-effort 自愈(台账表头+旧行补宽 · gitignore 重放 · marker 记录 · stderr 汇报)· `_schema_version` 写路径盖戳/读路径拦未来 · 迁移核心单源双通道 | 自愈范围刻意收窄到零风险幂等集,重量级仍归 bootstrap;框架仓自身守卫沿用既有 v8.35 判据 | **迁移要挂在积灰机器自己天天跑的命令上(state.py),不能挂在「希望用户去跑」的命令上(bootstrap)** —— 与 scratch 清理(ship1 push 即清)同一条教训的状态版;schema 载体从立戳那天起才有迁移权威,能早立就早立 |
| v8.323 | aon-core「字段已在手仍要人工 append」+ supersdk 47% 归档无台账行 · timing 归档后才 emit 的时序矛盾 | **工具算好数据再让人誊抄 = 誊抄必漏**:落行动作挂在「AI 记得照抄」上,而照抄环节既无 gate 也无幂等;timing 在归档 commit 后才给,合规写行只能二次提交 | archive 直接拼行+append+纳入归档 commit(机器格自算 · 判断格走 --ledger-* 参数 · 反思必填 gate · 幂等 · 无台账建表 · 旧 schema 先迁)· emit ledger_row 回显 · brief/spec 改口 | 判断格保留给 AI(rounds/external/findings/pauses)不硬造 —— 机器算不出的诚实缺省 `—` 并点名;台账失败不拦归档 | **「emit 给数据 + 指令让照抄」是半成品自动化 —— 数据在手的那一刻就该落盘,誊抄步骤存在一天就漏一天**;把落点挪进同一个 commit 还顺带治了时序矛盾(一个动作点两个病) |
| v8.324 | 两项目 26-28% 轮次纯协调开销 · 归因同质「格式门禁重试/字段名未预读/complete 时才拒没预告」· 单空格缩进换一轮返工 | **门禁与预告是两份手写载体**:门禁在 spec 对象里,预告在手写 brief 里 —— 双载体必漂,漂了的代价是整轮重试;解析器又比它拦的格式更窄(只认两空格),把合法变体当违规 | start brief 自动附 complete 契约块(与门禁同一份 spec 对象渲染 · 反向锁「有门禁必有预告」)· parse_frontmatter 缩进放宽 1-4 空格 | 5+ 空格不装懂(行式解析的诚实边界);预告块自动生成而非要求人补写 brief —— 又一例「载体的形状决定内容会不会出现」 | **complete 会拒的必须 start 可见,且预告必须与门禁同源自动生成 —— 双手写载体必漂**;格式门禁的解析器要比它拦的格式宽一档,否则拦的是缩进不是缺陷 |
| v8.325 | aon `.worktree/` 23G(13/14 已 merge 未清 · 31 天)· supersdk 僵尸壳被工具扫成双份 · `worktree_cleanup` 配置无任何消费者 | **回收挂在永不运行的阶段 + 配置键立了没接线**:ship2 是设计上的清理时点但物理上不被跑到;「ask」模式没有任何代码去问,配置成了安慰剂 | bootstrap 每 session 巡检:僵尸壳全清 · merged+干净按 worktree_cleanup 处置(auto 新默认/ask 报告/keep 计数)· 未 merge 与真残留永不动;构建世界五面纪律收编 conventions | 删除条件三重保守(merged + 干净 + 接力卡豁免逐文件核)· 存量显式 ask 不覆盖;untracked 目录折叠差点让豁免失效(-uall 修) | **配置键落地那天就必须有消费者 —— 无消费者的开关比没有开关更糟(用户以为自己选过了)**;回收动作的挂点判据依旧是「积灰机器自己会跑到的命令」,这是同一条教训第三次生效(scratch/迁移/worktree) |
| v8.326 | supersdk 双路冷审同为 opus-5 · 补 fable5 盲审当场查出 2 BLOCKER | **「错开」规则无比对载体**:外审 review_model 申报了没人比,主审路连申报槽都没有 —— 独立采样不变式的冗余度悄悄归 1,且没有任何信号(两路都绿) | 主审产物 +review_models 申报槽(列表形态适配行式解析)· 三 stage 注册 staggered evidence(≥2 申报全同 → 拒 · 存量 skip 带教学 hint · ultra-ingest 除外)· 例外走既有 bypass 协议 | 申报格式选列表不选嵌套 map(行式解析会把嵌套键拍平成顶层键 —— 载体迁就解析器而不是反过来);存量兼容用 skip+hint 而非硬拒 | **冗余机制的失效是静默的 —— 两路同模型时一切都绿,必须有机器比对让「冗余度=1」变成红灯**;申报槽位先行(v8.302 载体形状),比对门随后才有东西可比 |
| v8.327 | aon include_str! 引用 feature fixture · 归档删目录后 cargo 红 11 天;supersdk path mapper 造幽灵 SDK/docs/retros | **删除动作不问「还有谁引用」**:archive 的设计前提是「过程层无外部依赖」,但没有任何校验去证实它;path mapper 拼路径不核物理存在 —— 约定推导的路径落到不存在的目录时,工具选择创造而不是回退 | archive 前 git grep 反向引用扫描(命中 PENDING 列清单 · 例外 --archive-ref-exception 审计留痕)· retro path 拼前核目录存在,缺失回退根级 | zip 字符串形态显式排除(归档后合法引用形态不误伤)· grep 只扫 tracked(scratch 不背锅) | **凡「按设计删除/移动」的动作,前提假设(无外部引用 · 目标目录存在)必须机器证实而不是默认成立** —— 假设不成立时的代价由别人(11 天红灯)在别处(编译系统)付,动作方自己看不到 |
| v8.328 | 用户拍板:review 优先功能实现 · 兜底/测试门类建议收敛 · 不为不重要 bug 加复杂度 · 真缺陷必报 · PRD/TECH 同理 | 评审判据只有 severity 定级(真不真)没有**价值观维度**(值不值)—— 加固类建议全部合法准入,靠裁决轮逐条消化,轮次预算被低价值 finding 吃掉(实证 26-28% 协调开销 · 钟摆判例是它的极端形态) | review 规则 2.5 单源(真缺陷零门槛 · 加固须真实触发路径 · 修复代价>危害则 REJECT 合法 · 不借评审加门 · 简化不设门槛)+ goal/blueprint 指针 + reviewer prompt 自含过滤 | 准入收紧只对「往上加」方向 —— 简化方向显式豁免,防拍板被误读成「都别提」;confirmed bug 回归锁显式例外,防误伤既有纪律 | **准入判据要有两个轴:真不真(severity)与值不值(复杂度守恒)—— 只有前者时,评审会用全真的低价值建议合法地拖垮交付**;价值观类规则必须进 reviewer 的 prompt 正文(它只读 prompt) |
| v8.329 | 删 3 按钮定价成九段全链 · 用户四连拍板把装配决策点从 prepare 搬到 goal 调研后 | **定价发生在信息最少的时刻**:prepare 只有需求文本,Q1-Q4 是拍脑袋装配;而机器层(needs-ui/browser-e2e 在 goal-complete · roster 随时可调)其实一直支持 goal 出口装配 —— spec 在替机器提前抢答 | prepare 收缩为意图对齐+大类+micro 速通+机械配置;goal 3.7 链装配单源(四轴证据必填 · 两拍:自身面 AI 自定 · 下游随 PRD 导读默认执行不阻塞);Q1-Q4 迁入升级证据版 | 逐层追问把设计收敛到位(先问目标→再问哪步不准→再盘装配力度→再定交互形态);诚实交代了地板(文档重量不塌缩 · 九段→约六段)避免期望落空 | **决策点要放在证据出现之后,不是流程入口**(Feature 与 Bug 统一「先调研再定价」);spec 与机器形状打架时,先怀疑 spec 在抢答 —— 机器参数的位置常常就是正确的决策时点 |
| v8.330 | 用户拍板:降低对模型的限制 · 不强制 TDD · 兜底规范+自查表即可 · 必读项目开发与架构规范 | 盘点后发现拍板大半已实现(TDD 早撤 · HARD-RULES 已是兜底白名单)—— 真缺口是**兜底规则散在正文没有收口自查载体**、ARCHITECTURE 只「按需」、「方法自由」声明分散无总纲 | HARD-RULES +收口自查表(槽位式 · 判断题不设门 · 用户两例入表)· 读取契约升级(ARCHITECTURE 必读 · 并集 · 项目优先)· 「方法论不设限」总纲(只收三样)· 完工自查双源 | 先盘点再动刀 —— 避免把已完成的事再做一遍;自查表压进必读文件而非另立新文件(读取面不增) | **「降低限制」的正确形态 = 收窄到三样可点名的不可让渡(读什么 · 兜底自查 · 结果证据),其余显式归还自由** —— 隐式自由等于没有(模型会按最保守解释行事);自查载体是槽位表,不是散在正文里的规则 |
| v8.331 | 用户三连拍板:standards 收敛为 tech-rules 单文件(整合 HARD-RULES)· 三时点必读 · 项目规范同读且冲突项目优先 | **按技术栈分册是历史遗留**:约束经几轮减法后,backend 的主要动词已是「评审必查」—— 名为开发规范实为评审要点;「必读短文件+分册按需查」制造二跳与五条指针,读取面反而更碎 | 新 tech-rules 五节(逆默认/约定/方案与架构门/前端/自查表)+ 头部三句项目规范契约;内容按消费时点归位(scratch/起号→conventions · 脚本契约→scripts-policy · Designer 自查→ui-design-stage);三文件退役 764→411 行;三时点全载体接线 + 全库悬空引用机器锁 | 合并前逐条款核实质(v8308 锁抓出 6 处压缩措辞差 · 全数按原文回补)—— 搬家的验收标准是零丢失不是行数;历史版本锁不删除,按新载体重锁(迁移史保留在 docstring) | **文档切分轴要跟消费时点走,不跟技术栈走** —— 栈分册在约束变薄后必然名实分离;「起草读的就是评审查的」让 shift-left 从指针跳转变成同一页纸,这是合并真正的收益(不只是文件数变少) |
| v8.332 | 用户拍板:主对话不建议直接开发/测试 · 优先当 Orchestrator(拆解/调度/接线/推送/门禁/小修) | 档位体系只管了「用什么模型」没管「谁持有 context」—— 验证档白名单推走了测试执行,dev 侧档位表却写「主对话继承会话模型即是」默认背书主对话写码;主对话 context(编排状态/拍板记忆)被实现细节与测试日志挤占 | dev/test 各立 1.7 姿态规则(拍板职责清单逐项入规 · 小型精准修改显式出口)· 档位表执行档行位置/档位解耦 · 三 brief 动作点同步 · 不设机器门 | 出口判据沿用既有「边界清晰且够大才派」不另立;与验证档白名单衔接而非重复(执行已是硬约束 · 本版补位置总纲) | **位置与档位是两根轴:降档决定谁便宜,派发决定谁记得住** —— 主对话的 context 预算应花在编排记忆上,成块产出的细节留在 subagent 的一次性 context 里 |
| v8.333 | 截图:PRD 大白话列被 BDD 挤成竖条 · 用户拍板放第二列宽一点 | 列序把**给用户读的列**排在**给机器读的列**之后 —— markdown 渲染宽度由内容与位置驱动,长 BDD 天然吞掉右侧列宽;载体形状又一次决定内容可读性 | 模板列序重排(大白话第二)+ 「写一句完整的话」指引;机器门 v8.271 当年按表头名定位 · 天然列序无关 → 零机器改动零存量迁移 | 顺手核实机器面(evidence 按名不按序)再动模板 —— 重排才敢零迁移 | **表格列序即阅读优先级:人读列在前 · 机器列在后**;当年把解析写成按表头名而非按列号,今天的重排才免费 —— 解析器的宽容是给未来载体调整留的门 |
| v8.334 | supersdk Analytics case:1 文件 + 2 命令的「调研」→ 174 行 PRD · 冷审默认双路零判断输出 · 用户问调研深度够么并拍板「深调研→评审深度判断→回显不阻塞」 | 「按需选查」是零判据授权 —— 浅调研完全合规;评审面自定(v8.329)只留审计不回显,用户看不见「为什么是这两路」,深浅无从校准 | 深入调研四面必过(显式「不涉」出口 · grep 实测 · 深度判据「能答四轴+能写最可能错在哪」)· 评审深度判断卡(纪要+roster+额外评审+逐项理由 · 派发前回显 · 不停等)· brief 同步 | 判据写成可判定问句而非形容词(「深入」不可判 ·「能写出最可能错在哪」可判);回显与阻塞解耦沿用装配后移同律 | **授权浅执行的从来不是 AI 偷懒,是 spec 里的「按需/酌情」—— 把形容词判据换成可判定问句,浅就无处藏身**;透明(回显)与主权(阻塞)是两件事,回显尽量给、阻塞尽量省 |
| v8.335 | 用户拍板:初始化把 teamwork-space 引入 CLAUDE.md/AGENTS.md · 让不用 teamwork 的 agent 也能充分了解项目 | 知识地图只有装了 skill 的 session 会读 —— 项目知识的发现性被绑在框架安装上;而 v8.211 退役注入时把「流程指令」与「知识入口」一起退了,后者其实正是非 teamwork 用户需要的 | bootstrap 注入 managed 块(只含知识地图指针 · 顶部 · 幂等 · 版本 marker 原位升级 · 新旧 marker 不同族防误删)· 测试硬锁「块内零流程指令」 | 与 v8.211 的关系想清楚再动:受众相反 + 内容边界收紧 = 不是推翻是纠偏;零流程指令用禁词测试锁死(不靠自觉) | **退役一个机制时要拆开「它伤人的部分」和「它有用的部分」—— v8.211 连浴盆带孩子一起倒了,知识入口本该留下**;为反受众服务的注入,内容边界必须机器锁(禁词清单),否则流程指令会一点点渗回来 |
| v8.336 | 用户:design 是基于现有全景改造么?是的话去掉全景同步流程 | **stage 的存在理由已被自己的演进掏空**:新模式让设计=直接改全景后,「同步」只剩登记+判级,却仍占一个 stage + 一份独立产物 + 一个必传参数;更有 mtime 门逼二次 touch、同文件新旧规则互撞 —— 载体没跟上模型换代 | stage 全线退役(spec/转移/参数/文件/表行)· L1/L2 判级并入 ui_design 出口(L2 搭既有用户停等 · 零新增)· UI.md §全景变更判级替代独立 summary · 存量枚举兼容 + jump 出口 | 前提先验证再动刀(三条机器实证 + 消费数据 11:0)· 退役锁锁「活引用」不锁字面(迁移史标注合法)· 数字门当场抓 README 双语宣称 | **模型换代后要回头杀它掏空的 stage —— 判据:该 stage 的核心动词是否已被上游吸收**(「同步」被「直接改」吸收后,剩下的判级是条件暂停不是阶段);条件暂停的正确宿主是既有停等,不是新 stage |
| v8.337 | supersdk 装配卡实际输出只剩「环节+影响面」· 评审力度整维丢失 · 用户点名预期并锚定「目的是降低流程税」 | 装配卡要求是形容词式(「环节取舍+评审面」)—— v8.302 老病第 N 次复发:形容词糊得过;且丢的恰是**减税的验收面**(评审减没减用户看不见) | 固定三槽(流程阶段全链标进跳 · 评审力度逐 stage 是否×几路×谁×理由〔零也显式〕· 四轴证据)· 整卡 ≤6 行每槽一行理由一句 · 导读与 brief 同步 | 用户「降低流程税」的提醒直接进了设计:零路显式=减税减在明处;≤6 行上限防槽位自己变税 | **减税与可见是一对:砍掉的评审必须显式写「0 路+理由」—— 静默的减税与静默的漏配在产物上无法区分**;槽位模板自身要带体积上限,否则治糊的药变成新的税 |
| v8.338 | 用户拍板:PRD/Planning 的 1/2/3 停等第 2 项永远=继续讨论(把目标和方向聊清楚) | 旧选项 2 各处各样(修订重审/要改全景/合入+启动 BL)—— 全都**假设用户已经想清楚要改什么**,没有「还没想清楚 · 先聊」的一等公民入口,用户被逼在确认与给修改点间二选一 | R5(b) 恒定规则 + 四处停等落位(收尾选项顺延 · 硬门改号);红密度门撞线自降红(⏸️ 标记) | 讨论入口统一放第 2 位 = 肌肉记忆(任何方向停等回 2 就是聊);既有「就地讨论收敛不落成品后返工」原则借此从拆解一处推广到全部方向停等 | **选项设计要给「还没想清楚」留一等公民位置 —— 方向类决策的最大风险不是选错,是被迫在没想清楚时选**;恒定槽位(第 2 位)比每处自拟更值钱:用户不用读选项就知道怎么进入讨论 |
| v8.339 | supersdk:PR 上 3 个已知 blocker 被判「必须开 Bug 流再合回」· 用户纠偏同 feature 回 dev 修 | 「Ship 后不可回」把**远程已动**(不可逆的是历史)误推成**流程不可续**(MR 窗口期本来就是反馈循环)—— 守卫没区分「改历史」与「追加修复」,把 PR 反馈逼成新立项全套税 | jump-to-stage pushed 态唯一放行口(--to dev + --reason · reopened_fixes+WARN 双留痕 · phase 不变)· ship-stage 五步节 · reset-prev hint 三分(未合并/已合并/放弃) | 放行口收到最窄(仅 dev · 必带 reason · 其余照拒);复用 push rerecord 与 archive 幂等,零新命令 | **不可逆守卫要区分「改写历史」与「追加修复」—— 前者才不可逆,后者是交付闭环的一部分**;边界用平台事实划(MR 合没合),不用状态机阶段划 |
| v8.340 | 用户:ship1 之后需要自动检查 CI 的 pipeline | await-merge 立项时就点名「CI 红无人接」是原始痛点,落地却只轮询合并态 —— **痛点写进了 docstring,动作没写进循环**;CI 红时框架傻等合并,用户在平台先看到红灯 | push 后自动查一次 CI(emit ci_status · failing 带修复口)· await-merge 每轮带 CI · 未合并且红 → CI_FAILING 立即退出接 v8.339 修复口 · 解析纯函数化单测锁 | unknown 态不拦流程(CLI 缺失/未登录 = 环境事实);红灯检查显式置于 MERGED 判定前并测试锁序 | **docstring 里的痛点叙述要当 backlog 扫 —— 「为什么建这个机制」与「机制实际做什么」的差值就是欠账**;监控循环每轮采的信号面应等于它宣称守护的面 |
| v8.341 | 域名配置四轴全低仍吃满六路评审 · 用户问「这么简单为什么那么多 review」并给目标形态「直接开发+架构师 review+PM 盯 staging」· 消费 AI 中途切 micro 自救 | 装配判断表只有加法触发没有减法判据 —— AI 有权减(0 路已合法)但无判据可引,保守偏置默认全保;「简单不能当跳过理由」这句规则反过来成了不减的挡箭牌 | 减法侧两级分级缺省(超低→micro+轻门 · 低→单路合并/0 路/architect 单路)· 一致性倒逼(路数与四轴对不上必须写为什么不降)· insist-micro 正名 | 用户目标形态原句入规(超低档定义);消费 AI 的自救路径不当违规处理而是转正 | **权限不等于行为:给了「可以减」没给「何时减」,保守偏置会让权限休眠 —— 减法要有分级缺省表才会真发生**;用户在消费现场的抱怨句是最好的判据来源(原句入规) |
| v8.342 | 四档流程回归:tiny 立档 · lite 由 full 装配 | 「micro + 附加轻门」= 形容词式承诺,没有载体就不会发生 —— 想要的形态要给正式档位 | **档数 ≠ 配置项数**:preset 只给「不立就走不通链」的档,差一条边的用装配旋钮 —— 多一张转移图 = 多一处要同步的口径(legacy lite 三实现两张图的复发根因) | 降档四问:①链走得通吗(会死锁的门要显式放行)②用户主权动了吗(lite 保 PRD + 终确认)③独立性动了吗(单路仍错开模型)④原来的门换载体了还是撤了(test_refs 顶替 TC · 且校验引用真实存在) |
| v8.343 | 三维度拆开交 AI 组装 + 最小力度 dev+ship + 渐进式流程 + medium 档 + custom 装配 | 用户提「给 AI 强烈的提示有权利精简、不能过度保守」—— 但台账里连着四版写的是同一条:形容词/劝导不产生行为,缺省值与可判问句才产生(v8.341 已实测:0 路早合法、照样吃满六路) | **把偏置写进缺省值,把判据写成可判问句**:六档默认元组承载轻偏置 · 每档一句入场问句(判风险的种类不判改动大小)· 修订点问「有没有出现装配时不知道的事实」而非「要不要改计划」 · **链由维度推导**(matrix 是单源,静态图降为回退并被 ⊆ 锁住)—— 加一档只加一行,medium 实现中途落地即验证 | 渐进式只取「计划 + 显式修订点」不取「每步再定」:一次给全保住整体形状与抗棘轮,边界修订接住后到的证据;加与减同价(只让减举证 = 保守偏置搬回来);守卫顺序先判不可回溯再判连贯性 | **偏置的住处决定行为**:写在措辞里 = 休眠,写在缺省值里 = 生效;**「拆维度」必须同时给三样** —— 连贯性校验(拆开必然产生不连贯组合)、命名默认元组(一个词给一套连贯默认)、可判入场问句(否则只是把选择困难交给下游) |
| v8.344 | Grok 宿主:子代理把「测试写哪个文件」ask_user_question 弹到用户屏幕 · 用户拍板问题回路收口主对话 | 暂停点纪律只管主对话 · 子代理侧无对着工具名的红线 · 派发 prompt 也没带 —— 对没带全量 context 的执行路径,别处的规则等于不存在 | 红线对着**工具名**立(行为式表述糊得过 · 工具名糊不过)+ 派发 prompt 必带禁问句(与 Meta 首行同寄生)+ 主对话二分显式(实现细节自答重派 · 用户主权才 R5) | 密度门顶满按判例换 ❌ 不顶爆;既有 NEEDS_CONTEXT 回路零改动 —— 补的是通往它的独木桥,不是回路本身 | **跨宿主消费的框架,约束要写到「任何宿主变体」级别** —— 工具名会变,「向用户提问」这类能力不会;二分要两头都写死,否则堵住子代理、主对话原样转抛 = 问题换个出口 |
| v8.345 | 用户:ship1 后监控合并的同时查 pipeline · 自己引入的直接修 | v8.340 查了 CI 但**不归因** —— 任何红都退出去修,base 本来就红时把 AI 支去修它没弄坏的东西;且默认动作停在「给修复口」,把「要不要修」又留给了人 | 归因层(对照 base 分支同名 check)+ 归因决定动作:自己引入→中断直接修(不问用户)· base 预存在→不中断继续等 · 查不到 base 归到自己(保守偏置 · 代价不对称) | 形状直接复用 dev/test 的差分基线(不重新发明)· 绿/pending 不查 base · 「修不动/根因在别处」保留升级口,不给无限重试留口子 | **「检查了」不等于「能动作」** —— 检测加上去之后要接着问:检出来的东西**归谁**、**默认动作是什么**;凡「红/失败」类信号,归因维度(自己引入 vs 预存在)是动作可判的前提,否则信号只会变成又一个需要人来分诊的中断 |
| v8.346 | 年检(aon-core/supersdk/aib · 289 行台账):三条 P0 | ①降档时砍的是**产出最高**的一路(ext 逐 stage 都 > arch · 2.1× · 采纳 82%)——v8.341-343 那句「异质冷审边际收益压不过协调开销」是推的,没数据;②worktree 巡检挂在 bootstrap(v8.322 刚证明它不跑)· aon-core 积到 18G;③复发防御清单只接了读取端 · 可预防率 70.5% 而清单 0 条 | 单路默认改 external(路数不变 · 换留哪一路)· 巡检改挂 main-sync(积压发生的那一刻)· preventability 出现成骨架 + archive 加 defense-list 验收门 | P0-2 初诊断错了(以为是默认漂移)· 查 git 发现 v8.325 有意不覆盖 → **回滚重做**,不将错就错;判据把实测数字写进 spec,否则下一版又凭直觉改回去 | **框架自己的设计判断要定期拿消费数据复核 —— 推理出来的默认值会稳定地错在同一个方向**(直觉偏好「看得见的角色」architect,数据偏好「独立采样」external);凡「读取端已接线」的载体都要反问一句**谁来写它** —— 只读不写的载体永远是空的 |
| v8.347 | 用户看 case 问「监控为什么自动退出了」(aon-core SVC-CORE-B260831064524) | await-merge 默认 18×30s=9min 用尽即 WAITING+exit —— 但**等的是人去点合并**(原始痛点 132h 长尾),窗口差三个数量级;且 spec 明确让它 `nohup` 后台启动,而「AI 应自动重跑」在后台没有载体接住 | 默认窗口 18→120(≈1h)· 加 --until-final 自己循环到终态 · 非 tty 未开该 flag 时 WAITING 说破「我不会自己续等」并给出口 · spec/push emit/用户卡片三处载体同步带上 flag | 自续等仍保三个终态出口(不变成永不退出)· 前台跑不噪(警告只对真出问题的姿态发) | **载体缺口可以是「运行姿态」造成的** —— 同一句承诺在前台成立、在后台不成立;凡 spec 让某命令后台跑的,都要反问一句**它的输出谁读** —— 没人读的 emit 等于没 emit;等待窗的长度要按**等的对象**定(等 CI 用分钟级 · 等人用小时级),不按实现方便定 |
| v8.348 | 用户:测试流程要不要约束到「覆盖 CI 会查的项」(case:clippy 漏到 CI 才炸) | test stage **零处**提到 CI —— TEST-REPORT 记的是 AI 自选的命令,与 MR CI 命令集合无任何比对面;且实证里 AI **试过**找 CI 配置,只是猜错路径(真配置 include 在 infra/ci/*),空结果被当成没有 CI | `ci-commands` 扫出门禁类命令(文件:行)· test-stage 2.9 + TEST-REPORT §2.5 三态标注 · brief 在消费时点带上 · 与 ship 侧 CI 归因写明防/治分工 | **刻意不要求跑全集**(要 infra/太慢的强行复现是纯税)· 只要求「看过 + 每条给处置」· 跑不了的显式列出 = 已知风险清单 | **「AI 没做 X」先分清是不为还是不能** —— 这次是**不知道去哪找**,那就该机器端清单而不是加纪律;凡「本地与远端要同构」的要求,都要先问**同构面在哪儿可枚举**,枚举不出来的同构要求等于形容词 |
| v8.349 | 用户拍板 yolo 两段式(事故:协议强制 header · 线上请求归零) | **AI 识别到了风险、写进了文档,但文档是终点** —— 没有任何通道能把「写下来的风险」变成「必须停的等待」;yolo 零 stop 又把 diagnose 方案确认自动跳过;v8.63 的守卫只挡 main,staging(生产前最后一站)照样能自动合进去 | yolo 目标必须 `yolo/` 前缀(init-feature + set-mode 双入口)· archive 必填 --yolo-risk 记进分支上的 YOLO-PENDING.md(随 commit 原子合入)· yolo-promote 在合进真 target 前一次性摆出全部待确认项(破坏性单独计数)· 可判问句「今天能成功的明天会失败吗」 | promote 不代替用户点合并;台账 why 写在文件头(机理随台账走);既有 9 条 yolo 用例同步更新 | **零 stop ≠ 零确认 —— 无人值守的正确形态是把确认延后并批量化,不是取消确认**;凡「AI 识别到了但没升级」的事故,缺的不是识别力也不是记录,是**升级通道**:写进文档若无下游消费者,文档就是终点 |
| v8.350 | 用户拍板:AC 要确认有无「对原始用户意图理解偏差」的风险(事故:AON Link 被狭义解释) | **范围被悄悄收窄时 PRD 是完全自洽的** —— 冷审只能核对内部一致,拿不到用户原话;于是 TECH 写「static 不登记」、测试断言 static must remain unwired,**dev 与 review 都在认真验证一个错误的范围定义** | PRD §意图对照 三槽(术语解释「用户说过/我推的」· 排除项定性「做不到/我选的边界」· 反向验证「AC 全绿时用户要的事一定发生了吗」)+ goal-complete 机器门 + 终确认导读摆出风险行 | ②并进 Out of Scope 的性质列(不两处写)· 占位检测初版把表格行剔掉导致模板原样能过门 → 修并专锁 · 顶破瘦身门时压缩措辞而非改门限(v8.303 服从门裁) | **有一类偏差只有主对话看得见** —— 凡判据需要「用户原话」的检查,责任人就不能是冷审,再多路冷审也补不上;**自洽 ≠ 正确**:内部一致的产物在范围被收窄时读起来毫无破绽,所以要专设一处把「我的解释」与「用户说过」分开标注 |
| v8.351 | 用户:PM 要知道一旦理解错了,代价非常高 | 「要知道代价高」是**形容词式劝导** —— 本仓 v8.334/337/341/342 连着实证它不产生行为;而代价这件事本身此前只在事故复盘里出现,起草时点零载体 | ①表加末列「若这条错了最坏会怎样」逐行算具体后果(机器门认列)· 后果落生产/外部/不可逆 → 强制进待决策项(与信心无关)· why 改成**结构性事实**:意图错误是唯一一类下游全部质量门都拦不住的错(它们全以「意图正确」为前提)· 越认真做错得越彻底 | 模板零行预算(339/340)→ 代价做成**表的一列**而非第四槽:不加行,且语义更准(代价是针对某条「我推的」解释算的) | **「让人知道代价」的正确形态是让他算一遍,不是告诉他代价高** —— 写不出具体后果 = 没想过;凡想让 AI「重视」某件事,把「重视」翻译成一个**它必须填的格子**,否则重视只发生在措辞里 |
