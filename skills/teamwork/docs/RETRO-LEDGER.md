# RETRO-LEDGER(teamwork 框架自省台账)

> **telos**:框架自身的「问题 / 优化点 / 有用的地方 / 反思」**跨项目汇总一处** —— consuming 项目目录只放项目的事(`project-specs/PROCESS-LEDGER.md` 度量流程环节价值);框架自己的演化反思归框架仓。「**有用的地方**」是 CHANGELOG 没有的正反馈维度。
> **三层分工**:`CHANGELOG.md` = 单版变更细节(keep-5 · 归档定期清空 · **易逝**)· `PROCESS-LEDGER.md` = 项目侧流程环节价值(一行一 feature)· 本表 = 框架侧自省蒸馏(一行一版 · **永久**)。
> **写入时机**:每次流程完善发版(minor bump)与 CHANGELOG entry **同 commit** —— 发版三件套:CHANGELOG entry + 本表 1 行 + 版本 bump。🔴 单元格 ≤1 行 · 不复述 CHANGELOG(细节在那边 / git)。
> **消费方**:① 年检 /「teamwork 是否该存在」类审视(直接读表算账)② 任何框架改进 session 动手前读最近 10 行(防重复踩坑 / 重复讨论)。
> **膨胀控制**:超 ~500 行 → 年检时把老行主题化压缩(原文 git 可溯)。

---

| 版本 | 来源 case | 问题 | 优化点 | 有用的地方 | 反思 |
|---|---|---|---|---|---|
| v8.120 | PTR Assets prepare 总览(aon) | 流程概览只有「怎么流转」· 缺「要达成什么」 | 总览加「流程目标」首行(写要什么不写怎么做) | 暂停点的固定结构让缺口能被用户一眼指出 | review 第一校准点应是目标理解 · 不是流程形态 |
| v8.121 | 用户:全量 review 所有 md | 数字宣称/断链/旧名/脏标题 16 实锤(版本滞后 33 版 · stage 数三口径) | P0 批次 9 项口径对齐 + 断链清零 | 6 路并行 review + 主对话逐条复核挡掉 3 条误报 | 数字写死多处又无校验 → 演进必漂 |
| v8.122 | review P1 | frontend 1684 行教程混规范 + TDD 未注册 fork + 历史格式事故腐坏 | 瘦身 166 行 + spec 宣称 pytest 物化(4 测) | 负验证(regex 实测命中再信)防校验空匹配白过 | 文档体量 ≈ 物化欠账的度量 |
| v8.123 | 用户:guide 是否有必要(委托裁定) | 1554 行通用教程无归属层 · 维护负债 | 删除 + 「实施教程不入库」哲学行防回潮 | 三层律给了现成裁决依据(通用知识归模型) | 保守外迁回避不了「该不该存在」· 删除可逆(git) |
| v8.124 | 用户:后端规范也看下 | backend TDD fork + Date.now 腐坏 ×3 | 收敛 tdd 单源 + 全仓腐坏归零 | 同病例复扫暴露上次扫描正则的盲区 | 体检结论因文件而异:backend 主体是真规范 · 不照搬 frontend 砍法 |
| v8.125 | 用户:API 规范是否 DEV-RULES 优先 | §三写成绝对条款违用户主权 · 覆盖注册处两套口径(KNOWLEDGE 错档) | DEV-RULES 优先级链 + STANDARDS 全局优先级成文 | dev-rules 模板自带的边界表直接提供裁决依据 | standards 是「未规定时的缺省」· 不是凌驾项目的法律 |
| v8.126 | 用户:去 KNOWLEDGE 兼容层 | 兼容层让注册处仍是两个 | 收口单源两级链(有则为准 · 无则默认) | 用户对单源的坚持比兼容方案更简单正确 | 兼容层是迁移成本不是终态 · 能一步到位就别留尾巴 |
| v8.127 | 用户:体量分析后拍板删历史 | 冻结历史热存 16.3k 行占仓库 61% · 违自家三层律 | 删 docs/archive + 归档定期清空制(−55%) | git 历史天然就是冷库 · 删除完全可逆 | 框架要先对自己执行自己的律法 |
| v8.128 | aon「ci 编译失败」case | 现象类输入被措辞路由直接定 Bug · 未验证猜测进 prepare 总览押上命名/路由 | 排查先行律(按事实已知度定路由 · 不按用户用词) | 用户中途质疑「你排查到原因了么」= 暂停点人工监督在工作 | 判别题是「定流程所需事实是否已知」 |
| v8.129 | 用户:是否加流程价值反思 | 框架只有负反馈(出事加规则)· 仪式价值靠信念 | PROCESS-LEDGER + digest(零暂停 · 机器字段 · 指名消费方) | 「是否该存在」的存在性拷问直接催生了度量机制 | 反思流程要先防自己变成新仪式 |
| v8.130 | 用户:归档压缩后还能取数么 | 台账采集在归档删目录之后 = 取数断粮 + 写入无 MR 载体 | 两段式:planning-backref 采写(文件尚在)· finalize 后 emit | 收尾 MR 是现成合入载体 · 读码核证 --planning-artifacts 兼容 | 机制设计要沿数据生命周期走一遍 |
| v8.131 | 用户:框架反思归框架仓 | 框架自省散落 chat/CHANGELOG(易逝)· 正反馈无归档处 · 项目目录不宜放框架事 | 本表(发版三件套 · 一行一版 · 永久)+ 回填 11 行种子 | 项目侧/框架侧台账成对 · 反馈闭环补全 | 「有用的地方」也需要账本 · 否则年检只剩坏消息 |
| v8.132 | 用户三连问:goal 合理性 → 上抛风险 → 对抗性 | goal 五层皆有缺口:输入薄/无人杀前提/全员通过纸面/模板三方矛盾 | 调研前置+PL 质疑五问(subagent 隔离)+三闸早问门+verdicts·CHALLENGE 物化(+9 测) | 用户连环追问本身就是对抗评审 · 三问各堵一个我初版设计的洞 | 防上抛的不是禁令是入场券(调研穷尽)· 对抗的关键是不喂心路 |
| v8.133 | jdp console case:设计页顶在 / · 栈版本漂移 | spec 亲手教给根 URL ·「同栈」未定义镜像范围 · AI「有意省略 router」照章办事 | IA 镜像律 + 数据层唯一差异律 + route_path 直达预览(五文件联动) | 用户两轮反馈精确指出 spec 的两个缺口(镜像范围 · IA 结构) | 「全景」的定义要同时覆盖空间结构(路由)和物质构成(栈)· 缺一半就只是页面集 |
| v8.134 | 用户四轮推演:镜像最合理么→mock 态全景→规划层需终稿→分层同构+编译契约 | 全量镜像=双源死结 · mock 态撞规划期/全貌需求 · dev-stage 还残留已废 in-app 路由模型 | 分层同构律(L1 基建共享包完全一致 · L2 页面意图四要素 · 权威时效)+ 共享基建改动须验全景编译 | 用户逐轮否证把方案从两个极端逼到正确中间形态 · 共享包让「一致」从纪律变结构 | 同构承诺要按「能否由结构保证」分层 · 全镜像与废镜像都是错的 |
| v8.135 | codex-cli case:spawn_agent 契约要 「explicitly asks」 卡 teamwork 流 | 宿主工具契约与流程预设的 subagent 用法有缝隙 · 授权无长期载体 | 注入块加「Subagent 默认授权」段(三宿主通用 · 约束仅指针防双源)+ R4 挂授权指针 | 注入块正是宿主契约的读取处 · 一处声明三宿主生效 | 授权管「能不能」· 判据管「该不该」—— 两者分开才不会把授权读成滥用许可 |
| v8.136 | PTR case:claude -p 三轮两坑(stale review · 双嵌 PRD) | 审计副本固定名被下轮当输入(缓存无失效)· 模板整文件替换把 PRD 灌进占位符示例表 | 每轮唯一 doc 名(审计=输入)+ Prompt 主体提取 + 显式 doc staleness 门禁(改 2 增 3 测) | 用户方案(唯一命名保 doc 优先)优于我的废 doc 方案 —— 病根是隐式复用不是 doc 本身 | 审计产物与缓存必须分离 · 或者像这样合一但身份唯一 |
| v8.137 | 用户:确认前输出值得关注的点 | 出口无导读 · AI 替用户做的判断(REJECT/DEFER)不可见 | substep 9 六节照实抄导读 ·「替你做的判断」第一节 · 空节写无 | 入口流程目标(v8.120)与出口导读成对 · 暂停点两端都有校准面 | 导读的天敌是营销式总结 · 防法 = 每节必有产物出处 |
| v8.138 | PTR case:sitemap 仅描述行更新仍停 reviewer 暂停点 | 暂停粒度(语义级)与触发粒度(文件级)错配 · 推荐理由自证无风险还停 = 纯过场 | 变更判级:L1 三判据全过不暂停(WARN 留痕 + digest)· L2 结构变更才停 · change_level 物化 | LEDGER「暂停点 改:默」kill criteria 第一个判例先到 · 事实已知度判别三连复用(v8.128/132/138) | 暂停点价值在「用户真要决策」· 事实已查明的确认 = 把审计成本转嫁给用户 |
| v8.139 | 用户:外部评审发起后完全黑盒 · 提议同名 log + 模型自报首行 | v8.55 日志跑完才写/藏 ~/.teamwork 不配对 · 运行窗口零痕迹 · stderr 死因等验尸 | 流式执行器:START 行 harness 写 · 实时落 prompt-doc 同名 .log · 超时保留部分输出 · codex 补审计 doc | 用户方案对一半(配对对 · 自报错)—— print 模式输出整体到达 · 模型首行物理不可能先到 | 时间戳/状态行可枚举归脚本 · 评审内容不可枚举归模型 —— 可观测性是 harness 的职责不是被观测者的 |
| v8.140 | 用户(承 v8.139):prompt 注入让模型开机写 .log 声明开始+时间戳 | 模型零工具写不了文件 · 「开始时」恰是模型没起来的窗口 · 直注入必落空 | 拆两层:RUNNING 心跳(harness · 活性)+ 首行 REVIEW-ACK 回显(模型 · 对应性 · WARN 不 BLOCK) | 用户的执念点出真盲区:pid 到 END 之间心跳缺失;ACK 把防 stale 补到输出侧 | 用户提案不可行时别只说不行 —— 拆出背后两个诉求各给可行载体 |
| v8.141 | 用户:claude -p 支持 --allowedTools 么 · 重点解决 MCP 卡死 | v8.106 归因错了:裸 -p 也每轮 spawn 项目 MCP(marker 实测)· 现行一直在拉火药库只是新 CLI 不阻塞 | --strict-mcp-config 固定进 argv(零 spawn · auth 完好)· 测试 pin 缺一不可 | 四组对照 + marker 文件法把「相关」和「因果」拆开了 · C4 还白捡一个解锁选项 | 修历史结论先复现历史实验 —— 环境(CLI 版本)变了 · 结论的边界条件要重标 |
| v8.142 | 用户:更新提示要带更新描述 | 升级暂停点只有版本号 · 判断「升不升」要出门翻 GitHub | outdated 拉线上 CHANGELOG 标题行进 prompt(cap 8 · 断档加 git 历史注 · 失败降级指针) | 发版纪律的标题行=现成蒸馏摘要 · 零新增维护成本就喂给了暂停点 | 暂停点的决策参考应随暂停点到场 · 不应让用户出门取 |
| v8.143 | 用户:rsync 去掉 · 本地项目走 dev 升级 · 你只管提交到 dev | 发版 rsync 是无成文的 session 习惯 · 本机项目被静默推到未发布版本(无提示/无确认/无 backup) | 交付止于 push dev 成文进 CHANGELOG 头部 · 本机项目走 localconfig update_channel=dev 标准链路 | 用户从「静默最新」一个观察反推出整条交付边界问题 | 例程里没成文的步骤 = 下个 session 的隐性行为 —— 砍掉它也要成文 · 否则惯性会把它带回来 |
| v8.144 | aon 收尾 transcript:终态对但 AI 做了 20 条 git 手术 | pop 结果无视(改动埋 stash 被手工重写成双份)· pull 失败一律喊分叉(实际仅落后一条 pull 即愈)· stash 跨 feature 堆积 | pop 必查 + rev-list 对症判别 + 残留清除即补 pull + stash 盘点透出 | 沙箱实测推翻两个想当然:staged 删除不阻塞 ff-pull ·「下次自愈」的下次不该留给人 | 错误文案是更深的债 —— 工具喊错病名 · AI 就开错刀;终态残留 = 下一轮的事故输入 |
| v8.145 | 用户:重新思考 ship · 终点=提 MR · 收尾不属于本次 ship | 12 个版本修同一条尾巴 = 架构信号:在不可控主工作区做内容性工作 | ship1 全交付(worktree 内 · 单 MR 原子)/ ship2 零内容清场 · 砍双 MR 链路 −979 行 | 用户一句原则(内容/清理分离)解开十二版补丁没解开的结 · diff 还更干净了 | 反复修补不收敛 = 该换位置不是该再修 —— case 火山的根因常是职责放错了环境 |
| v8.146 | 用户(承 v8.145):MR 创建后大概率冲突 · 要不要自动检测处理 | v8.145 把共享追加文件移进 feature 分支 · 冲突从「可能」升「并行必然」(设计时低估) | archive 内置 sync:behind 自动 merge · INDEX 机械自动解(origin 为基+重放己行)· 代码冲突 PENDING 留 AI | 重跑 archive 顺势成为冲突修复入口 —— 幂等命令天然是修复入口 | 大重构的 trade-off 标注要按「并行场景」估频率 · 用户对实操频率的直觉比设计推演准 |
| v8.147 | aon 实战截图:v8.146 防线首战 · LEDGER 冲突 AI 手工 union | v8.146 保守把 LEDGER 留给 AI · 实战证明其处置零判断纯机械 | 三方对比纯增行判定 + union 进脚本(名单制 · 非纯增拒动留 AI) | 防线首战全对还暴露了下一刀 —— 实战是最好的边界测试 | 「可枚举」的边界由实战画:AI 重复做同一个零判断动作 = 该物化的信号 |
| v8.148 | 用户连查三轮:ship2→框架反馈通道/年检/项目级总结全是空 | 反馈环只建「写」侧 · 「读/rollup」整个没规范 · 真判例(jdp commit bug)漏掉 | ship2 后落审计到安装目录 docs/audit/(机器数据工具抽 + 三段判断 AI 静默)· 框架跨项目 harvest | 写侧勤勉到贴「供年检」标签 · 收件人(年检)却从没建 —— 寄信给不存在的地址 | 建消费方前先建产物的回收落点 · 否则数据写得越勤漏得越多 |
| v8.149 | 用户:goal 外部评审去掉 · 总挑细节和过度设计 | external 是 MVP(82% 采纳)但 goal=业务对齐阶段 · 技术挑刺错位 · 框架早知道只贴创可贴(让 AI 自筛) | DEFAULT_REVIEW_ROLES goal 去 external(1 处接线)· 防线交 Architect 内审 · 能力 opt-in 保留 | 高采纳率掩盖了阶段错配 —— 「该不该用」要拆到「哪个阶段该用」· 创可贴(让 AI 筛)是该移病因的信号 | 仪式价值是分阶段的 · 一个环节在 A 阶段值≠在 B 阶段值 |
| v8.150 | 用户:AI 处理 review 应先质疑再确认再采纳 · 不盲目认同 | 举证责任不对称:rejected 必给依据是硬的 · ADOPT 只填「改了什么」· adversarial 示例全是 reject 方向 → 盲采成无摩擦默认 | §12 加固定思考顺序(质疑→确认→裁决)+ 举证对称(confirmed 同责)· adversarial schema 双向 | 框架点名了盲采是默认倾向却只防了反向(盲驳)· 设防方向跟病不匹配 | 防反模式要防在「阻力最小的那条路」上 · 防错方向 = 没防 |
| v8.151 | 用户(承 v8.150):相关 brief 要不要对应提示 | v8.150 强化的是被动 spec(要 AI 主动读)· findings 消费那刻 brief 零质疑姿态 = 防线不在决策点 | finding 质疑姿态做成常量接进 external-review emit + review brief(消费时点主动推) | 改 spec 是必要不充分 —— 主动告知型框架里 · 防线得跟到 state.py 推 brief 的那一刻 | doc 是被动的 · brief 是主动的 · 行为改变要落在主动推送处 |
| v8.152 | 用户:REJECT 也要给理由 | v8.151 推姿态进 brief 时只摆明 ADOPT · REJECT 只剩抽象「对称」· 犯了 v8.150 要治的同款不对称 | hint+brief 两个方向都点名实证(ADOPT 给 X · REJECT 给 Y) | 抽象说「对称」不够 · 不摆明的那侧就是会被忽略的那侧 —— 我自己刚踩了一遍 | 对称要逐侧摆明 · 一句「对称」=没对称 · 连改 spec 的人都会偏向新强调的那侧 |
| v8.153 | 用户:disable_heterogeneous_review 改名 disable_external_review | 旧 jargon 名 + 是用户文件里的 key · 硬改会 silent break 别的机器;且 bootstrap additive 自愈会补默认顶翻旧值 | 全量改名 + 两 reader OR 兼容旧名(新或旧 true 即禁)· 顺带校正 exec→subagent stale 注释 | 改用户文件的 key 不能假设没人用 · OR 兼容比 fallback-if-absent 更稳(防 healer 补默认顶翻) | rename 用户配置必留兼容读 · additive 自愈场景要用 OR 不能用「缺则回退」 |
| v8.154 | 用户(承 v8.153):不用兼容 | v8.153 默认按「rename 必留兼容」加了 OR · 但该 key 无任何项目使用 · 兼容是凭空成本 | 撤 OR · 只读新名 · 兼容测试反转为 hard-break 守护 | 「留兼容」是好默认但不是铁律 —— 确知零使用时 · 兼容反而是债(多读一个 key + 误导后人) | 兼容与否看实际使用面 · 不靠教条;v8.145「不留兼容期」同源 |
| v8.155 | 用户:goal 改成草稿后并行派 3 Agent 评审 | 数据证 in-context 的 arch/qa 锚定鼓掌(info-only)· 漏细微 gap · 框架只隔离了 PL 没推广 | goal §3+§5 → 并行 3 隔离冷审循环 · PM 退整合者 · architect.md 翻默认隔离 | 一条贯穿多轮的诊断线(过场→举证→external→subagent)收口成一次流程重构 · 用户的直觉每步都对 | 防鼓掌的真解是隔离不是劝诫 —— 「怀疑者视角防鼓掌」是乐观假设 · 结构(冷审)才靠谱 |
| v8.156 | 用户:归档 INDEX 描述灌了过程信息(评审/测试/全绿) | --archive-desc 引导只说「极简描述」没说业务非过程 · AI 把手头过程数据塞进业务索引 | 引导改「只业务不过程」+ ✅/❌ 对照例 + 过程信号嗅探 WARN | 「极简」不等于「业务」—— 没说清内容类型 · AI 就拿最顺手的(刚跑完的过程数据)填 | 字段语义要说清「写什么类型」· 只说长度/格式 = 留给 AI 自由发挥的空当 |
| v8.157 | TermPro locate-vs-open case:既有行为变更没进用户拍板 | PRD 把默认行为改动登记「取舍」叙述当既定事实 · PL 五问无一覆盖「改既有行为必 ratify」· 8 轮评审打磨实现不回炉前提 | PL 加第六问「既有行为变更」+ §待决策项 硬规则(改既有行为必入) | 评审越细越巩固错框 —— 防的不是漏想是「该停下问的地方没停」· 前提逃逸比细节漏检更贵 | 「取舍」叙述段是 premise 蒙混的温床 · 改既有行为必须显式 ratify 不能 narrate |
| v8.158 | 用户:PRD 机读内容预览隐藏 + 待决策项上移 | frontmatter AC 的 description 机读不用却两处重复撑爆预览 · 决策项埋文档底用户难快读 | frontmatter 瘦留机读字段(description 归 body 人读单源)· 指引转 HTML 注释 · 待决策项移到交付预期下 | 「隐藏机读」的真解常是「去冗余」不是「加注释」—— 先查谁真机读 description(答案:没人) | 重复的人读/机读双份是预览噪音源 · 字段归位(机读进 frontmatter · 人读进 body)比同步两份更省 |
| v8.159 | 用户:PRD 模板瘦身 · 过时/历史背景 | 模板攒了多版迁移考古(重构合并/删字段/历史兼容/方案 A-B)+ 版本演进注 + label 清理留的悬空「：」 | 删死历史(迁移考古/版本注/artifact)· 留有用 why(防漂移/省评审轮等判断依据)· 384→367 | 「历史背景」分两种:死迁移考古该删 · 活的 why(为什么这么定)该留 —— 砍的是「曾经是什么」不是「为什么是这样」 | 文档考古会沉积 · 定期砍迁移注 · 但别误伤非可枚举判断的 why |
| v8.160 | 用户:查其他模板是否有 prd.md 同类问题 | 10 模板攒了贴在活规则上的版本号(v7.3/v8.x)+ 决策考古(撤销 off/已移除字段/上线 Week 计划) | 剥版本号留规则 · 删考古块留 why(治本双副本/残留 WARN)· 假阳性不碰(功能 marker/状态枚举/活约束)· −18 行 | 死历史是横切问题:一个模板有、多半全有 —— 单点修完该同律扫一遍同类 · 但「废弃」分两种(状态枚举 live vs 迁移考古 dead)别一刀切 | 文档考古跨文件沉积 · 修一处后同律 grep 全库 · 核验功能 marker/数据模型再删 |
| v8.161 | harvest docs/audit/(aifriend 20 条 ship2 审计)最高频信号 | external review 评 merge_target...HEAD · 长 WS/stacked 分支累积 → 跨 feature 串味(finding 大半非本 feature)+ 600s 超时 | 进 dev 冻结 pre-dev HEAD 作 review_base_commit · review 外审评 base...HEAD = 本 feature 增量 · 祖先校验+merge_target 兜底 · +11 测试 | 一个症状(串味)+一个症状(超时)常同根:都因 diff base 选了不前进的累积点 · 治 base 一处灭两症 · 比分别加「scope 分流工具」+「分块评审」都简单 | 反馈消费侧(v8.148 audit)第一次产出框架级改进 · 数据驱动定位根因比拍脑袋准 |
| v8.162 | 设计讨论:意图理解偏差谁来兜 | 冷审/PL 只查前提 soundness(理解对的前提下稳不稳)· 抓不了 fidelity(有没有理解对)—— 共享派生产物 = 全锚同一误读 · garbage in 严格验证过的 garbage out | 意图确认提到 prepare 前门(规划→执行膜)· 暴露 AI 补的假设让用户校(唯一 oracle)· setup 塌一行 · 459→417 | fidelity ≠ soundness:前者只能用户校(accountability 不可转移)· 给没确认的前提加更多 AI 严谨反而制造虚假信心 · 把对的门挪到最早位置 = 砍下游 churn 而非加仪式 | 暴露假设(非干净 restatement)是抓误读零件 · 原料早采(user-intent/ai_rationale)只是没 surface · 反转信噪比净更短 |
| v8.163 | 用户追问 prepare 前读代码么 | v8.162 意图门 🧩 没区分「意图假设」(用户可拍)vs「代码假设」(AI 在猜)· 而 prepare 在强制读代码前 → 可能让用户确认 AI 本该去查的代码事实 | 🧩 只列意图解读假设 · 禁抛未验证代码猜测(留 goal 深门)· 一句硬约束 | 浅意图门(读代码前)只能拍「想要什么」· 不能拍「系统怎样」—— 后者 AI 没查就是猜 · 把它当假设抛给用户 = 误导 review · 浅/深分工的边界就在「有没有读过代码」 | 加暂停点/反射时必问「这层有没有 ground truth」· 没有就别让用户为 AI 的猜测背书 |
| v8.164 | 设计讨论:模板该不该留 AI 发散区 | 统一模板平铺必填/按需 · 没给「结构没预见到的」留口 · 既有行为侦测埋在 HTML 注释(被动·模型可能不读)| 三层显式(必填核/按需/开放区)· 开放区=可见尖问题+「实质 or 无+理由」· 既有行为侦测提成主动挑衅 · +14 行 | 开放区不是空白「自由发挥」(得通用废话)· 是逼判断的尖问题 + 必答装置 · 且只放人读(机读禁入保确定性地板)· 侦测=开放/后果=刚性 的 factoring | 模板=活契约:开放区反复出现的 → 该毕业进必填核 · 人人 N-A 的 → 降级 · 边界随数据移 |
| v8.165 | 用户实测 Zed/TermPro 双截图 | v8.158 赌「frontmatter 预览隐藏」· 但 Zed/GitHub 都不隐藏 → 机读 AC 裸露顶部 · 修 viewer 改不了 Zed | 机读契约搬进 <!-- TEAMWORK-MACHINE --> HTML 注释块(所有渲染器隐藏)· 2 解析器优先读块兜底 frontmatter · +6 测试 | 「隐藏机读内容」HTML 注释才到处生效 · frontmatter 只有部分渲染器隐藏 —— 别赌渲染器行为 · 选确定的载体 · 带兜底防 in-flight 破 | 实测>假设:截图 2 个编辑器才暴露「frontmatter 不隐藏」是普遍而非个例 · 跑跑看真能逼出盲区 |
| v8.166 | 用户看 audit 截图要加耗时/模型 | audit 实际数据只有总时长 · 没各阶段耗时(看不出瓶颈)· 没记主对话模型(harvest 没法按模型分析质量)| _stage_durations 抽 duration_minutes 渲染各阶段 + 最耗时% · host 确定性 + model 由 PMO --main-model 声明 · +4 测试 | 确定性的进工具(各阶段耗时/分析 = stage_contracts 现成数据)· 非确定性的留声明(精确 model state 不记 · PMO 知道 → 显式传)· 别把 AI 才知道的硬塞进「工具自动抽」 | 加 arg 先确认绑哪个 subparser(ship-phase vs ship-finalize 两个 · 差点加错)· --help 实测验证 |
| v8.167 | 实战:ui_design 涉交互「降智」(Deli Yang) | designer 角色全是全景机制 · 零交互质量 rubric · 模型对交互没判断力 → 要人逐条喂细节 | 吸收 design-review+frontend-design 的可枚举设计判断成 rubric(交互状态/视觉地板/copy)· host-agnostic · 按扩展既有 app 裁剪 | 把「设计 taste」拆成可枚举 rubric 模型就不需要天生品味 —— 这是 R0(可枚举进脚本)用在设计质量层(之前只用在机制层)· 但选择性吸收:质量地板要、brand-distinctiveness ethos 不要(语境是扩展既有 app 非 greenfield)| 吸收外部 skill = 吸收知识不吸收 skill 本体(host-locked + 重耦合不可移植)· 蒸馏 + 按本框架语境裁剪 |
| v8.168 | 实战:重点 review 指引太粗(ADMIN)| 第一节把 QA/PL 的 NEEDS_REVISION collapse 成「全部 ADOPT」一句 · 用户看不到冷审实际抓了什么 | 改冷审逐条:每条 finding 一句话通俗 + 结论(ADOPT/REJECT/DEFER)· REJECT/DEFER 标重点抽查 · 禁 collapse | 导读的价值是让用户看到「评审实际发现+怎么处理」· 把多条 collapse 成一句省了字但杀了信息 —— 摘要要保留可抽查的颗粒度 · 不是越短越好 | 「照实抄不美化」要延伸到「不 collapse」· summary 藏掉逐条 = 另一种美化 |
