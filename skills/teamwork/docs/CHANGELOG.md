# Changelog

> 📦 本文件**保留最近 5 版**(发布时最旧一版迁入 [CHANGELOG-ARCHIVE.md](./CHANGELOG-ARCHIVE.md))。归档**定期清空**(v8.127 立制 · 完整历史 = git 提交历史 · 不在工作区热存)。
> 🔴 **发版三件套**(同 commit):本文件 entry(细节 · 易逝)+ [RETRO-LEDGER.md](./RETRO-LEDGER.md) 1 行(框架自省蒸馏 · 永久)+ 版本 bump。
> 🔴 **交付止于 push dev**(v8.143 用户拍板):发版**不** rsync 本机安装副本(`~/.agents/skills/teamwork`)—— 本机消费项目与其他机器同路:bootstrap 升级提示(channel 按各项目 `.teamwork_localconfig.json.update_channel` · 本机项目配 `dev`)→ 用户确认 → `update.py` tarball 覆盖。框架仓工作区 ≠ 交付渠道。

## v8.360 · 防过度防护:先问冗余,再算 ROI(用户拍板)

> 起因 case(SVC-CORE payout 两态 · 财务账本):TECH 列了 7 项 DB 守卫。用户「有必要做这个 check 么,是不是代码保障就好了」→「**你看下还有哪些不必要的 check,减少过渡保护**」→ AI 第二轮自己分出三组,**第一组「纯冗余,建议全删」**:up 前置断言「无 `status='failed'` 行」(紧接着重建的列 CHECK 对存量行本来就会 23514)· 三条常驻 grep 门「零引用」(删掉的表和函数编译期就报错)——「它们只是把后面必然发生的失败提前说清楚,**删了行为不变**」。
> 🔴 **这些冗余是在冷审 26 条全部处置、Round 2 双路 APPROVE 之后,靠用户追问两次才挖出来的。** 框架明明有 ROI 规则(v8.266/v8.279)却没拦住。

### 三层原因
- ① 🔴 **分类漏网(决定性)**:ROI 算账的对象定义是「降级兜底/安全兜底(fallback / degradation / 重试熔断 / 防御层)」= **出错时怎么办**(fail-safe);而 DB CHECK / trigger / 前置断言是**不让错发生**(guard)。实证:那份 TECH 白纸黑字写着「🛡️ **兜底清单:无**」,同时挂着 7 项 DB 守卫 —— **逐项算账、填 ROI 表、随暂停点透出**这整条流程从未触发。用户看到的是「变更明细」,而那张表问的是「为何非更简方案不可」= 为什么要**改**它,不是为什么要**有**它。
- ② **ROI 对守卫天然算得过**:财务账本脏数据后果极高 → 概率×后果永远立得住。v8.279 自己就写了「安全加固/兜底降级**听着最负责任故最难驳**」。而 AI 第二轮用的判据不需要估概率:**「删了行为会变吗」**,当场可判。
- ③ **冷审拦不住**:冷审的 mandate 是找缺陷,而「多一道无害的断言」读起来是更严谨 —— **防护读起来永远像更负责任**(与 v8.354「限制读起来永远像最小范围」同族)。

### 变更
- **清单正名 + 扩容**:§兜底清单 → **§兜底与守卫清单**,显式纳入 **DB CHECK / 约束 / trigger / 前置断言 / 运行时校验 / 常驻门禁(grep 门、lint 门)**。暂停点触发条件、blueprint 透出段、goal/blueprint brief、external 协议**六处消费方一并更新**(先 grep 出全部消费方再改 —— v8.356 的教训)。
- **前置判据(可判问句 · ❗ 顺序不可换)**:**这道防护删掉之后,行为会变吗?**
  - **不变**(后面必然有另一道拦住同一件事)→ **纯冗余,直接删** · **不进清单、不用算 ROI**;
  - **只在绕过正常路径时变**(运维直写 / 迁移脚本 / 将来绕过 service 的函数)→ 有取舍 → 进清单算 ROI,**随暂停点给用户拍板**;
  - **正常路径就变** → 那不是防护,是**功能**,归各自章节。
  🔴 **为什么顺序不能换**:ROI 对纯冗余也会算出「保留」—— 成本才十几行、后果却极高,于是永远立得住。三分法正是那个 AI 第二轮自己得出的,**框架里没有这个分法**,所以它第一轮没做。
- **清单表加「🔴 删了行为会变吗」列**,并写明「答『不变』的**不该出现在本表**」。
- **blueprint ⚔️ 对抗段补守卫版判据**(复用 v8.354「限制必要性」的形状):**这道守卫是需求要求的,还是我加的?**「我加的」→ 要么说出「不加会出什么事」**且那件事不会被别的机制拦住**,要么删。
- **external 协议同步**:external 天然偏加防御层 → 其 finding 裁决也是**先问冗余再算 ROI**,答「不变」直接 REJECT。
- 📎 撞 v8.283 价值门(🔴 规则 39 条 vs 3 处 why = 13.0 > 12)→ **服从门裁**:把新增规则的实证理由显式标成 `why:`(而不是削 🔴),比值回到 7.8。

- test_guard_redundancy_v8360(16 · 含「新判据不吃掉既有 ROI/透出」的回归锁)· 全量 **1792 passed**

## v8.359 · WS 拆解明细:去「人维护」+ 用户决策事实入槽(用户拍板)

> 用户(WS-48 财务链路截图):「这个**人维护**去掉,ws 这里 feature 补充一点可以详细一些,**不能丢用户决策的事实**」。

### 两个问题都真
- ① §拆出的 feature 标着「规划态 · **人维护**」,但这节恰恰是 **feature-planning 流程产出的** —— 标注会让 AI 以为不归它写,于是写薄或不写。
- ② 槽位只有 范围 / 依赖 / **高层 AC**,**规划期用户拍的板没有落点**:选了哪个方案(税务方案 A 不是 B)、打款 failed 分几态、定了什么口径 —— 全留在规划当时的对话里。

> 🔴 **这是 v8.353「已确认意图入 PRD」的上游断点**:v8.353 把 prepare 确认的意图搬进了 PRD,但更早一层(WS 规划期的决策)仍然只活在对话里。换 session / 派 subagent 就没了,下游 PM 只能凭「范围」一句去猜用户当初要什么 —— 而**意图错了是唯一一类下游全部质量门都拦不住的错**。

### 变更
- **去掉「人维护」标注**;`ws-lint` 新增一条:存量 WS 仍写该标注 → 报出(否则它会一直被抄下去)。
- **新增 `features[].user_decisions`(机读)+ 正文 🔒「用户已拍板的」段**:选了什么 / 排除了什么(并标「做不到」还是「我们选的边界」)/ 定了什么口径 · 🔴 **原样搬不润色**(润色 = 二次解释,偏差正是这么进来的)。
- **`ws-lint` 查槽位存在**(逐件点名缺的那些)· 🔴 **不查非空** —— `[]` 是合法值(该件未经用户单独拍板);强制非空会**逼出编造的「决策」**(与 v8.358 同一判断:必填 + 无出口 = 幻觉配方)。
- **链条写明**:这些事实是起 feature 时 `init-feature --user-intent` 的**输入**,再落进 PRD §已确认意图 —— 载体到位还不够,不说清往哪传,填了也没人用。
- **拆解明细改为「宁详勿略」**:原措辞「核心 AC(**高层** · 详细 AC 在 Feature PRD)」会诱导写薄,而这节是下游**唯一**能拿到规划期上下文的地方;AC 可写到具体字段 / 路由 / 状态值。
- 📎 ws-lint 拿到的是**文本**而既有 `_parse_ws_features` 收 `Path` → 拆出 `_parse_ws_features_text` 两边共用,**没写第二份扫描器**(双实现必漂 · v8.352 `_flow_key` 漏 floor 即实证)。

- test_ws_user_decisions_v8359(13)· 全量 **1776 passed**

## v8.358 · 多轮审查的幻觉风险:范围锁定 + 落点举证(用户提问 → 拍板)

> 用户:「多轮审查是否容易幻觉出来一些不存在的场景」→「落」→「**这个场景在本 feature 的真实输入下会发生吗,这个反问会应用在 goal 和 tech 阶段么**」。

### 查证:防御很完整,但**装反了**
现有机制相当强:`code_evidence` 明写「防止**猜测式 finding**(reviewer 没读代码就提问题)」· 逐条裁决**质疑 → 确认 → 回读真实代码** · `adversarial_self_check`(ADOPT 前先写这条不成立的最强反方)· 对抗复现「**复现不出降 MINOR**」· rejected 词表含 false positive。年检数据也佐证它在起作用:external 采纳率 **82.3%** —— 每 6 条就有 1 条被判不成立驳回。
🔴 但 `grep -c "禁全量重扫|范围锁定"` → **review=1 · goal=0 · blueprint=0**,而 goal-stage 原文要求 Round 2+「核实 fix + **找新**」:
- **review 有代码可 grounding**,finding 能被回读当场证伪 —— 却锁得最严;
- **goal/blueprint 是纯文档评审**,「这个需求可能有问题」既证实不了也证伪不了 —— 却允许并鼓励找新。Round 1 把真问题找完后仍被要求找新,**剩下能找的只有边角**。

### 📎 v8.355 加剧了这件事(自陈)
新增的 ⚔️ 对抗段(「不许写 ✅」)与 💡 清单外洞察(「≥1 条」)恰恰是**必填产出 + 零证据要求**的组合 —— `code_evidence` 只管 `technical-consistency`,**够不着 `premise-challenge` 和清单外洞察**;而 💡 那段本就是让人「想出清单上没有的东西」,是幻觉最高发的形态。两条都留了「无 + 理由」出口(这点是对的),但**出口存在 ≠ 压力不存在**:模型面对必填格,倾向填满而不是填「无」。

### 变更
- **验证轮范围锁定推广到 goal / blueprint**(协议单源 review-stage 规则 5):只核实上轮 finding 处置 + **修订处的连带影响** · ❗ **禁全量重扫**(新 finding 仅限出自修订处、或 BLOCKER 级并附「**为何首轮未发现**」)· rejected 不得复提。goal 原文的「核实 fix + **找新**」删除。
- **⚔️ 与 💡 必须落地**:① **指认落点** —— 这条质疑/洞察对着 PRD/TECH/代码的**哪一句**?说不出具体落点 = 不成立,撤回;② **可判问句:这个场景在本 feature 的真实输入下会发生吗?** 说不出具体触发条件 → 降级或撤回(与「复现不出降 MINOR」同构)。并明写「**填满不是目的 —— 编一个出来比空着更贵**」。

### 🔴 用户追问逼出的关键分流:goal 是例外
「真实输入下会发生吗」在 `blueprint`/`review` 成立(有确定的方案/实现作参照),**在 `goal` 不成立**:
- PRD 阶段**正在定义「什么是真实输入」** → 拿它驳回质疑是**循环论证**;
- 更危险的是它会给「**悄悄收窄范围**」一个正当理由 —— v8.350 的实证事故正是这个形状(狭义解释某能力并排除另一半,PRD 完全自洽,dev 与 review 都在认真验证一个错误的范围定义)。
→ **goal 阶段说不出触发条件 = 范围未定 → 进 §待决策项让用户拍板,不许撤回**,与规则 4.5「这个排除是**做不到**还是**我选的边界**」同一判据。**两条规则方向相反,必须显式分流,否则防幻觉会吃掉防范围收窄。**

- test_multi_round_hallucination_v8358(12 · 含「两条规则互不吃掉」的回归锁)· 全量 **1763 passed**
- 发版三件套 · 🔴 54(门 <55)

## v8.357 · 档位覆盖:不要用高档模型做底智商任务(用户拍板)

> 起因 case(aon-main DEV-F260910162653):用户问「你做测试用的是什么模型」,那边 AI 答「实现路继承 Fable 5.1 · 验证档任务用 sonnet · 正式测试证据另派 sonnet」—— **完全合规**(白名单本来就写着「执行测试 · 单测 · 集成测试 · e2e」降验证档)。用户接连拍板:「涉及测试的都降档」→「测试和验证类任务都要降档」→「**你全局检查下,不需要用高档模型做底智商任务**」→「ship 也用验证档模型」→「micro 默认使用执行档」。
> 🔴 **载体归位**:那边的 AI 把拍板写进了项目级 memory(`~/.claude/projects/<项目>/memory/`)—— 模型分档是**框架级**规则,写进项目 memory = 换个项目就失效,下个项目的 AI 还会把同样的问题再问用户一遍。

### 全局扫描查出的缺口(不是「没写规则」,是**规则没覆盖到的面**)
- ① **白名单只管「派发时定档」,管不到已派出的执行型 agent 内部做什么** —— 实现路继承主模型,却在里面跑**全量 vitest + 变异验证** = 拿深度档干机械活。技术上实现 subagent 不能中途换模型 → 只能划边界:实现路只跑**与本路改动直接相关的最小自测**(保住 TDD 红绿循环不跨 agent 往返),**全量套件 · 变异验证 · 基线指纹采集 · `xx-complete` 正式证据 → 一律另派验证档**。
- ② 🔴 **`test-stage` 零档位声明**(`browser-e2e` 有、`test` 没有)—— 整个 stage 就是「把『实现说它好了』变成机器可验的通过证据」,却默认继承会话主模型。**最大漏口**。
- ③ **`execute-stage` 零声明** —— micro 的准入白名单已经把改动卡死成零逻辑变更(文案/样式/资源/配置常量/注释),**门槛最低的 stage 反而没有档位约束**。
- ④ **`ship-stage` 零声明** —— push / MR / 合入监控 / CI 日志拉取与归因 / worktree 清理 / 归档誊抄,全是搬运活。
- ⑤ 🔴 **一处真冲突**:白名单写「**冷审执行**」降验证档,而 goal/blueprint 写「**冷审**必用主模型 · 不许降到验证档」—— 两条硬规则直接打架。

### 变更
- **白名单正名**「**测试与**验证类白名单」(原名让人以为不管「测试」· 同 §Out of Scope 与限制 / §查询性能与数据量 的正名问题)+ 补齐 **变异验证 · 全量套件跑批 · 基线/指纹采集 · 覆盖率统计**。
- **定档判据换成可判问句 · 三档各一句**(原「校验/枚举型 vs 判断/创造型」仍是**形容词式分类**,分不了界,且「执行」档在判据里根本没出现):① **照着已有清单核对** → `验证`;② **按既定方案把东西写出来** → `执行`;③ **要想出清单上没有的东西** → `深度`(不许降)。❗ **换个便宜模型产出不会更差的,就没有理由用贵的** —— 会话主模型是为了跟你对话选的,不是为了跑测试和改文案选的。
- **修 ⑤ 的冲突**:白名单「冷审执行」→「**冷审验证轮**(Round 2+ 核实上轮 finding 修没修)」· 明写「**首轮全量冷审属判断型,不降**:它要找的是没人想到的东西」。两侧口径就此一致。
- **逐 stage 补档位声明**:`test`(整 stage 测试与验证类)· `ship`(用户拍板 · 🔴 **保留一个例外**:CI 红归因到本 feature 后**要改代码**那段是实现活,按实现路的档走)· `execute`(**执行档** —— 用户拍板;micro 改的是文案/常量,但那仍是「**写出来**」不是「照着清单核对」,按判据归执行不归验证)· `dev`(内联边界 + 「测试节奏 AI 自定,但**模型档位不自定**」)。
- **判断型三个 stage 确认不加降档**:`diagnose`(根因排查)· `ui-design`(设计)· `pm-acceptance`(产品视角验收决策)—— 降档会直接伤产出。

- test_model_tier_coverage_v8357(14)· 全量 **1751 passed**
- 发版三件套 · 🔴 54(门 <55)

## v8.356 · v8.355 的接线补全(四路 subagent 审计 · 用户要求)

> 用户:「现在你派几个 subagent 整体 review 一下,看下是否有描述冲突、冗余等问题。」
> 三路审计共报 **33 条**,逐条回读核实**全部成立**,其中 **2 条 P0 + 1 条 P1 是真 bug**。
> 🔴 **根因是方法错误**:v8.355 按「**我想到的载体**」逐个改,而不是**先 grep 出全部消费方再改** —— 框架自己就有这条规则(「改契约必 grep 消费方 · 不凭记忆」),**没对自己用**。整批漏掉的文件:`claude-agents/` · `FLOWS.md` · `ROLES.md` · `docs/prepare.md` · `docs/conventions.md`。

### 🔴 P0-1 · 0 路冷审是个死锁(门在逼 AI 造假)
`PRD-REVIEW.md` 的产物 spec **没挂 `review_artifact=True`** —— v8.355 新加的「按路数跳过评审产物」对它**永不生效**;而全仓唯一挂了标记的 TECH-REVIEW 只在 blueprint 出现,blueprint 只在 medium/full 上链且 roster 恒非空 → **那条改动在任何默认档上都是 no-op**,CHANGELOG 里「lite/tiny 同受益」当时是**一句空话**。
实测复现:lite 档(goal 有意 0 路)只交 PRD.md → FAIL;如实写 0 路记录 → 仍 FAIL;**只有编造一条不存在的裁决才能过**。
- 修:`PRD-REVIEW.md` 挂 `review_artifact=True`;`review_after_primary` / `prd_verdicts_all_pass` 补 0 路放行。
- 🔴 修的过程里**又栽了一次 v8.305 的坑**(把「roster 未初始化」当成「有意 0 路」,对存量放松,被既有测试当场抓出)→ 谓词抽成 `_stage_lanes_deliberately_zero` 单函数,并加锁禁止就地重写。

### 🔴 P0-2 · brief 与门直接对立(默认档就踩)
goal brief 写着「roster 里没有的角色 gate 自动放行(**如去 pl → PL 质疑免**)」,而门 v8.355 起按路数判。medium 档(`goal: ["external"]`)照 brief 写完整单路冷审 → `goal-complete` FAIL,且是唯一拦截项。
- 修:三份 brief 的派发段与「结果(完成判定)」段全部改为**N 路同一份清单**、逐路交三段;装配卡去掉角色轴(`路数×角色×模型` → `路数×模型`、`几路×谁` → `几路×什么模型`)。

### 🟠 P1-3 · external lane 的 prompt 里根本没有统一清单(写入端没接)
`claude-agents/reviewer.md` 是 external 冷审 subagent 拿到的**唯一**指令,仍是 pre-v8.355 的 C1–C6 角色式 checklist。实测渲染出的 prompt 关键词计数:`清单外洞察 0 · 对抗 0 · 证否 0 · coverage 0`。而 tiny/lite/medium/Bug 四条默认路径上,review 的**唯一**冷审路就是 external —— 等于 v8.355 的统一清单**在真正干活的那一路上从未生效**,coverage 只能由主对话代笔,而主对话代笔正是该 stage 明令禁止的热审。
- 修:prompt 模板换成统一三段(⚔️ 按 target 给对抗内容:prd=质疑七问 / blueprint=rival 设计强制 / code=防御与限制必要性)+ 输出 schema 补 `coverage` / `challenge` / `outside_checklist_insight`;测试**验渲染出来的那份**,不只验模板。

### 其余修复
- **coverage 门仍是角色白名单**(`_REVIEW_MAIN_ROLES = ("architect","qa")`):roster 为 `["pl"]` / `["fast"]` 时与 `cross_review_coverage` 双双放行 = **零 coverage 强制** → 改为「除 external lane 外每路都要」。
- **💡 洞察门对 lane-first 布局判空**(`## 💡 清单外洞察` + `### <lane>`,与 REVIEW.md 的 per-lane 子键同构):窗口遇任何标题即断 → 改为只在**同级或更高级**标题处断;占位符原样与单字「无」仍被正确拒绝(放宽窗口 ≠ 放松门)。
- **文档面 28 处**:`goal-stage` 门清单旧触发条件 + 漏列新门 · 产物契约按角色分段 · 清单错置在「不强制全用」的手段菜单里 · medium 仍写 `[fast]`;`ROLES.md` / `FLOWS.md` / `docs/prepare.md` / `docs/conventions.md` 整批补齐;`roles/` 与 `templates/prd.md` 的角色席位残留。

### 🔴 三段规则改为单源(「双载体必漂」的现场教训)
v8.355 把三段的完整表述**在 SKILL 和三份 stage doc 各写了一遍且没标单源** —— 当场就漂了三处:goal 专属的「质疑七问」被写成通用底线(review 的对抗点与七问无关)· ⚔️ 段禁写口径三处不同(goal 禁「✅ 或查过无发现」、另两处只禁 ✅)· ⚔️ 段的「≥1 条或显式无」只有 goal 写(blueprint/review 的空对抗段不违反任何成文要求)。
- 修:**SKILL 成为三段通用要求的单源**(证否句式 / 禁写项 / 数量),三份 stage doc 只写**本 stage 查什么** + 指向单源;测试改为「验单源 + 验引用」,不再要求各处重述。

- test_derole_wiring_v8356(19)· 8 份既有测试按新语义重锚 · 全量 **1737 passed**
- 发版三件套 · 🔴 54(门 <55)· prd.md 339 行(门 <340)
